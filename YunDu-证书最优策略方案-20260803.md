# YunDu 证书最优策略方案

> 日期：2026-08-03
> 范围：面板 + node-agent + 订阅渲染的证书策略整体设计
> 基线：yundu-src（node-service/node-agent 均已部署本地 v0.7.22 修复版本）

---

## 一、目标与原则

1. **零 SSH**：证书的申请、签发、下发、续期、替换全部由面板控制面完成，节点 agent 只负责应用与热替换。
2. **按节点类型分层**：不搞“全真实证书”或“全自签”一刀切，伪装性、安全性、运维成本按协议自动平衡。
3. **客户端零手动**：订阅渲染自动携带正确的 TLS 校验参数，用户不需要填 pin、不需要开全局 insecure。
4. **伪装优先**：直连节点默认借用大厂 CDN 域名做 SNI（如 `cn-beijing.cdn.aliyuncs.com`），不暴露机场自有域名。

---

## 二、节点分类与默认策略

| 节点类型 | 默认策略 | 证书来源 | 客户端校验 | 伪装性 | 说明 |
|---|---|---|---|---|---|
| REALITY（vless/xhttp + reality） | `none` | 无证书，`dest` 大厂域名伪装 | REALITY 公钥校验 | 最高 | 服务端不持有证书，不申请、不续期 |
| 直连 TLS/UDP（trojan/anytls/ss+tls/hy2/tuic） | `fixed_self_signed` | 面板持久化自签（cert_bundles） | 用户显式选择：`allow_insecure` 或 `pinSHA256` | 高 | SNI 用伪装域名，不暴露真实域名 |
| CDN/Tunnel（ws/grpc/xhttp/httpupgrade） | 边缘证书 | nginx/CF 真实证书，源站自签兜底 | 正常校验边缘证书 | 高 | 源站不直接面对客户端证书校验 |
| 严格校验 / 合规业务节点 | `auto_acme` | 面板 ACME 真实证书（DNS-01） | 严格校验 | 中 | 显式切换，暴露自有域名 |

### 2.1 策略字段

节点新增/复用 `cert_policy`，取值范围：

- `none`：REALITY，不参与证书流程。
- `fixed_self_signed`：持久自签（默认，覆盖绝大多数直连 TLS/UDP 节点）。
- `auto_acme`：自动申请真实证书并绑定节点。
- `bundle`：显式绑定已有证书包（cert_bundle_id）。

默认值由 TerminationClass 自动推断，无需用户选择；用户可显式覆盖。

---

## 三、自签直连节点设计（核心）

### 3.1 持久化自签

现状：自签证书在 node-service 进程内存缓存，进程重启后重建，指纹变化会导致已下发的 pin 失效。

目标：自签证书写入 `cert_bundles`（`provider=self-signed`，版本号递增），首次生成后全节点共享同一份 PEM：

- `injectCertFromBundle` 四级回退中，自签兜底改为“查 cert_bundles → 无则生成并持久化 → 有则复用”。
- 指纹稳定后，客户端 pinSHA256 长期有效。

### 3.2 客户端校验：用户显式选择，系统只提示不覆盖

原则：**显式优于隐式**。`allow_insecure` 是安全语义，SNI 伪装是流量特征，系统不自动写入，
只负责判定证书来源、给出建议值、做一致性校验。节点创建者保留完全控制权。

代码事实：

- sing-box 系客户端（SFA/SFI/SFM/Karing/Hiddify，项目主客户端）TLS outbound 只支持 `insecure`，不支持 hex pinSHA256 字段。
- Clash Meta 走 `skip-cert-verify`，同样不消费 pinSHA256。
- URI 格式已支持自动推：hysteria2 `pinSHA256`、Xray 系 `pinnedPeerCertSha256`。
- xray 在当前项目只负责 xhttp/reality 节点：reality 不持证书，xhttp CDN 走 nginx/CF 边缘证书，均无“客户端验证自签”场景。

结论：

- 新建直连 TLS 节点时，表单提供建议默认值（`allow_insecure` 复选框、SNI 候选），但不自动写入配置；用户显式勾选/填写。
- `allow_insecure` 勾选后订阅渲染全客户端自动生效（sing-box `insecure`、Clash `skip-cert-verify`、Shadowrocket/Quantumult 对应字段）。
- `pinSHA256` 保留为 URI 高级选项，供支持客户端的强校验场景使用。
- 长期增强：sing-box 渲染器支持内嵌 `certificate`（PEM 信任锚），自动把自签证书塞进订阅，客户端严格校验且零手动，是比 hex pin 更适合本架构的方案。

### 3.3 自签判定与一致性校验

- 创建节点时无法预先判定最终证书来源，判定发生在保存/渲染之后：复用 `injectCertFromBundle` 四级回退逻辑，抽出只读 `ResolveCertSource`，返回 `cert_source`（cert_bundle / san_match / tls_certificates / self_signed）与证书指纹。
- 节点编辑页展示只读“当前证书来源”状态（自签兜底含指纹、证书包、ACME 等），用户据此决定 `allow_insecure` 与 pin。
- 保存前一致性校验（仅警告，不阻断）：最终来源为自签、且 `allow_insecure=false`、且 `pin_sha256` 为空时，提示“客户端将校验失败”，用户可显式确认后继续。

### 3.4 伪装 SNI 管理

- 不自动填充 SNI；表单提供常用伪装域名候选下拉（`cn-hnzz-cm-01-01.bilivideo.com`、`cn-beijing.cdn.aliyuncs.com`、`dl.google.com`、`www.primevideo.com` 等），用户自行选择或填写。
- 校验规则：直连 TLS 节点 SNI 非空；禁止用机场自有域名作为直连节点 SNI，防止暴露机场身份。
- hy2/tuic 等 UDP 直连节点 SNI 不参与 DNS 解析，伪装域名可直接使用。

---

## 四、真实证书（auto_acme）设计

### 4.1 现状缺口

- 面板“TLS 证书”页已支持 ACME 自动/自签/上传/CF Origin，后端有 lego 申请、自动续期、续期后同步 cert_bundles。
- 节点编辑器有 `cert_mode=acme + acme_domains/acme_email` 表单，但后端没有消费逻辑，单节点 ACME 未闭环。
- agent 侧 ACME 默认 DNS 模式，但节点环境无 `CF_Token`，实际不启用。

### 4.2 目标流程

```text
节点保存/创建
  → cert_policy=auto_acme 或节点缺证书
  → 面板创建 tls_certificate（ACME，DNS-01）
  → 自动 ObtainCertificate
  → 写入 cert_bundles
  → 回写节点 cert_bundle_id
  → 下次渲染注入真实 PEM 并随 TLS materials 下发
  → 15 天阈值自动续期 → 同步 cert_bundles → 原子热替换
```

前置条件：面板配置默认 ACME 账户（email + DNS provider + 加密凭证），复用现有 `acme_credentials_encrypted` 加密存储。

---

## 五、证书下发与热替换（已具备，保持不变）

1. 渲染时 `injectCertFromBundle` 四级回退：cert_bundle_id → SAN 匹配 → tls_certificates → 自签兜底。
2. PEM 通过 payload `TLSMaterials` 随加密 manifest 下发。
3. agent `SetContentPEM` 原子写 `/etc/yundu/certs/{domain}/`，xray/sing-box/nginx 共享。
4. 证书内容稳定时配置 hash 不变，不触发版本风暴（limiter 排序修复已保证渲染确定性）。

---

## 六、与优选 IP / CDN 的兼容

- CDN 回源 Host 固定为 `server_name` 后，优选 IP 只改变 TCP 目标地址，TLS SNI 仍是域名，nginx 按 SNI 路由不受影响；固定 Host 反而避免 Host 头与 SNI 不一致导致后端校验失败。
- 约束：节点 transport host 必须与 SNI/server_name 一致（现网已核对一致）；建议补 precheck，不一致时告警。
- UDP 直连节点（hy2）不经过 nginx，与固定 Host 无关。

---

## 七、落地改造清单

| 文件 | 改动 |
|---|---|
| `apps/node-service/internal/service/termination_class.go` | 按类型推断 `cert_policy` 默认值 |
| `apps/node-service/internal/service/node_service.go` | 保存/创建钩子：按策略自动建证书、绑定 bundle、触发 ACME |
| `apps/node-service/internal/cert/service.go` | 新增 `AutoProvisionForNode`：申请 → cert_bundles → 回写节点 |
| `apps/node-service/internal/service/deployment_service.go` | 自签兜底改为读写 cert_bundles（持久化 + 版本）；抽出 `ResolveCertSource` |
| `apps/node-service/internal/service/node_service.go` | 保存前证书一致性校验（自签 + 非 insecure + 无 pin → 仅告警） |
| `packages/subscription/renderer/uri.go` | 保留 pinSHA256 输出，统一 AllowInsecure 语义 |
| `packages/subscription/renderer/singbox.go` | 可选支持 `certificate` PEM 内嵌（长期增强） |
| `apps/admin-web/src/components/nodes/NodeConfigEditor.tsx` | cert_policy 选择 + 伪装 SNI 字段 |
| `apps/admin-web/src/pages/TLSCertificates.tsx` | 默认 ACME 账户（email + DNS provider + 凭证） |

---

## 八、上线步骤

1. **阶段一（判定与提示）**：抽出 `ResolveCertSource`，节点编辑页展示证书来源；表单提供 `allow_insecure` 与伪装 SNI 的建议默认值（不自动写入）；保存前做“自签 + 非 insecure + 无 pin”一致性告警。自签证书持久化到 cert_bundles。修复 P29 等当前 `allow_insecure=false` 导致客户端失败的问题（通过告警引导，不静默改配置）。
2. **阶段二（自动绑定）**：节点保存时自动匹配/绑定已有 cert_bundle；真实证书手动创建后一键绑定。
3. **阶段三（全自动 ACME）**：配置默认 ACME 账户后，`auto_acme` 节点全自动申请/绑定/续期，实现真正零 SSH。

---

## 九、运维检查清单

- 订阅中自签节点应出现 `insecure=1`（或 URI `insecure=1`），真实证书节点应为 `insecure=0`。
- cert_bundles 中 `provider=self-signed` 的条目应稳定存在，指纹不随重启变化。
- 直连节点 SNI 不出现机场自有域名。
- 每 10 分钟 config_versions 增量接近 0；证书续期后 hash 变化属正常单次变更。
- 节点证书目录 `/etc/yundu/certs/{domain}/` 中的 PEM 与面板 cert_bundles 一致。
