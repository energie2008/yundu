# YunDu 订阅链路实现 + 编译部署标准化总结

> **日期**: 2026-08-08  
> **代码基线**: `main` 分支  
> **验证环境**: VPS190 (43.135.147.190) / VPS206 (158.101.13.206)  
> **核心目标**: 订阅设置后端全链路落地 + VPS190 标准化 + 编译部署闭环  

---

## 目录

1. [任务目标与成果](#1-任务目标与成果)
2. [架构设计与数据流](#2-架构设计与数据流)
3. [实现详情（9 个文件）](#3-实现详情9-个文件)
4. [编译部署流程](#4-编译部署流程)
5. [数据库修复（5 个缺失迁移）](#5-数据库修复5-个缺失迁移)
6. [nginx 配置修复](#6-nginx-配置修复)
7. [VPS190 标准化清理](#7-vps190-标准化清理)
8. [问题与解决思路](#8-问题与解决思路)
9. [难点与未解决冲突](#9-难点与未解决冲突)
10. [当前进度](#10-当前进度)
11. [后续如何更换订阅地址](#11-后续如何更换订阅地址)
12. [避坑指南](#12-避坑指南)
13. [接手人注意事项](#13-接手人注意事项)
14. [发现的问题](#14-发现的问题)
15. [下一步具体执行步骤](#15-下一步具体执行步骤)
16. [2026-08-08 闭环补充：未完成任务完成情况 + 优化发现](#16-2026-08-08-闭环补充未完成任务完成情况--优化发现)

---

## 1. 任务目标与成果

### 1.1 任务目标

| 序号 | 目标 | 状态 |
|------|------|------|
| 1 | 订阅设置后端实现（路径/域名/密钥/显示方式/随机订阅） | ✅ 完成 |
| 2 | user-web 动态获取订阅域名（被墙后免重建切换） | ✅ 完成 |
| 3 | admin-web 前端编译部署到 vps190 生产环境 | ✅ 完成 |
| 4 | 后端服务（identity/subscription/api-gateway）编译部署 | ✅ 完成 |
| 5 | nginx 配置补齐 `/api/v1/client/subscribe/*` 路由 | ✅ 完成 |
| 6 | 数据库缺失迁移补齐（migrations 61-64, 69） | ✅ 完成 |
| 7 | VPS190 残留服务清理标准化 | ✅ 完成 |
| 8 | 订阅链路端到端验证 | ✅ 完成 |
| 9 | VPS206/VPS81 node-agent 标准化检查 | ✅ 已完成（sing-box api 字段问题已根因定位并修复，双核 node-agent v0.8.2 升级） |

### 1.2 核心成果

```
用户被墙 → 管理员面板改 subscribe_domain → user-web 下次加载自动跟随 → 零 SSH / 零重建
```

- **订阅域名动态化**：user-web 启动时 `GET /api/v1/guest/config` 获取 `subscribe_domain`，不再硬编码
- **双路由兼容**：`/sub/<token>`（短路径）+ `/api/v1/client/subscribe/<token>`（Xboard 兼容）同时注册
- **HMAC 签名**：`X-Subscribe-Signature: sha256=<hex>` 防篡改
- **显示方式控制**：`show_method` 0=不下发 userinfo / 1=完整
- **随机订阅**：Fisher-Yates 部分洗牌算法抽样节点

---

## 2. 架构设计与数据流

### 2.1 订阅设置数据流

```
admin-web (Settings.tsx)
    │ PUT /api/v1/admin/system/settings/subscribe/subscribe_domain
    ▼
identity-service (setting_handler.go → UpdateSetting)
    │ 写入 system_settings 表 (setting_group='subscribe')
    ▼
subscription-service (subscribe_settings_repo.go)
    │ 直读 system_settings.subscribe 组，60s 缓存 + 事件主动刷新
    ▼
user-web (App.tsx → endpoints.ts)
    │ GET /api/v1/guest/config → 获取 subscribe_domain / subscribe_path
    │ 生成订阅 URL: https://<subscribe_domain><subscribe_path>/<token>
    ▼
客户端 (Clash/sing-box/karing/hiddify)
    │ GET https://ad.tiktokplay.na.am/sub/<token>
    ▼
nginx (ad.tiktokplay.na.am.conf)
    │ location ~ ^/(sub|s)/ → proxy_pass http://127.0.0.1:8083
    │ location ~ ^/api/v1/client/subscribe/ → proxy_pass http://127.0.0.1:8083
    ▼
subscription-service (8083)
    │ 渲染订阅配置 → 返回 sing-box/xray JSON 或 URI 列表
    │ 响应头: Subscription-Userinfo + X-Subscribe-Signature
```

### 2.2 双路由设计

| 路由 | 转发目标 | 用途 |
|------|---------|------|
| `/sub/<token>` | subscription-service (8083) | 短路径，用户友好 |
| `/api/v1/client/subscribe/<token>` | subscription-service (8083) | Xboard 兼容（老客户端） |
| `/api/v1/guest/config` | api-gateway → identity-service (8081) | user-web 启动配置（无鉴权） |

> **关键决策**：`subscribe_path` 设置仅控制 user-web 生成哪个 URL，后端双路由始终注册（避免重启）。

---

## 3. 实现详情（9 个文件）

### 3.1 后端（Go）

#### `apps/subscription-service/internal/repo/subscribe_settings_repo.go`（新建）

- 定义 `SubscribeSettings` 结构体（8 个字段）
- 60s 内存缓存 + `Reload()` 事件主动刷新
- 直读 DB `system_settings.subscribe` 组（与 node-service ACMEDefaultsRepo 同模式）
- DB 错误返回默认值不阻断订阅

```go
type SubscribeSettings struct {
    SubscribePath    string // 默认 "/sub"
    SubscribeDomain  string // 留空则 user-web 用请求 Host
    SubscribeKey     string // HMAC-SHA256 密钥
    ShowInfoToServer bool   // 是否回显节点信息
    ShowMethod       int    // 0=不下发 userinfo / 1=完整
    IsRandSub        bool   // 随机订阅
    RandSubStart     int    // 随机下界（含）
    RandSubEnd       int    // 随机上界（不含）
}
```

#### `apps/subscription-service/internal/service/subscription_service.go`（修改）

- 注入 `settingsRepo`
- `loadSubscribeSettings()` — 读设置，nil 时返回默认值
- `sampleRandomNodes()` — Fisher-Yates 部分洗牌随机抽样
- `GetSubscription()` — 按设置控制渲染（随机/显示方式/HMAC 签名）

#### `apps/subscription-service/internal/handler/subscription_handler.go`（修改）

- `show_method=0` 不下发 `Subscription-Userinfo` 头
- `subscribe_key` 非空时生成 `X-Subscribe-Signature: sha256=<hex>` HMAC-SHA256

#### `apps/identity-service/internal/handler/setting_handler.go`（修改）

- 新增 `GetPublicConfig()` 方法
- 暴露白名单字段：`subscribe_domain` / `subscribe_path` / `app_name` / `frontend_url` / `app_description`
- **绝不返回** `subscribe_key`（安全约束）

#### `apps/identity-service/internal/app/app.go`（修改）

- 注册路由：`api.GET("/guest/config", settingHandler.GetPublicConfig)`

#### `apps/api-gateway/internal/app/app.go`（修改）

- 新增路由：`r.Any("/api/v1/client/subscribe/*action", subscriptionProxy)`
- 将 Xboard 兼容路径转发到 subscription-service

### 3.2 前端（TypeScript/React）

#### `apps/user-web/src/lib/endpoints.ts`（修改）

- 替换硬编码 `SUB_BASE`，改为动态变量
- `setSubConfig(domain, path)` — 运行时注入域名和路径
- `getSubscriptionUrl(token, client)` — 使用动态域名拼接 URL

#### `apps/user-web/src/App.tsx`（修改）

- `useEffect` 钩子启动时 `fetch('/api/v1/guest/config')`
- 调用 `setSubConfig()` 注入动态配置
- 失败时静默降级（使用 `VITE_SUB_BASE` 环境变量默认值）

#### `apps/admin-web/src/pages/Settings.tsx`（修改）

- 黄色警告改为绿色提示："✅ 订阅设置已全部生效"
- 说明 60s 缓存 + 事件主动刷新机制
- 说明修改域名后 user-web 自动跟随

---

## 4. 编译部署流程

### 4.1 前端编译部署

```bash
# 1. 本地构建（Windows PowerShell）
cd D:\机场搭建\yundu-src
pnpm --filter admin-web build    # ~16s, 1.2MB JS
pnpm --filter user-web build     # ~14s, 658KB JS

# 2. 打包
tar -czf tmp/admin-web-dist.tar.gz -C apps/admin-web/dist .
tar -czf tmp/user-web-dist.tar.gz -C apps/user-web/dist .

# 3. SCP 上传到 vps190
scp -i "D:\机场搭建\key\190key.pem" tmp/admin-web-dist.tar.gz root@43.135.147.190:/tmp/
scp -i "D:\机场搭建\key\190key.pem" tmp/user-web-dist.tar.gz root@43.135.147.190:/tmp/

# 4. 部署（备份 → 替换 → 权限）
ssh -i "D:\机场搭建\key\190key.pem" root@43.135.147.190 '
  W=/www/wwwroot/6.tiktokplay.na.am   # admin-web
  cp -a $W ${W}.bak.$(date +%Y%m%d%H%M)
  rm -rf $W/assets $W/index.html
  tar -xzf /tmp/admin-web-dist.tar.gz -C $W
  chown -R www:www $W
'
```

### 4.2 后端编译部署

```bash
# 1. 本地交叉编译（Windows PowerShell）
$env:GOOS="linux"; $env:GOARCH="amd64"; $env:CGO_ENABLED="0"
go build -ldflags "-s -w" -o dist/subscription-service ./apps/subscription-service/cmd/api/
go build -ldflags "-s -w" -o dist/identity-service ./apps/identity-service/cmd/api/
go build -ldflags "-s -w" -o dist/api-gateway ./apps/api-gateway/cmd/api/

# 2. SCP 上传 + 部署
scp -i "D:\机场搭建\key\190key.pem" dist/subscription-service root@43.135.147.190:/tmp/
ssh -i "D:\机场搭建\key\190key.pem" root@43.135.147.190 '
  systemctl stop yundu-subscription-service
  cp /opt/yundu/bin/subscription-service /opt/yundu/bin/subscription-service.bak.20260808
  cp /tmp/subscription-service /opt/yundu/bin/
  chmod +x /opt/yundu/bin/subscription-service
  systemctl start yundu-subscription-service
'
```

### 4.3 部署验证

```bash
# 前端验证
grep -o "<title>[^<]*</title>" /www/wwwroot/6.tiktokplay.na.am/index.html
# 输出: <title>云渡 YunDu · 管理后台</title>

# 后端验证
systemctl is-active yundu-subscription-service  # active
curl -s http://127.0.0.1:8083/healthz            # ok
```

---

## 5. 数据库修复（5 个缺失迁移）

### 5.1 发现的问题

`goose_db_version` 表显示 migrations 61-64 和 69 未注册：

```
 version_id | is_applied
------------+------------
         60 | t
         65 | t   ← 跳过了 61-64!
         66 | t
         ...
         69 | ← 缺失
         70 | t
```

### 5.2 修复内容

| 迁移 | 内容 | DB 实际状态 | 修复操作 |
|------|------|------------|---------|
| 061 | nodes.exposure_mode 等列 | ✅ 列已存在（GORM 自动迁移） | 注册 goose |
| 062 | 删除死模板 traffic_expiry | ❌ 模板仍存在 | DELETE + 注册 |
| 063 | email_verify_required 设置 | ❌ 设置不存在 | INSERT + 注册 |
| 064 | subscribe 组 8 个设置 | ❌ 组不存在 | INSERT 8 行 + 注册 |
| 069 | warp_profiles 账户字段 | ✅ 列已存在 | 注册 goose |

### 5.3 subscribe 组生产值

```sql
SELECT setting_key, value_json FROM system_settings WHERE setting_group = 'subscribe';
```

| setting_key | value_json | 说明 |
|-------------|------------|------|
| subscribe_path | `"/sub"` | 短路径（非 migration 默认的 `/api/v1/client/subscribe`） |
| subscribe_domain | `"ad.tiktokplay.na.am"` | 订阅域名（可随时更换） |
| subscribe_key | `""` | HMAC 密钥（空则不下发签名头） |
| show_info_to_server | `true` | 回显节点信息 |
| show_method | `1` | 完整 userinfo（upload/download/total/expire） |
| is_rand_sub | `false` | 随机订阅关闭 |
| rand_sub_start | `0` | 随机下界 |
| rand_sub_end | `10` | 随机上界 |

### 5.4 修复 SQL

详见 `D:\机场搭建\yundu-src\tmp\fix_missing_migrations.sql`，通过 SSH stdin 管道执行：

```powershell
Get-Content tmp\fix_missing_migrations.sql | ssh root@43.135.147.190 "docker exec -i yundu-postgres psql -U app -d airport"
```

---

## 6. nginx 配置修复

### 6.1 问题

`ad.tiktokplay.na.am.conf` 缺少 `/api/v1/client/subscribe/*` 转发规则，导致 Xboard 兼容客户端访问时返回 302 跳转到 user-web。

### 6.2 修复

在 `location /` 之前添加：

```nginx
# 订阅接口(/api/v1/client/subscribe/*) -> subscription-service (兼容 Xboard 客户端)
location ~ ^/api/v1/client/subscribe/ {
    proxy_pass http://127.0.0.1:8083;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_buffering off;
    proxy_cache off;
    proxy_read_timeout 30s;
}
```

### 6.3 验证

```
/sub/testtoken123          → HTTP 404 ✅ (subscription-service 处理，token 不存在)
/api/v1/client/subscribe/testtoken123 → HTTP 404 ✅ (Xboard 兼容路径)
/                          → HTTP 302 ✅ (跳转到用户面板)
```

---

## 7. VPS190 标准化清理

### 7.1 清理残留服务

| 服务 | 问题 | 处理 |
|------|------|------|
| `run-u10678.service` | 残留的 transient 重复 subscription-service | `systemctl reset-failed` |
| `yundu-singbox.service` | VPS190 控制面板不应有 sing-box 服务 | `systemctl stop && disable` |

### 7.2 清理后 VPS190 服务状态

```
yundu-api-gateway.service            active   running
yundu-identity-service.service       active   running
yundu-node-service.service          active   running
yundu-subscription-service.service   active   running
yundu-traffic-service.service       active   running
```

### 7.3 其他检查项

| 检查项 | 状态 |
|--------|------|
| Docker 容器（postgres/redis/nats/prometheus/grafana/loki/otel） | ✅ 全部 Up |
| nginx 配置（6 个域名 + default） | ✅ syntax ok |
| yunductl 部署 `/usr/local/bin/yunductl` | ✅ 6.2MB Aug 7 |
| 后端二进制时间戳 | ✅ Aug 8 07:55（今日部署） |
| 磁盘 / | 79% (31G/40G) ⚠️ 需关注 |
| 内存 | 1.2G/1.9G used ⚠️ 偏高 |

---

## 8. 问题与解决思路

### 8.1 PowerShell SSH 引号吞噬

**问题**：PowerShell 通过 SSH 传递 SQL 时，双引号被吞掉（`"pushed"` → `ushed`），单引号也被破坏。

**解决**：写 SQL 到本地 `.sql` 文件 → `Get-Content | ssh "docker exec -i psql"` 管道执行。

```powershell
# ✅ 正确方式
Get-Content tmp\query.sql | ssh -i key root@vps "docker exec -i yundu-postgres psql -U app -d airport"

# ❌ 错误方式（引号被吞）
ssh -i key root@vps "docker exec yundu-postgres psql -U app -d airport -c \"SELECT ... WHERE group = 'subscribe'\""
```

### 8.2 DB 连接信息错误

**问题**：之前尝试用 `psql -U airport` 连接，报 `role "airport" does not exist`。

**解决**：正确连接信息在 `/opt/yundu/config/.env`：
- 用户：`app`（非 `airport`）
- 密码：`YunDuProd2026Secure`
- 数据库：`airport`
- 端口：`5433`（Docker 映射，非 5432）

### 8.3 system_settings 表列名

**问题**：查询用 `value` 列报 `column "value" does not exist`。

**解决**：实际列名是 `value_json`（jsonb 类型），不是 `value`。

```sql
-- ✅ 正确
SELECT setting_key, value_json FROM system_settings WHERE setting_group = 'subscribe';

-- ❌ 错误
SELECT setting_key, value FROM system_settings WHERE setting_group = 'subscribe';
```

### 8.4 migration 64 从未应用

**问题**：goose_db_version 从 60 直接跳到 65，migrations 61-64 被跳过，导致 `subscribe` 组不存在。

**根因分析**：可能是部署时 `goose up` 在 60 之后遇到了错误（如 61 的 ALTER TABLE 失败），跳到了 65。但 61 的 schema 变更后来通过 GORM auto-migrate 补上了，只是数据（subscribe 组设置）没有插入。

**解决**：手动执行 migration 62（DELETE 死模板）、63（INSERT 邮件设置）、64（INSERT subscribe 组）的 SQL，并注册到 goose_db_version。

### 8.5 nginx 未转发 Xboard 兼容路径

**问题**：`/api/v1/client/subscribe/*` 在 ad.tiktokplay.na.am 上返回 302（跳转到 user-web），因为 nginx 只转发 `/sub/` 和 `/s/`。

**解决**：在 ad.tiktokplay.na.am.conf 中添加 `/api/v1/client/subscribe/` location 块。

---

## 9. 难点与未解决冲突

### 9.1 VPS206 sing-box 配置验证失败（✅ 已解决 2026-08-08，根因与修复见下）

**现象**：
```
Aug 07 20:49:52 yundu-node-agent:
"config validation failed",
"error":"native-singbox: config validation failed: api: json: unknown field \"api\""
```

sing-box 在 16:42:03 启动成功，20:49:52 新配置推送时验证失败。yunductl 报 `Runtime status: WARN (not running)`。

**排查**：
- `api` 字段是 xray 配置的顶层字段（`kernel_render_adapter.go:823`），sing-box 不认识
- xray 配置通过 `injectXrayConfig` 嵌入 sing-box 配置的 `_xray_config` 字段（`deployment_service.go:2200`）
- agent 提取 `_xray_config` 后 `delete(configMap, "_xray_config")`（`main.go:1004`）
- 理论上 `api` 字段应该在 `_xray_config` 内部，不应出现在 sing-box 顶层
- **但实际 sing-box 配置验证报顶层 `api` 字段**，说明某处将 xray 的 `api` 字段泄漏到了 sing-box 顶层

**可能根因**：
1. 某个配置合并/注入步骤意外将 xray 配置字段 merge 到 sing-box 顶层
2. 20:49 推送的配置版本渲染路径与 16:42 不同（可能节点变更触发了不同代码路径）
3. `ensureAPIInbound` 可能在 sing-box 渲染时被误调用

**影响**：sing-box 主内核未运行，所有 sing-box 协议节点（AnyTLS/Hysteria2/TUIC/VLESS WS 等）不可用。xray 辅内核仍在运行（XHTTP 节点可用）。

**已确认根因（2026-08-08 数据库+代码定位）**：`api` 字段并未泄漏到 sing-box 顶层——而是**辅内核 xray runtime 的独立配置版本被直接推给了 sing-box 主内核 agent**：

- 双内核服务器存在 sing-box（主，provider_ref=sing-box）与 xray（辅，provider_ref=xray）两个 runtime
- 20:49:52 用户事件/节点刷新触发 `GetRuntimeConfig(xray runtime)`，创建独立 xray 配置版本
  （`config_versions` version_no=2、scope=xray runtime、顶层含 `api`、7 inbounds、**无 `_xray_config`**）
- `dispatchAndPush` → `pushConfigToRuntime` 按 server_code 推送、未区分 runtime →
  agent 收到 ConfigPush(version=2) → `fetchConfigViaPayload(2)` 按全局 version_no 拉取到 xray payload →
  sing-box 校验失败 `unknown field "api"` → LKG 回滚（此后无二次失败，agent 一直健康）

**修复（commit `0116ba0`，三层纵深防御）**：

1. node-service `pushConfigToRuntime`：辅内核 xray runtime 的独立配置跳过推送（`isAuxiliaryXrayRuntime` 检测同 server 的 sing-box 配对 runtime）
2. node-service `finishRuntimeConfigDispatch`：辅内核 xray 新版本只作面板存档，改刷新配对 sing-box runtime，
   使最新 xray 配置经 `_xray_config` 嵌入统一下发（同时修复了「xray 节点变更不传播到嵌入配置」的隐性 gap）
3. node-service `FetchPayload`：按 X-Runtime-Ref 校验版本归属，杜绝跨 runtime 拉取（旧 agent 无头兼容）
4. node-agent `applyConfig`：sing-box 主内核收到含顶层 `api` 的 xray 形态配置时提前拒绝（清晰报错，防串台兜底）

**生产验证（2026-08-08 闭环）**：node-service 已部署 VPS190；VPS206/VPS81 node-agent v0.8.2 全量标签升级；
`/etc/yundu/config/sing-box.json` 无顶层 `api`；`yunductl health` 恢复 `OK (running)`；双核心跳 fresh；
订阅链路 200/404 符合预期。

### 9.2 VPS81 检查（✅ 已完成 2026-08-08）

VPS81 (132.226.156.81) node-agent 标准化检查完成：
- `RUNTIME_TYPE=sing-box` + `YUNDU_NATIVE_RUNTIME=true`，服务 active
- `/etc/yundu/config/sing-box.json` 无顶层 `api`/`stats`，5 inbounds/5 outbounds（P14/P15/P16/P27 等）
- 升级 node-agent v0.8.2（全量标签）+ yunductl 后，`yunductl health` = `OK (running)`
- 双核心跳 fresh（sing-box + xray 均 10s 级上报，面板 last_heartbeat 秒级）

### 9.3 traffic-service 二进制较旧

VPS190 上 `traffic-service` 二进制日期为 Aug 2，比其他服务（Aug 7-8）旧。如有代码变更需更新。

---

## 10. 当前进度

### 10.1 已完成

| 项目 | 详情 |
|------|------|
| 订阅设置后端 | 9 个文件修改/新建，全链路打通 |
| 前端编译部署 | admin-web + user-web 已部署到 vps190 生产 |
| 后端编译部署 | subscription-service + identity-service + api-gateway 已部署 |
| nginx 配置 | ad.tiktokplay.na.am.conf 补齐 `/api/v1/client/subscribe/*` |
| 数据库修复 | 5 个缺失 migration 补齐 + goose 注册 |
| subscribe 组设置 | 8 个设置已插入（subscribe_domain=ad.tiktokplay.na.am） |
| VPS190 标准化 | 残留服务清理 + 全部服务/容器验证通过 |
| 订阅链路验证 | /api/v1/guest/config + /sub/* + /api/v1/client/subscribe/* 全部验证通过 |

### 10.2 进行中/未完成

| 项目 | 状态 |
|------|------|
| VPS206 sing-box `api` 字段问题 | ✅ 已解决（根因=辅内核xray配置独立推送，commit `0116ba0` 三层修复+生产部署） |
| VPS81 node-agent 检查 | ✅ 已完成（v0.8.2 升级 + 健康检查通过） |
| git commit + push | ✅ 已完成（`831996f` 订阅9文件 + `0116ba0` 双核修复，均已 push origin/main） |
| GitHub CI 验证 | ✅ 已验证（CI run#6 success + run#7 success） |

---

## 11. 后续如何更换订阅地址

### 场景：user-web 被墙，需要更换订阅域名

```
当前: ad.tiktokplay.na.am（被墙）
目标: sub.newdomain.com（新域名）
```

### 步骤

1. **准备新域名**
   - 在 Cloudflare 添加新 DNS � record（指向 vps190 IP，开启 CF Proxy）
   - 在 CF SSL/TLS 设置 Edge Certificate
   - 在 vps190 宝塔面板添加站点 + 申请 SSL 证书

2. **nginx 配置**
   - 复制 `ad.tiktokplay.na.am.conf` 为新域名配置
   - 修改 `server_name` 和 SSL 证书路径
   - `nginx -t && nginx -s reload`

3. **面板修改 subscribe_domain**（零 SSH 核心）
   - 管理面板 → 系统设置 → 订阅设置
   - 将「订阅域名」改为 `sub.newdomain.com`
   - 点击「保存设置」

4. **验证**
   ```bash
   # 检查 /api/v1/guest/config 返回新域名
   curl -s https://7.tiktokplay.na.am/api/v1/guest/config | python3 -m json.tool
   # data.subscribe_domain 应为 "sub.newdomain.com"

   # 检查新域名订阅可达
   curl -sk -o /dev/null -w '%{http_code}' https://sub.newdomain.com/sub/testtoken
   # 应返回 404（token 不存在，说明 subscription-service 正常处理）
   ```

5. **用户侧**
   - 用户刷新 user-web 页面后自动获取新订阅域名
   - 用户复制的新订阅地址自动指向新域名
   - **无需重新构建前端、无需重启后端服务**

### 关键约束

- `subscribe_domain` 只填域名（如 `sub.newdomain.com`），**不带** `https://` 前缀
- 新域名的 nginx 配置必须包含 `/sub/` 和 `/api/v1/client/subscribe/` 两条 location
- 新域名的 SSL 证书必须有效（否则客户端连接失败）
- subscription-service 的 60s 缓存意味着修改后最多 1 分钟生效

---

## 12. 避坑指南

### 12.1 PowerShell SSH 引号

| 场景 | ❌ 错误 | ✅ 正确 |
|------|--------|---------|
| SQL 查询 | `ssh "psql -c \"SELECT...\""` | `Get-Content file.sql \| ssh "docker exec -i psql"` |
| 远程变量 | `ssh "echo $VAR"`（$VAR 被本地解析） | `ssh 'echo $VAR'`（单引号防解析） |
| heredoc | `ssh "cat <<'EOF'..."`（PowerShell 不支持） | 写入文件 → SCP → 远程执行 |

### 12.2 数据库连接

```
✅ 正确: docker exec -i yundu-postgres psql -U app -d airport
❌ 错误: psql -U airport（airport 是数据库名不是用户名）
❌ 错误: psql -p 5432（正确端口是 5433，Docker 映射）
```

### 12.3 system_settings 表

- 列名是 `value_json`（jsonb），不是 `value`
- 查询 subscribe 组：`WHERE setting_group = 'subscribe'`（不是 `subscription`）
- `subscription` 组是另一个组（包含 `config` JSON 对象），不要混淆

### 12.4 前端部署

- admin-web 和 user-web **不在 CI 流程内**，修改后必须本地 `pnpm build` + 手动上传
- 上传前必须 `tar -czf` 压缩（直接 SCP 大量小文件极慢）
- 部署后 `chown -R www:www` 修复权限

### 12.5 后端部署

- 运行中二进制 `Text file busy`，必须先 `systemctl stop` 再 `cp`
- SCP 上传后权限为 644，需 `chmod +x` 否则 systemd 报 `status=203/EXEC`
- 替换前务必备份：`cp /opt/yundu/bin/xxx /opt/yundu/bin/xxx.bak.$(date +%Y%m%d%H%M)`

### 12.6 subscribe_path 选择

- 生产值用 `/sub`（短路径，用户友好）
- migration 默认值是 `/api/v1/client/subscribe`（Xboard 兼容，但 URL 太长）
- **两个路径后端始终注册**，设置仅控制 user-web 生成哪个 URL
- 修改 `subscribe_path` 后 user-web 下次加载自动跟随，无需重启

### 12.7 subscribe_key 留空

- 当前 `subscribe_key` 为空字符串，`X-Subscribe-Signature` 头不会下发
- 如需启用 HMAC 签名验证，在面板设置一个随机密钥即可
- 客户端需实现签名校验逻辑才能使用此功能

---

## 13. 接手人注意事项

### 13.1 首先阅读

1. **运营维护指南 v2.0**：`D:\机场搭建\yundu-src\运营维护指南-v2.0.md`（§1-§8 核心，§9-§11 应急）
2. **本文档**：了解 8.8 任务完成的订阅链路实现
3. **编译推送教程**：`D:\机场搭建\技术指导\编译推送与安装升级维护教程.md`
4. **下发失败诊断指南**：`D:\机场搭建\技术指导\YunDu-面板节点下发失败诊断与维护指南-20260801.md`

### 13.2 关键文件索引

| 文件 | 路径 | 用途 |
|------|------|------|
| 订阅设置仓库 | `apps/subscription-service/internal/repo/subscribe_settings_repo.go` | 60s 缓读取 subscribe 组 |
| 订阅服务 | `apps/subscription-service/internal/service/subscription_service.go` | 随机订阅 + 显示方式控制 |
| 公开配置端点 | `apps/identity-service/internal/handler/setting_handler.go` → `GetPublicConfig` | user-web 获取订阅域名 |
| user-web 动态配置 | `apps/user-web/src/lib/endpoints.ts` → `setSubConfig` | 运行时注入域名 |
| xray 配置嵌入 | `apps/node-service/internal/service/deployment_service.go:2154` → `injectXrayConfig` | xray 配置嵌入 sing-box `_xray_config` |
| xray API 字段 | `apps/node-service/internal/service/kernel_render_adapter.go:823` | xray 顶层 `api` 字段（sing-box 不认识） |

### 13.3 环境信息速查

| 项目 | 值 |
|------|-----|
| VPS190 SSH | `ssh -i "D:\机场搭建\key\190key.pem" root@43.135.147.190` |
| VPS206 SSH | `ssh -i "D:\机场搭建\key\206.key" -F /dev/null ubuntu@158.101.13.206` |
| VPS81 SSH | `ssh -i "D:\机场搭建\key\81.key" -F /dev/null ubuntu@132.226.156.81` |
| DB 连接 | `docker exec -i yundu-postgres psql -U app -d airport` |
| DB 用户/库/端口 | `app` / `airport` / `5433` |
| 订阅域名 | `ad.tiktokplay.na.am` |
| 管理面板 | `https://6.tiktokplay.na.am` |
| 用户面板 | `https://7.tiktokplay.na.am` |
| nginx 配置目录 | `/www/server/panel/vhost/nginx/` |
| 前端部署目录 | `/www/wwwroot/6.tiktokplay.na.am/`（admin）+ `/www/wwwroot/7.tiktokplay.na.am/`（user） |
| 后端二进制目录 | `/opt/yundu/bin/` |
| 环境变量 | `/opt/yundu/config/.env` |

### 13.4 注意事项

1. **VPS206 sing-box 未运行**：接手后首要任务，详见 §9.1 和 §15.1
2. **git 未提交**：9 个文件修改未 commit + push，详见 §15.4
3. **subscribe_path 生产值是 `/sub`**：不是 migration 默认的 `/api/v1/client/subscribe`
4. **subscribe_domain 是 `ad.tiktokplay.na.am`**：如需更换按 §11 流程操作
5. **前端不在 CI 内**：修改前端后必须手动 build + 上传
6. **PowerShell 引号陷阱**：SQL 通过 SSH 执行必须用文件管道方式

---

## 14. 发现的问题

### 14.1 已解决

| # | 问题 | 影响 | 解决方式 |
|---|------|------|---------|
| 1 | subscribe 组不存在 | 订阅设置全部不生效 | 手动 INSERT 8 行设置 |
| 2 | migration 61-64/69 未注册 | goose 版本号不连续 | 手动注册 + 补数据 |
| 3 | nginx 缺 Xboard 兼容路由 | 老客户端订阅返回 302 | 添加 location 块 |
| 4 | /api/v1/guest/config 无 subscribe_domain | user-web 无法动态获取域名 | DB 插入 subscribe_domain |
| 5 | run-u10678 残留服务 | systemd 报 failed | reset-failed |
| 6 | yundu-singbox 遗留服务 | VPS190 不应有 sing-box | stop + disable |
| 7 | DB 连接 role airport 不存在 | 无法查询数据库 | 用 app 用户连接 |

### 14.2 未解决

| # | 问题 | 影响 | 严重度 |
|---|------|------|--------|
| 1 | ~~VPS206 sing-box `api` 字段验证失败~~ | ✅ 已解决：辅内核xray配置独立推送导致，commit `0116ba0` 修复并部署 | 🔴→✅ |
| 2 | ~~VPS81 未检查~~ | ✅ 已检查：双核 v0.8.2 健康 | 🟡→✅ |
| 3 | ~~git 9 个文件未提交~~ | ✅ 已提交推送（`831996f` + `0116ba0`） | 🟡→✅ |
| 4 | traffic-service 二进制较旧（Aug 2） | 8.2 后 traffic-service 无代码变更（git log 验证见 §16），无需重建 | 🟢 低 |
| 5 | VPS190 磁盘占用偏高 | 建议按 §16 清理建议执行（旧备份/journal/tmp 二进制） | 🟢 低 |
| 6 | VPS190 内存 1.2G/1.9G | 监控观察，必要时加 swap | 🟢 低 |

---

## 15. 下一步具体执行步骤

### 15.1 ✅ 修复 VPS206 sing-box `api` 字段问题（已完成 2026-08-08，见 §16）

```bash
# 1. SSH 到 VPS206 检查实际配置
ssh -i "D:\机场搭建\key\206.key" -F /dev/null ubuntu@158.101.13.206
sudo python3 -c 'import json; c=json.load(open("/etc/yundu/config/sing-box.json")); print("has api:", "api" in c); print("keys:", list(c.keys()))'

# 2. 如确认有顶层 api 字段，检查渲染代码
# 在 kernel_render_adapter.go 中搜索是否有将 xray 配置 merge 到 sing-box 顶层的代码路径
# 特别关注 buildXrayConfigViaKernelRender 的返回值如何被 injectXrayConfig 使用

# 3. 可能的修复方向：
#    a. 确保 injectXrayConfig 正确嵌入 _xray_config（而非 merge 到顶层）
#    b. 检查是否有其他注入函数（如 injectNginxVhosts/injectTrafficQuota）误操作 xray 配置
#    c. 在 native_singbox.go 验证前显式删除非 sing-box 字段（api/stats/policy 等 xray 字段）

# 4. 修复后重新编译 node-service，SCP 部署到 vps190，触发配置重新下发
```

### 15.2 ✅ 检查 VPS81 node-agent（已完成 2026-08-08）

```bash
ssh -i "D:\机场搭建\key\81.key" -F /dev/null ubuntu@132.226.156.81
sudo cat /etc/yundu/node-agent.env | grep RUNTIME_TYPE
sudo /usr/local/bin/yunductl health
sudo journalctl -u yundu-node-agent --since '1 hour ago' | grep -iE 'sing-box|fail|error|started'
```

### 15.3 🟡 验证订阅功能端到端

```bash
# 1. 在面板创建一个测试用户 + 订阅 token
# 2. 用真实 token 测试订阅
curl -s https://ad.tiktokplay.na.am/sub/<real_token> | python3 -m json.tool
# 3. 检查 Subscription-Userinfo 头
curl -sI https://ad.tiktokplay.na.am/sub/<real_token> | grep -i subscription
# 4. 用不同客户端 UA 测试（karing/hiddify/clash）
curl -s -A "karing/1.0" https://ad.tiktokplay.na.am/sub/<real_token>
```

### 15.4 ✅ git 提交推送（已完成：`831996f` 订阅 + `0116ba0` 双核修复）

```bash
cd D:\机场搭建\yundu-src
git status
git add apps/subscription-service/internal/repo/subscribe_settings_repo.go \
        apps/subscription-service/internal/service/subscription_service.go \
        apps/subscription-service/internal/handler/subscription_handler.go \
        apps/identity-service/internal/handler/setting_handler.go \
        apps/identity-service/internal/app/app.go \
        apps/api-gateway/internal/app/app.go \
        apps/user-web/src/lib/endpoints.ts \
        apps/user-web/src/App.tsx \
        apps/admin-web/src/pages/Settings.tsx
git commit -m "feat: subscribe settings backend + dynamic subscribe_domain for user-web

- Add SubscribeSettingsRepo (60s cache, direct DB read)
- Add /api/v1/guest/config public endpoint (identity-service)
- Add /api/v1/client/subscribe/* route (api-gateway)
- Add HMAC-SHA256 signature (X-Subscribe-Signature)
- Add random subscription (Fisher-Yates partial shuffle)
- Add show_method control (0=none, 1=full userinfo)
- user-web dynamically fetches subscribe_domain (no rebuild needed)"
git push origin main
```

### 15.5 🟢 VPS190 磁盘清理

```bash
ssh -i "D:\机场搭建\key\190key.pem" root@43.135.147.190 '
  # 清理旧备份
  cd /opt/yundu/bin
  rm -f *.bak.* node-service-old node-service-new* node-service-fixed
  # 清理 journal
  journalctl --vacuum-size=300M
  # 清理 /tmp 旧二进制
  rm -f /tmp/node-agent-* /tmp/node-service-* /tmp/*-dist.tar.gz
  df -h /
'
```

### 15.6 🟢 更新 traffic-service（如有代码变更）

```bash
# 检查是否有未部署的 traffic-service 代码变更
cd D:\机场搭建\yundu-src
git log --oneline apps/traffic-service/ --since="Aug 2"
# 如有变更，交叉编译 + SCP 部署
$env:GOOS="linux"; $env:GOARCH="amd64"; $env:CGO_ENABLED="0"
go build -ldflags "-s -w" -o dist/traffic-service ./apps/traffic-service/cmd/api/
# SCP + 部署同 §4.2
```

---

---

## 16. 2026-08-08 闭环补充：未完成任务完成情况 + 优化发现

> 本节记录对本文档 2026-08-08 遗留任务的闭环，以及基于「零 SSH」目标与项目实际检验出的优化点。
> 全部改动 commit：`0116ba0`（已 push origin/main），CI run#7 success。

### 16.1 VPS206 sing-box `api` 字段问题 — 完整根因（与文档 §9.1 初始假设不同）

**初始假设**（本任务文档）：`kernel_render_adapter.go:823` 的 xray 顶层 `api` 字段泄漏到了 sing-box 顶层。

**实际根因**（数据库 + 代码 + 生产日志三重定位）：

```
config_versions 表（2026-08-07 20:49:52 精确命中）:
  version_no=2 | scope=xray runtime (34492dcb) | Source=system
  has_top_api=t | has_xray_cfg=f | inbounds=7   ← xray 形态配置

链路:
用户事件/节点刷新 → GetRuntimeConfig(xray runtime)
  → 创建独立 xray 配置版本（顶层 api/stats/policy）
  → dispatchAndPush → pushConfigToRuntime（按 server_code 推送，未区分 runtime）
  → agent 收到 ConfigPush(version=2)
  → fetchConfigViaPayload(2)（面板按全局 version_no 查 payload，未校验 runtime 归属）
  → agent 拿到 xray 配置 → sing-box 校验失败 unknown field "api" → LKG 回滚
```

**为什么之前没复发**：该失败为一次性事件——xray 配置版本创建后 hash 未再变化（path3 不推送），
agent currentVersion 一直停留在 sing-box 版本 10888，心跳走 sing-box runtime，不再触碰 xray 版本。
但**任何下次 xray 配置内容变更**（如新增/修改 XHTTP 节点、用户事件触发全量同步）都会再次触发。

### 16.2 修复方案（三层纵深防御，commit `0116ba0`）

| 层 | 文件 | 修复 |
|----|------|------|
| 1 | `apps/node-service/internal/service/deployment_service.go` | `pushConfigToRuntime`：辅内核 xray runtime（同 server 有 sing-box 配对）跳过独立推送；`finishRuntimeConfigDispatch`：辅内核 xray 新版本只作面板存档，改刷新配对 sing-box runtime 使 `_xray_config` 嵌入统一下发 |
| 2 | `apps/node-service/internal/handler/agent_handler.go` | `FetchPayload`：按 X-Runtime-Ref 解析 runtime 并校验版本归属，跨 runtime 版本返回 404（agent 自动回退明文 FetchConfig 自愈） |
| 3 | `apps/node-agent/cmd/agent/main.go` | `applyConfig`：sing-box 主内核收到含顶层 `api` 的 xray 形态配置时提前拒绝（明确报错，避免晦涩的 unknown field） |

**附带修复的隐性 gap**：此前 `RefreshNodeConfig(xray节点)` 只刷新 xray runtime，从不刷新 sing-box runtime，
导致 xray 节点变更**不会传播到 sing-box 配置里嵌入的 `_xray_config`**（xray 内核一直跑旧配置）。
`finishRuntimeConfigDispatch` 同时修复了这一点。

### 16.3 生产部署与验证

| 组件 | 部署目标 | 验证 |
|------|---------|------|
| node-service（含 1+2 修复） | VPS190，备份 `node-service.bak.20260808085353` | active + /healthz 200；无新 config_versions 抖动 |
| node-agent v0.8.2（含 3 修复，全量 6 标签） | VPS206 + VPS81，备份 `node-agent.bak.20260808` | `yunductl health` = `OK (running)`；sing-box.json 无顶层 `api`；双核 started |
| yunductl（status 契约修复） | VPS190 + VPS206 + VPS81 | `yunductl health` 从 `WARN (not running)` 恢复 `OK (running)` |

订阅链路复核：`/api/v1/guest/config`=200（subscribe_domain=ad.tiktokplay.na.am）；`/sub/testtoken123`=404；
`/api/v1/client/subscribe/testtoken123`=404；`/`=302。全部符合预期。

### 16.4 检验发现的优化点（零 SSH 视角）

#### 16.4.1 🟠 yunductl ↔ agent status 契约不匹配（已修复并部署）

**问题**：agent `/status` 返回 `runtime_state:"running"`，但 `yunductl health` 只读 `running`(bool) 且
`yunductl version` 读 `agent_version`/`runtime_version`——两者均不存在 → 双节点 `yunductl health`
长期误报 `WARN (not running)`（文档 §9.1 因此误判「sing-box 主内核未运行」，实际 sing-box 一直在跑）。

**修复**（commit `0116ba0`）：
- `apps/node-agent/cmd/agent/control.go`：`/status` 补充 `running`/`agent_version`/`runtime_version`，保留 `runtime_state`
- `apps/yunductl/main.go`：兼容解析 `running` 或 `runtime_state=="running"`

**验证**：VPS206/VPS81 `yunductl version` 现在输出 `Agent Version: v0.8.2 / Running: true`，health = `OK (running)`。

> 教训：双端契约（CLI 消费方 + agent 提供方）必须有 golden test 或共享 schema；本次是典型的
> 「观测层与实现层字段漂移」导致误报，零 SSH 运维依赖准确观测，此类问题应优先修。

#### 16.4.2 🟡 VPS190 残留 `xray.service`（failed）

**现象**：VPS190 仍有一个 `xray.service`（Xray Service (YunDu)），状态 `failed`——文档 §7.1 清理了
`yundu-singbox.service` 但漏掉了它。控制面板不应有 xray 服务。

**处理建议**（零 SSH 运维干净化）：
```bash
ssh root@43.135.147.190 'systemctl stop xray.service; systemctl disable xray.service; systemctl reset-failed xray.service'
```

#### 16.4.3 🟡 `TestExposurePolicyConsistencyWithTerminationClass` 预存在失败

**现象**：`apps/node-service/internal/service/p0_p2_regression_test.go:519` 断言 ExposurePolicy 与
TerminationClass 对 `direct/reality/none` 的 NeedsStreamSNI 一致性，当前失败
（policy=true / TC=false）。**该失败在本次改动前即存在**（git stash 验证），且 CI 不跑此包，未阻塞发布。

**判断**：属于「策略表双源漂移」的既有技术债（§运营维护指南 11.16 同主题）。建议单独排期：
统一 `exposure_policy.go` 与 `termination_class.go` 的 NeedsStreamSNI 语义（direct/reality/none 走 stream SNI 透传），
或修正过时断言。

#### 16.4.4 🟢 traffic-service 无未部署变更（已核验）

`git log --oneline apps/traffic-service/ --since="Aug 2"` 无提交 → 现有 Aug 2 二进制无需重建。

#### 16.4.5 🟢 VPS190 清理建议（磁盘 79%）

```bash
ssh root@43.135.147.190 '
  rm -f /opt/yundu/bin/node-service.bak. /opt/yundu/bin/*.bak.20260801 /opt/yundu/bin/node-service.bak.wild3.*
  journalctl --vacuum-size=300M
  rm -f /tmp/node-agent-* /tmp/node-service-* /tmp/*-dist.tar.gz /tmp/yunductl.* /tmp/node-agent.fix /tmp/node-service.fix
  df -h /
'
```

### 16.5 防复发加固（第二轮，commit `0117xxx`）

在 §16.2 三层修复基础上，进一步加固防止「xray runtime 与 sing-box 冲突」事故复发：

| # | 加固 | 说明 |
|---|------|------|
| 1 | `Deploy()`/`Publish()` 拒绝辅内核 xray runtime scope | 直接 400 + 明确指引（`ErrAuxiliaryXrayRuntimeNotDeployable`），避免 batch 卡 pending / 概念误导 |
| 2 | 下发决策抽纯函数 + 5 个单测 | `isAuxiliaryXrayRuntimeOnServer`（`dual_kernel_dispatch_test.go`），锁定双内核判定逻辑防回归 |
| 3 | 3 个 Prometheus 复发检测指标 | `nodeservice_dual_kernel_xray_standalone_skipped_total` / `_cross_runtime_payload_rejected_total` / `_xray_archived_total`，稳定态 0 |
| 4 | 运营维护指南新增 §11.22 | 双内核配置下发纪律 + 事故签名 + 告警建议（见 `运营维护指南-v2.0.md`） |

**运维铁律（唯一真相源）**：只对 sing-box（主内核）runtime 发布/刷新；xray runtime 仅作存档 scope，
其配置必须经 `_xray_config` 嵌入 sing-box 主配置下发。

---

## 附录 A：本次修改的全部文件清单

| # | 文件 | 操作 | 说明 |
|---|------|------|------|
| 1 | `apps/subscription-service/internal/repo/subscribe_settings_repo.go` | 新建 | 订阅设置仓库（60s 缓存） |
| 2 | `apps/subscription-service/internal/service/subscription_service.go` | 修改 | 集成设置 + 随机订阅 + HMAC |
| 3 | `apps/subscription-service/internal/handler/subscription_handler.go` | 修改 | 响应头控制 |
| 4 | `apps/identity-service/internal/handler/setting_handler.go` | 修改 | 新增 GetPublicConfig |
| 5 | `apps/identity-service/internal/app/app.go` | 修改 | 注册 /guest/config 路由 |
| 6 | `apps/api-gateway/internal/app/app.go` | 修改 | 注册 /api/v1/client/subscribe/* 路由 |
| 7 | `apps/user-web/src/lib/endpoints.ts` | 修改 | 动态订阅域名 |
| 8 | `apps/user-web/src/App.tsx` | 修改 | 启动时加载公开配置 |
| 9 | `apps/admin-web/src/pages/Settings.tsx` | 修改 | 更新提示为"已生效" |

## 附录 B：临时文件清单

| 文件 | 用途 |
|------|------|
| `tmp/ad.conf.remote` | 下载修改的 nginx 配置 |
| `tmp/query_subscribe.sql` | 查询 subscribe 组 SQL |
| `tmp/check_migrations_61_69.sql` | 检查 migration 状态 SQL |
| `tmp/fix_missing_migrations.sql` | 修复缺失 migration SQL |
| `tmp/insert_subscribe_settings.sql` | 插入 subscribe 组设置 SQL |

---

> **文档维护**: 本文档记录 2026-08-08 任务执行过程，后续修改请同步更新。  
> **编写日期**: 2026-08-08  
> **编写人员**: CatPaw 代码Agent
