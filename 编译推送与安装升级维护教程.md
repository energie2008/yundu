# YunDu 编译推送与安装升级维护教程

> 仓库地址：https://github.com/energie2008/yundu
> 本地源码目录：`d:\机场搭建\yundu-src`
> 生成日期：2026-07-23
> **更新日期**：2026-08-17（新增§十二：v0.8.3 发版/部署/新VPS接入全流程实录增补）

---

## 一、整体流程概览

YunDu 有**两条部署路径**，根据紧急程度选择：

### 路径 A：CI 发版（标准流程，适用正式发布）

```
本地修改源码 (yundu-src)
        │
        ▼
git commit + git push origin main   ← 推送代码到 GitHub
        │
        ▼
git tag v0.3.x + git push origin v0.3.x   ← 打 tag 触发 CI
        │
        ▼
GitHub Actions 自动编译 (linux amd64 + arm64, 纯静态)
        │
        ▼
自动发布 GitHub Release (7 个二进制 × 2 架构 = 14 个产物)
        │
        ▼
VPS 上执行 install.sh upgrade 一键升级
```

### 路径 B：本地交叉编译热修复（紧急修复，适用生产 hotfix）

```
本地修改源码 (yundu-src)
        │
        ▼
本地交叉编译 (GOOS=linux GOARCH=amd64 CGO_ENABLED=0)   ← 见「六、本地交叉编译热修复部署」
        │
        ▼
SCP 上传二进制到 vps190  →  chmod +x  →  systemctl stop  →  cp  →  systemctl start
        │
        ▼
（如有新 migration）SCP 上传 SQL  →  docker exec psql -f  →  goose_db_version 注册
        │
        ▼
验证：systemctl status + tail 日志 + 节点下发状态 SQL 检查
```

> **选择建议**：紧急修复（生产故障/下发失败）用路径 B，5 分钟完成部署；正式发版用路径 A，经过 CI 验证更可靠。731/801 的架构缺陷修复全部使用路径 B 部署。

**核心原则**：日常推送代码用 `main` 分支，发布版本用 `v*` tag。只有打 tag 才会触发编译发布。

> ⚠️ **前端不在 CI 流程内**：上面两条链路都只产出 **Go 后端二进制**，`admin-web`（管理后台）和 `user-web`（用户前台）的前端产物**不会**被编译。改了前端必须本地 `build` 后手动上传到面板服务器，详见「十一、前端构建与部署」。

---

## 二、仓库结构说明

```
yundu-src/
├── .github/workflows/release.yml   # CI/CD 工作流（打 tag 自动编译发布）
├── apps/                           # 服务源码
│   ├── node-agent/                 # 节点代理（双内核 xray + sing-box）
│   ├── node-service/               # 节点服务（面板端）
│   ├── api-gateway/                # API 网关
│   ├── identity-service/           # 认证/用户/订单
│   ├── subscription-service/       # 订阅服务
│   ├── traffic-service/            # 流量统计
│   ├── yunductl/                   # 命令行工具
│   ├── admin-web/                  # 管理后台前端
│   └── user-web/                   # 用户前端
├── packages/                       # 共享包（config/proto/subscription 等）
├── cmd/migrate/                    # 数据库迁移工具
├── migrations/                     # 数据库迁移 SQL
├── deploy/docker/                  # Docker 部署配置（含可观测性栈）
├── docs/                           # 正式文档（ADR/OpenAPI/Runbooks）
├── install.sh                      # 一键安装/升级脚本
├── go.work                         # Go workspace 配置
└── go.work.sum                     # Go workspace 校验和
```

---

## 三、日常编译推送流程

### 3.1 修改源码后提交推送

在本地 `d:\机场搭建\yundu-src` 目录操作：

```bash
# 查看改了什么
git status

# 查看具体改动
git diff

# 添加所有改动
git add -A

# 提交（描述清楚改了什么、为什么改）
git commit -m "fix(node-service): 修复 balancer 降级导致流量走 direct 的问题"

# 推送到 GitHub
git push origin main
```

> **注意**：推送 `main` 分支只会更新代码，**不会触发编译**。需要发布新版本时才打 tag。

### 3.2 多次小改动的规范

```bash
# 每个逻辑改动单独提交，方便回溯
git add apps/node-service/internal/service/kernel_render_adapter.go
git commit -m "fix(node-service): balancer 改为直接 outboundTag 路由"

git add apps/node-agent/cmd/agent/main.go
git commit -m "fix(node-agent): sing-box route rules 前插确保优先匹配"

# 一起推送
git push origin main
```

---

## 四、创建 Release 触发 CI 编译

### 4.1 打 tag 并推送（标准方式）

```bash
# 进入源码目录
cd d:\机场搭建\yundu-src

# 创建带注释的 tag（推荐用带注释的，会记录发布信息）
git tag -a v0.3.1 -m "YunDu v0.3.1

修复:
- node-service balancer 降级逻辑
- node-agent sing-box route rules 顺序"

# 推送 tag 到 GitHub（这一步触发 CI）
git push origin v0.3.1
```

### 4.2 版本号规范

| 版本号 | 含义 | 示例 |
|--------|------|------|
| `vX.Y.0` | 大版本/里程碑 | `v0.3.0` 首个 CI 版本 |
| `vX.Y.Z` | Bug 修复/小改进 | `v0.3.1`、`v0.3.2` |
| `vX.0.0` | 大版本升级 | `v1.0.0` 正式发布 |

**规则**：日常修复递增第三位（`v0.3.0 → v0.3.1 → v0.3.2`），有重大改动递增第二位。

### 4.3 重新打 tag（CI 失败需要重试时）

如果 CI 编译失败，修复代码后需要**删除旧 tag 再重新打**：

```bash
# 删除本地旧 tag
git tag -d v0.3.1

# 删除 GitHub 上的旧 tag
git push origin :refs/tags/v0.3.1

# 提交修复代码
git add -A
git commit -m "fix(ci): 修复 go.sum 缺失条目"
git push origin main

# 重新打同名 tag
git tag -a v0.3.1 -m "YunDu v0.3.1"
git push origin v0.3.1
```

### 4.4 查看 CI 运行状态

**方式一：浏览器查看**

打开 https://github.com/energie2008/yundu/actions ，能看到所有 CI 运行记录。

**方式二：命令行查看（无需认证）**

```bash
# 查看最近 5 次 CI 运行
curl -s https://api.github.com/repos/energie2008/yundu/actions/runs?per_page=5 | jq '.workflow_runs[] | "\(.id) | \(.status)/\(.conclusion) | \(.head_branch) | \(.created_at)"'

# 查看指定 run 的 job 详情（替换 RUN_ID）
curl -s https://api.github.com/repos/energie2008/yundu/actions/runs/RUN_ID/jobs | jq '.jobs[] | "\(.name) | \(.status)/\(.conclusion)"'
```

CI 通常 5-8 分钟完成（首次更慢，因为要下载 Go 依赖）。

### 4.5 验证 Release 发布成功

```bash
# 查看最新 Release 信息和产物列表
curl -s https://api.github.com/repos/energie2008/yundu/releases/latest | jq '{
  tag: .tag_name,
  published: .published_at,
  assets: [.assets[] | {name: .name, size_mb: (.size/1048576 | floor), downloads: .download_count}]
}'
```

预期输出 14 个产物（7 个服务 × 2 个架构）：

```
node-agent-amd64      ~81MB
node-agent-arm64      ~76MB
node-service-amd64    ~59MB
node-service-arm64    ~55MB
api-gateway-amd64     ~44MB
api-gateway-arm64     ~41MB
identity-service-amd64  ~49MB
identity-service-arm64  ~46MB
subscription-service-amd64 ~50MB
subscription-service-arm64 ~47MB
traffic-service-amd64 ~47MB
traffic-service-arm64 ~44MB
migrate-amd64         ~14MB
migrate-arm64         ~13MB
```

> **注意**：Release 产物**只有后端二进制**，不含前端静态文件。发版后面板界面若无变化属正常现象——前端要单独构建部署，见「十一、前端构建与部署」。

---

## 五、一键安装

### 5.1 安装 node-agent（节点端，部署在 VPS 节点上）

```bash
# 在目标 VPS 上以 root 执行
curl -fsSL https://github.com/energie2008/yundu/raw/main/install.sh | bash -s -- agent \
  --token=你的AGENT_TOKEN \
  --endpoint=https://你的面板地址
```

**参数说明**：

| 参数 | 必填 | 说明 | 示例 |
|------|------|------|------|
| `--token` | 是 | Agent 认证 token | `--token=abc123def` |
| `--endpoint` | 是 | 面板 URL | `--endpoint=https://panel.example.com` |
| `--runtime` | 否 | 内核类型，默认 **sing-box**（P2 翻转后主内核为 sing-box） | `--runtime=sing-box` |
| `--version` | 否 | 指定版本，默认 latest | `--version=v0.3.1` |

**安装后自动完成**：
- 下载 node-agent 二进制到 `/opt/yundu/bin/node-agent`
- 创建配置目录 `/etc/yundu`
- 创建 systemd 服务 `yundu-node-agent`
- 启动服务并设置开机自启

> ⚠️ **安装后必须检查**：确认 `/etc/yundu/node-agent.env` 中 `RUNTIME_TYPE=sing-box` 已显式设置。VPS81 曾因该文件为空导致 sing-box 主内核未启动，node-agent 独自占用端口收到请求后 RST 连接。
> ```bash
> cat /etc/yundu/node-agent.env
> # 应包含：RUNTIME_TYPE=sing-box
> ```

**验证安装**：

```bash
# 查看服务状态
systemctl status yundu-node-agent

# 查看实时日志
journalctl -u yundu-node-agent -f

# 重启服务
systemctl restart yundu-node-agent
```

### 5.2 安装面板（控制端，部署在面板服务器上）

```bash
# 在面板服务器上以 root 执行
curl -fsSL https://github.com/energie2008/yundu/raw/main/install.sh | bash -s -- panel \
  --db-url=postgres://app:YunDuProd2026Secure@127.0.0.1:5433/airport?sslmode=disable \
  --redis-url=redis://127.0.0.1:6380
```

> **实际环境 DB 连接信息**（vps190）：
> - PostgreSQL 容器名：`yundu-postgres`
> - 用户：`app`（不是 postgres/yundu）
> - 数据库：`airport`（不是 yundu）
> - 密码：`YunDuProd2026Secure`
> - 端口：`5433`（仅监听 127.0.0.1，不对公网暴露）
> - 迁移表：`goose_db_version`（不是 schema_migrations），字段 `version_id` + `is_applied`

**安装的服务**：

| 服务名 | 端口 | 说明 |
|--------|------|------|
| yundu-api-gateway | 8080 | API 网关 |
| yundu-identity-service | 8081 | 认证/用户/订单 |
| yundu-node-service | 8082 | 节点服务 |
| yundu-subscription-service | 8083 | 订阅服务 |
| yundu-traffic-service | 8084 | 流量统计 |

> ⚠️ **端口安全**：8081-8090/9082 端口**不可对公网暴露**，需 iptables 拦截公网访问（但需先放行 Docker 网段 172.16.0.0/12，否则 Prometheus 无法抓取 /metrics）。5433/6380/4223（PG/Redis/NATS）同样不可公网暴露。

**安装后必须执行的步骤**：

```bash
# 1. 执行数据库迁移（必须！否则服务无法启动）
/opt/yundu/bin/migrate up

# 2. 配置环境（若安装时未传 --db-url）
# 编辑 /etc/yundu/panel.env 填入 DB 和 Redis 连接
vi /etc/yundu/panel.env

# 3. 重启所有服务
systemctl restart 'yundu-*'

# 4. 查看所有服务状态
systemctl status 'yundu-*'
```

> **node-service 特别说明**：
> - systemd 配置 `MemoryMax=200M`（内存上限 200MB）
> - 日志输出到 `/opt/yundu/logs/node-service.log`（非 journald）
> - 查看日志：`tail -f /opt/yundu/logs/node-service.log`
> - 服务名：`yundu-node-service.service`

---

## 六、升级

### 6.1 升级 node-agent（节点端）

```bash
# 升级到最新版
curl -fsSL https://github.com/energie2008/yundu/raw/main/install.sh | bash -s -- upgrade agent

# 升级到指定版本（回滚也用这个）
curl -fsSL https://github.com/energie2008/yundu/raw/main/install.sh | bash -s -- upgrade agent --version=v0.3.0
```

升级过程：下载新二进制 → 替换 → 自动重启服务。

### 6.2 升级面板（控制端）

```bash
# 升级所有面板组件到最新版
curl -fsSL https://github.com/energie2008/yundu/raw/main/install.sh | bash -s -- upgrade panel

# 升级到指定版本
curl -fsSL https://github.com/energie2008/yundu/raw/main/install.sh | bash -s -- upgrade panel --version=v0.3.1
```

升级过程：下载 5 个服务 + migrate → 逐个替换 → 重启所有服务。

> **注意**：面板升级后可能需要执行 `migrate up` 更新数据库结构（如有新迁移文件）。

### 6.3 全部升级

```bash
curl -fsSL https://github.com/energie2008/yundu/raw/main/install.sh | bash -s -- upgrade all
```

### 6.4 回滚到旧版本

如果新版本有问题，用 `--version` 指定旧版本回滚：

```bash
# 回滚 node-agent 到 v0.3.0
curl -fsSL https://github.com/energie2008/yundu/raw/main/install.sh | bash -s -- upgrade agent --version=v0.3.0
```

> **前提**：旧版本的 Release 在 GitHub 上未被删除。GitHub Release 不会因打新 tag 而消失。

---

## 六B、本地交叉编译热修复部署（生产 hotfix 标准流程）

> **适用场景**：紧急修复生产故障（节点下发失败、配置渲染错误等），需要 5 分钟内完成编译+部署。
> **验证记录**：731/801 的架构缺陷修复（约100行改动，5个文件）全部使用此流程部署，24/24 节点验证通过。
> **密钥路径**：`D:\机场搭建\key\`（190key.pem → vps190面板，206key → vps206节点，81key → vps81节点）

### 6B.1 本地交叉编译（Windows PowerShell）

```powershell
# === 编译 node-service（面板端，部署到 vps190）===
cd D:\机场搭建\yundu-src\apps\node-service
$env:GOOS="linux"; $env:GOARCH="amd64"; $env:CGO_ENABLED="0"
go build -o D:\机场搭建\tmp\node-service-new ./cmd/api/

# === 编译 node-agent（节点端，部署到 vps206/vps81）===
cd D:\机场搭建\yundu-src\apps\node-agent
$env:GOOS="linux"; $env:GOARCH="amd64"; $env:CGO_ENABLED="0"
# ⚠️ 必须携带全部 6 个功能标签，缺任何一个都会导致对应协议启动失败
go build -tags "with_utls,with_wireguard,with_gvisor,with_quic,with_grpc,with_clash_api" `
  -ldflags "-s -w -X main.AgentVersion=v0.7.13" `
  -o D:\机场搭建\tmp\node-agent-new ./cmd/agent/
```

**node-agent 编译标签说明（缺一不可）**：

| 标签 | 缺失后果 |
|------|----------|
| `with_utls` | REALITY inbound 启动失败 |
| `with_wireguard` | WireGuard endpoint 启动失败 |
| `with_gvisor` | WireGuard userspace 栈依赖缺失（需配合 with_wireguard） |
| `with_quic` | hysteria2/tuic 协议启动失败 |
| `with_grpc` | gRPC 传输启动失败 |
| `with_clash_api` | 流量统计采集失效（clash API 不可用） |

> 完整二进制约 63MB（含全部标签）。CI 中的标签配置见 §10.4。

### 6B.2 上传二进制到 VPS

```powershell
# 上传 node-service 到 vps190（面板）
scp -i "D:\机场搭建\key\190key.pem" `
  D:\机场搭建\tmp\node-service-new root@43.135.147.190:/tmp/node-service-new

# 上传 node-agent 到 vps206（节点）
scp -i "D:\机场搭建\key\206key" `
  D:\机场搭建\tmp\node-agent-new ubuntu@158.101.13.206:/tmp/node-agent-new
```

### 6B.3 部署二进制（SSH 到 VPS 执行）

> ⚠️ **关键避坑**：SCP 上传后二进制权限为 **644**（无执行权限），必须 `chmod +x` 后再启动，否则 systemd 报 `status=203/EXEC`。

```bash
# === 部署 node-service 到 vps190 ===
ssh -i "D:\机场搭建\key\190key.pem" root@43.135.147.190
chmod +x /tmp/node-service-new
systemctl stop yundu-node-service
cp /tmp/node-service-new /opt/yundu/bin/node-service
systemctl start yundu-node-service
# 验证
systemctl status yundu-node-service --no-pager | head -5
tail -10 /opt/yundu/logs/node-service.log
```

```bash
# === 部署 node-agent 到 vps206 ===
ssh -i "D:\机场搭建\key\206key" ubuntu@158.101.13.206
chmod +x /tmp/node-agent-new
sudo systemctl stop yundu-node-agent
sudo cp /tmp/node-agent-new /opt/yundu/bin/node-agent
sudo systemctl start yundu-node-agent
# 验证
sudo systemctl status yundu-node-agent --no-pager | head -5
sudo journalctl -u yundu-node-agent -n 10 --no-pager
```

> **替换原则**：运行中的二进制文件不可直接覆盖（报 `Text file busy`），必须先 `systemctl stop` 再 `cp`。

### 6B.4 数据库迁移（如有新 migration 文件）

> ⚠️ 迁移系统使用 **goose**（表名 `goose_db_version`，不是 schema_migrations）。手动执行 migration 后需注册版本号。

```powershell
# 1. 上传 migration SQL 到 vps190
scp -i "D:\机场搭建\key\190key.pem" `
  D:\机场搭建\yundu-src\migrations\0000NN_xxx.sql `
  root@43.135.147.190:/opt/yundu/migrations/
```

```bash
# 2. SSH 到 vps190 执行 migration
ssh -i "D:\机场搭建\key\190key.pem" root@43.135.147.190

# 复制 SQL 到 postgres 容器
docker cp /opt/yundu/migrations/0000NN_xxx.sql yundu-postgres:/tmp/migration.sql

# 执行 migration（通过 psql -f，避免引号问题）
docker exec yundu-postgres psql -U app -d airport -f /tmp/migration.sql

# 注册 goose 版本号（NN 替换为实际版本号，如 70）
docker exec yundu-postgres psql -U app -d airport -c \
  "INSERT INTO goose_db_version (version_id, is_applied, tstamp) VALUES (NN, true, NOW()) ON CONFLICT DO NOTHING;"

# 验证 migration 已注册
docker exec yundu-postgres psql -U app -d airport -c \
  "SELECT version_id, is_applied FROM goose_db_version ORDER BY version_id DESC LIMIT 5;"
```

### 6B.5 部署后验证

```bash
# === 服务状态验证 ===
systemctl status yundu-node-service --no-pager | head -10

# === 日志验证（检查有无 ERROR/panic）===
tail -50 /opt/yundu/logs/node-service.log | grep -i 'ERROR\|panic\|fatal'
# 预期：无输出（或仅 WARN 级别）

# === 节点下发状态验证（SQL）===
cat > /tmp/check_nodes.sql << 'SQLEOF'
SELECT s.code AS server, n.code AS node,
       n.metadata->>'_dispatch_status' AS status,
       n.metadata->>'_dispatch_version' AS ver
FROM nodes n
JOIN runtimes r ON n.runtime_id=r.id
JOIN servers s ON r.server_id=s.id
WHERE n.deleted_at IS NULL
ORDER BY s.code, n.code;
SQLEOF
docker cp /tmp/check_nodes.sql yundu-postgres:/tmp/check_nodes.sql
docker exec yundu-postgres psql -U app -d airport -f /tmp/check_nodes.sql
# 预期：全部节点 status=pushed，无 failed
```

### 6B.6 完整一键部署脚本（node-service + migration）

将以下命令组合为一次 SSH 会话（适合反复执行的热修复场景）：

```bash
ssh -i "D:\机场搭建\key\190key.pem" root@43.135.147.190 '
set -e
echo "=== 1. 部署二进制 ==="
chmod +x /tmp/node-service-new
systemctl stop yundu-node-service
cp /tmp/node-service-new /opt/yundu/bin/node-service
systemctl start yundu-node-service
echo "=== 2. 服务状态 ==="
systemctl is-active yundu-node-service
echo "=== 3. 日志检查 ==="
sleep 2
tail -5 /opt/yundu/logs/node-service.log
echo "=== DEPLOY_OK ==="
'
```

---

## 七、仅下载二进制（不安装）

有时只想下载二进制到本地检查或手动部署：

```bash
# 下载最新版 node-agent 到 /tmp
curl -fsSL https://github.com/energie2008/yundu/raw/main/install.sh | bash -s -- download \
  --bin=node-agent --dest=/tmp

# 下载指定版本
curl -fsSL https://github.com/energie2008/yundu/raw/main/install.sh | bash -s -- download \
  --bin=node-service --version=v0.3.0 --dest=/tmp
```

**可用的 `--bin` 值**：
`node-agent`、`node-service`、`api-gateway`、`identity-service`、`subscription-service`、`traffic-service`、`migrate`

---

## 八、手动下载二进制（不使用脚本）

如果 VPS 无法访问 GitHub raw（但能访问 releases），可手动下载：

```bash
# 确定架构
ARCH=$(uname -m)
case "$ARCH" in x86_64) ARCH=amd64;; aarch64) ARCH=arm64;; esac

# 下载（替换 VERSION 和 BIN_NAME）
VERSION=v0.3.1
BIN_NAME=node-agent
curl -fSL -o /opt/yundu/bin/node-agent \
  https://github.com/energie2008/yundu/releases/download/${VERSION}/${BIN_NAME}-${ARCH}
chmod +x /opt/yundu/bin/node-agent

# 重启服务
systemctl restart yundu-node-agent
```

---

## 九、常见问题排查

### 9.1 CI 编译失败

**查看失败日志**：

```bash
# 查看失败的 job
curl -s https://api.github.com/repos/energie2008/yundu/actions/runs/RUN_ID/jobs | jq '.jobs[] | select(.conclusion=="failure") | {name, steps: [.steps[] | select(.conclusion=="failure") | .name]}'
```

常见原因和解决：

| 错误信息 | 原因 | 解决 |
|----------|------|------|
| `missing go.sum entry` | go.sum 不完整 | 本地 `go mod tidy` 后提交 |
| `undefined: xxx` | 代码语法错误 | 检查代码，本地先 `go build` 验证 |
| `cannot find module` | 依赖缺失 | `go mod tidy` 补全依赖 |

**本地验证编译（推荐推送前测试）**：

```bash
# 用 WSL 测试编译（与 CI 环境一致，需携带全部 6 个功能标签）
wsl -d Ubuntu-22.04 -- bash -c '
  cd /mnt/d/机场搭建/yundu-src/apps/node-agent && \
  GOWORK=off CGO_ENABLED=0 GOOS=linux GOARCH=amd64 \
  go build -tags "with_utls,with_wireguard,with_gvisor,with_quic,with_grpc,with_clash_api" \
  -ldflags "-s -w -X main.AgentVersion=v0.7.13" \
  -o /tmp/test-build ./cmd/agent/ && \
  echo "编译成功" || echo "编译失败"
'
```

> **PowerShell 本地编译**（无需 WSL）：
> ```powershell
> cd D:\机场搭建\yundu-src\apps\node-agent
> $env:GOOS="linux"; $env:GOARCH="amd64"; $env:CGO_ENABLED="0"
> go build -tags "with_utls,with_wireguard,with_gvisor,with_quic,with_grpc,with_clash_api" `
>   -ldflags "-s -w -X main.AgentVersion=v0.7.13" `
>   -o D:\机场搭建\tmp\node-agent-new ./cmd/agent/
> ```

### 9.2 GLIBC 版本不兼容

**症状**：VPS 上启动报 `GLIBC_2.32 not found` 或 `GLIBC_2.34 not found`。

**原因**：CI 编译的二进制是 `CGO_ENABLED=0` 纯静态，不应有此问题。如果出现，说明使用了非 CI 产物（如本地 WSL 编译时忘记加 `CGO_ENABLED=0`）。

**解决**：
```bash
# 确认二进制是纯静态
file /opt/yundu/bin/node-agent
# 应输出: statically linked

# 如果不是，用 CI 产物重新安装
curl -fsSL https://github.com/energie2008/yundu/raw/main/install.sh | bash -s -- upgrade agent
```

### 9.3 下载失败 / 网络超时

**症状**：install.sh 执行时报 `下载失败` 或 `curl: (28) Connection timed out`。

**解决**：

```bash
# 方式一：指定更长超时（手动下载）
curl -fSL --connect-timeout 60 --max-time 600 -o /tmp/node-agent \
  https://github.com/energie2008/yundu/releases/download/v0.3.1/node-agent-amd64

# 方式二：用 GitHub 代理镜像
curl -fSL -o /tmp/node-agent \
  https://ghproxy.com/https://github.com/energie2008/yundu/releases/download/v0.3.1/node-agent-amd64

# 方式三：本地下载后 scp 上传
scp node-agent-amd64 root@VPS_IP:/opt/yundu/bin/node-agent
```

### 9.4 服务启动失败

```bash
# 查看错误日志
journalctl -u yundu-node-agent -n 50 --no-pager     # node-agent 用 journald
tail -50 /opt/yundu/logs/node-service.log            # node-service 用文件日志

# 常见原因：
# 1. token 或 endpoint 错误 → 检查 systemd unit 里的参数
cat /etc/systemd/system/yundu-node-agent.service | grep ExecStart

# 2. 配置目录权限问题
ls -la /etc/yundu/

# 3. 端口被占用
ss -tlnp | grep -E '8080|8081|8082|8083|8084'

# 4. ⚠️ 二进制无执行权限（SCP 上传后常见）
#    症状：systemctl status 显示 status=203/EXEC
#    解决：chmod +x /opt/yundu/bin/node-service
ls -la /opt/yundu/bin/node-service
# 应显示 -rwxr-xr-x（755），若为 -rw-r--r--（644）则需 chmod +x

# 5. ⚠️ 运行中文件被覆盖（Text file busy）
#    症状：cp 报 "Text file busy"
#    解决：先 systemctl stop 再 cp，不可直接覆盖运行中二进制
```

### 9.5 升级后服务异常

```bash
# 回滚到上一个版本
curl -fsSL https://github.com/energie2008/yundu/raw/main/install.sh | bash -s -- upgrade agent --version=v0.3.0

# 查看所有历史 Release
curl -s https://api.github.com/repos/energie2008/yundu/releases | jq '.[] | .tag_name'
```

### 9.6 改了前端但面板界面没变化

**症状**：修改了 admin-web 界面代码，`git push` + 打 tag 发版、CI 编译成功，但打开面板还是旧界面。

**原因**（2026-07-30 实际踩坑）：

| 原因 | 判断方法 |
|------|----------|
| CI 不编译前端 | Release 资产里只有 14 个后端二进制，没有前端文件 |
| 部署到了错误目录 | `/opt/yundu/admin-web` 是历史遗留目录，nginx 实际用的是宝塔的 `/www/wwwroot/域名/` |
| 浏览器/CDN 缓存 | `curl` 拿到新内容但浏览器显示旧界面 |

**排查步骤**：

```bash
# 1. 确认 nginx 真正的站点根目录（不要凭记忆猜目录）
nginx -T 2>/dev/null | grep -E 'server_name|root ' | grep -v '#'

# 2. 确认该目录下 index.html 是否已是新版
grep -o '<title>[^<]*</title>' /www/wwwroot/6.tiktokplay.na.am/index.html

# 3. 从外网确认线上返回新版
curl -s https://6.tiktokplay.na.am | grep -o '<title>[^<]*</title>'
```

**解决**：按「十一、前端构建与部署」重新构建并部署到正确目录；若 curl 已是新版而浏览器仍旧，按 `Ctrl+F5` 强制刷新，必要时在 Cloudflare 清理该域名的缓存。

### 9.7 PowerShell SSH 执行 SQL 引号被吞

**症状**：通过 PowerShell SSH 执行 `psql -c "SELECT ... WHERE x='y'"` 时，双引号和单引号均被 PowerShell 破坏，SQL 报语法错误或 `column does not exist`。

**根因**：PowerShell 在将命令传递给 SSH 时，会先解析并剥离双引号；SSH 到远程 shell 后单引号也可能被二次解析。

**正确方式**（heredoc + docker cp + psql -f）：

```bash
# 1. 用 heredoc+单引号写入 SQL 文件（'SQLEOF' 阻止变量展开）
cat > /tmp/diag.sql << 'SQLEOF'
SELECT s.code, n.code, n.metadata->>'_dispatch_status' AS status
FROM nodes n
JOIN runtimes r ON n.runtime_id=r.id
JOIN servers s ON r.server_id=s.id
WHERE n.deleted_at IS NULL;
SQLEOF

# 2. docker cp 到 postgres 容器
docker cp /tmp/diag.sql yundu-postgres:/tmp/diag.sql

# 3. psql -f 执行（完全避免引号问题）
docker exec yundu-postgres psql -U app -d airport -f /tmp/diag.sql
```

> **禁止**：`docker exec yundu-postgres psql -U app -d airport -c "SELECT ... WHERE x='y'"` —— PowerShell 会吞掉双引号和单引号。

### 9.8 DB 连接信息错误

**症状**：`psql -U postgres` 或 `psql -U yundu` 报 `role does not exist`；查 `schema_migrations` 表报 `relation does not exist`。

**正确连接信息**：

| 项 | 错误值 | 正确值 |
|----|--------|--------|
| 用户 | postgres / yundu | **app** |
| 数据库 | yundu | **airport** |
| 迁移表 | schema_migrations | **goose_db_version** |
| 迁移字段 | version | **version_id** + **is_applied** |
| 连接方式 | 直连 psql | **docker exec yundu-postgres psql -U app -d airport** |

```bash
# 正确的 DB 查询方式
docker exec yundu-postgres psql -U app -d airport -c '\dt'           # 列出表
docker exec yundu-postgres psql -U app -d airport -c '\d nodes'      # 查看表结构

# 查看已应用的 migration 版本
docker exec yundu-postgres psql -U app -d airport -c \
  'SELECT version_id, is_applied FROM goose_db_version ORDER BY version_id DESC LIMIT 5;'
```

### 9.9 runtimes 表无 deleted_at 列

**症状**：执行 SQL 查询 `SELECT ... FROM runtimes WHERE deleted_at IS NULL` 报 `column does not exist`。

**根因**：`runtimes` 表使用 `ON DELETE CASCADE` 硬删除（删除 runtime 时级联删除关联 nodes），**没有 deleted_at 列**。与 `nodes` 表（有 deleted_at 软删除）不同。

**解决**：查询 runtimes 表时不要加 `deleted_at IS NULL` 条件。

### 9.10 config_versions 表无 runtime_id 列

**症状**：执行 SQL `SELECT runtime_id FROM config_versions` 报 `column does not exist`。

**根因**：`config_versions` 表用 `scope_id` 字段关联 runtime（`cv.scope_id = rt.id`），不是 `runtime_id`。

**解决**：
```sql
-- 正确的 JOIN
SELECT cv.version_no, cv.created_at
FROM config_versions cv
JOIN runtimes rt ON cv.scope_id = rt.id
WHERE rt.server_id = '...';
```

---

## 十、附录

### 10.1 产物清单

| 二进制名 | 用途 | 部署位置 | 构建标签 | 二进制约 |
|----------|------|----------|----------|:---:|
| node-agent | 节点代理（双内核 xray + sing-box） | VPS 节点 | `with_utls,with_wireguard,with_gvisor,with_quic,with_grpc,with_clash_api` | 63MB |
| node-service | 节点服务（面板端） | 面板服务器 | 默认 | 59MB |
| api-gateway | API 网关 | 面板服务器 | 默认 | 44MB |
| identity-service | 认证服务 | 面板服务器 | 默认 | 49MB |
| subscription-service | 订阅服务 | 面板服务器 | 默认 | 50MB |
| traffic-service | 流量统计 | 面板服务器 | 默认 | 47MB |
| migrate | 数据库迁移 | 面板服务器 | 默认 | 14MB |

> ⚠️ **node-agent 编译标签更新**（2026-08-01）：原文档仅列出 3 个标签（`with_quic,with_wireguard,with_utls`），实际需要 **6 个标签**。缺失标签会导致运行时启动失败（详见 §6B.1 标签说明表）。CI 配置也需同步更新（见 §10.4）。

### 10.2 标准目录结构（安装后）

```
/opt/yundu/
├── bin/                    # 二进制安装目录
│   ├── node-agent          # 节点端（VPS 节点）
│   ├── node-service        # 面板端（vps190）
│   ├── api-gateway
│   ├── identity-service
│   ├── subscription-service
│   ├── traffic-service
│   └── migrate
├── logs/                   # 日志目录（node-service 输出到此，非 journald）
│   └── node-service.log    # node-service 日志文件
├── migrations/             # 数据库迁移 SQL 文件
│   ├── 000001_xxx.sql
│   └── ...
└── src/yundu/              # vps190 上的源码副本（可能滞后于本地，编译推荐用本地交叉编译）

/etc/yundu/                 # 配置目录（权限 700）
├── config/
├── certs/
├── panel.env               # 面板环境配置（含 DB/Redis 连接）
├── node-agent.env          # 节点端环境配置（含 RUNTIME_TYPE=sing-box）
└── node-agent.yaml         # 节点端运行配置

/etc/systemd/system/        # systemd 服务文件
├── yundu-node-agent.service        # node-agent 服务（节点端）
├── yundu-api-gateway.service
├── yundu-identity-service.service
├── yundu-node-service.service     # node-service 服务（MemoryMax=200M）
├── yundu-subscription-service.service
└── yundu-traffic-service.service
```

> **node-agent.env 关键配置**（VPS 节点端）：
> ```bash
> # /etc/yundu/node-agent.env
> RUNTIME_TYPE=sing-box        # 主内核类型（必须显式设置，否则可能默认 xray 导致双核异常）
> ```
> VPS81 曾因 node-agent.env 为空（缺失 RUNTIME_TYPE）导致 sing-box 主内核未启动，node-agent 独自占用端口收到请求后 RST 连接。

> **node-service systemd 关键配置**（面板端）：
> - `MemoryMax=200M`（内存上限 200MB）
> - 日志输出到 `/opt/yundu/logs/node-service.log`（非 journald）
> - 查看：`tail -f /opt/yundu/logs/node-service.log`

### 10.3 常用运维命令速查

```bash
# === 服务管理 ===
systemctl status yundu-node-agent          # 查看状态（节点端）
systemctl status yundu-node-service        # 查看状态（面板端）
systemctl restart yundu-node-agent         # 重启
systemctl stop yundu-node-agent            # 停止
systemctl enable yundu-node-agent          # 开机自启

# === 日志 ===
# node-agent（节点端，用 journald）
journalctl -u yundu-node-agent -f          # 实时日志
journalctl -u yundu-node-agent -n 100      # 最近 100 行
journalctl -u yundu-node-agent --since "1 hour ago"

# node-service（面板端，用文件日志）
tail -f /opt/yundu/logs/node-service.log   # 实时日志
tail -100 /opt/yundu/logs/node-service.log # 最近 100 行
grep 'ERROR\|panic' /opt/yundu/logs/node-service.log | tail -20  # 查看错误

# === 批量操作面板服务 ===
systemctl status 'yundu-*'                 # 查看所有面板服务
systemctl restart 'yundu-*'                # 重启所有面板服务

# === 数据库迁移（migrate 工具）===
/opt/yundu/bin/migrate up                  # 执行迁移
/opt/yundu/bin/migrate down                # 回滚最近一次迁移

# === 数据库查询（docker exec psql，避免引号问题用 -f）===
# 查看已应用的 migration 版本
docker exec yundu-postgres psql -U app -d airport -c \
  'SELECT version_id, is_applied FROM goose_db_version ORDER BY version_id DESC LIMIT 5;'

# 查看节点下发状态
cat > /tmp/check.sql << 'SQLEOF'
SELECT s.code, n.code, n.metadata->>'_dispatch_status' AS status
FROM nodes n JOIN runtimes r ON n.runtime_id=r.id JOIN servers s ON r.server_id=s.id
WHERE n.deleted_at IS NULL ORDER BY s.code, n.code;
SQLEOF
docker cp /tmp/check.sql yundu-postgres:/tmp/check.sql
docker exec yundu-postgres psql -U app -d airport -f /tmp/check.sql

# 查看 runtime 唯一性
docker exec yundu-postgres psql -U app -d airport -c \
  'SELECT s.code, r.runtime_type, r.provider_ref FROM runtimes r JOIN servers s ON r.server_id=s.id ORDER BY s.code;'

# === 版本检查 ===
/opt/yundu/bin/node-agent --version        # 查看当前 agent 版本
curl -s https://api.github.com/repos/energie2008/yundu/releases/latest | jq .tag_name  # 查看最新 Release 版本
```

### 10.4 CI/CD 关键配置说明

`.github/workflows/release.yml` 中的关键设置：

| 配置 | 值 | 说明 |
|------|------|------|
| 触发条件 | `push: tags: v*` | 只有打 `v` 开头的 tag 才触发 |
| 编译环境 | `ubuntu-latest` | GitHub 托管 runner |
| Go 版本 | `1.26` | 指定 Go 版本 |
| `CGO_ENABLED` | `0` | 纯静态编译，无 GLIBC 依赖 |
| `GOWORK` | `off` | 关闭 workspace 模式，独立编译每个 app |
| `GOPROXY` | `goproxy.cn` | 国内代理加速依赖下载 |
| node-agent 标签 | `with_utls,with_wireguard,with_gvisor,with_quic,with_grpc,with_clash_api` | **6个标签**（详见 §6B.1），支持 REALITY/WireGuard/Hysteria2/Tuic/gRPC/Clash API |
| 目标平台 | `linux/amd64, linux/arm64` | 两种架构 |

> ⚠️ **CI 标签待更新**：当前 `.github/workflows/release.yml` 中 node-agent 的编译标签可能仍是旧的 3 个（`with_quic,with_wireguard,with_utls`），需更新为完整的 6 个标签。否则 CI 产出的 node-agent 会缺失 REALITY（缺 with_utls）、WireGuard（缺 with_wireguard+with_gvisor）、hysteria2/tuic（缺 with_quic）、流量统计（缺 with_clash_api）等能力。

---

## 十一、前端构建与部署（admin-web / user-web）

> CI 只编译 Go 后端，前端改动需要按本节流程单独发布。本节内容基于 2026-07-30 实际部署验证。

### 11.1 站点目录对照（vps190 = 43.135.147.190，宝塔 nginx）

| 站点 | 域名 | nginx 根目录 |
|------|------|------------|
| 管理后台 admin-web | 6.tiktokplay.na.am | `/www/wwwroot/6.tiktokplay.na.am` |
| 用户前台 user-web | 7.tiktokplay.na.am | `/www/wwwroot/7.tiktokplay.na.am` |

> ⚠️ **踩坑记录**：服务器上还存在 `/opt/yundu/admin-web` 目录（含多个 .bak 备份），但 nginx **并未使用**该目录，部署到那里不会生效。部署前先用 `nginx -T | grep -E 'server_name|root '` 确认真实根目录。

### 11.2 本地构建

在 `d:\机场搭建\yundu-src` 目录执行：

```powershell
# 构建管理后台（产物在 apps/admin-web/dist）
pnpm --filter admin-web build

# 构建用户前台（产物在 apps/user-web/dist）
pnpm --filter user-web build

# 若 pnpm 不在 PATH（本机实际情况），用 npm exec 调用：
npm exec --yes -- pnpm --filter admin-web build
# 或直接在子目录里 npm run build：
cd apps/admin-web; npm run build
```

### 11.3 打包上传

```powershell
# 打包 dist（在 yundu-src 根目录）
tar -czf tmp/admin-web-dist.tar.gz -C apps/admin-web/dist .

# 上传到 vps190
scp -i "d:\机场搭建\key\190key.pem" tmp/admin-web-dist.tar.gz root@43.135.147.190:/tmp/
```

### 11.4 服务器端部署（备份 → 替换 → 改属主）

> ⚠️ PowerShell 下 ssh 远程命令要用**单引号**包裹，双引号会把 `$W`、`$(date ...)` 在本地就展开导致出错（2026-07-30 踩坑）。

```powershell
ssh -i "d:\机场搭建\key\190key.pem" root@43.135.147.190 'set -e; W=/www/wwwroot/6.tiktokplay.na.am; cp -a $W ${W}.bak.$(date +%Y%m%d%H%M); rm -rf $W/assets $W/index.html; tar -xzf /tmp/admin-web-dist.tar.gz -C $W; chown -R www:www $W 2>/dev/null || true; grep -o "<title>[^<]*</title>" $W/index.html; echo DEPLOY_OK'
```

部署 user-web 同理，把目录换成 `/www/wwwroot/7.tiktokplay.na.am`、包换成 user-web 的 dist 即可。

### 11.5 验证与缓存

```bash
# 线上验证（看标题是否为新版）
curl -s https://6.tiktokplay.na.am | grep -o '<title>[^<]*</title>'
```

- 域名套了 Cloudflare（cf-cache-status: DYNAMIC，index.html 未被 CDN 缓存），一般部署完 curl 立即生效
- 浏览器仍显示旧界面时 `Ctrl+F5` 强制刷新；构建产物 JS/CSS 文件名带 hash，正常会自动更新

### 11.6 回滚

```bash
# 每次部署前都会生成 .bak.时间戳 备份，回滚直接整目录恢复：
ls -d /www/wwwroot/6.tiktokplay.na.am.bak.*
rm -rf /www/wwwroot/6.tiktokplay.na.am
cp -a /www/wwwroot/6.tiktokplay.na.am.bak.202607300216 /www/wwwroot/6.tiktokplay.na.am
```

### 11.7 待优化

- 建议后续在 `.github/workflows/release.yml` 增加前端构建 job（pnpm build 后把 dist 打包进 Release 产物），实现前后端一体化发版，避免发版遗漏前端

---

## 快速参考卡

```bash
# ===== 路径 A：推送代码（不发版本）=====
cd d:\机场搭建\yundu-src
git add -A && git commit -m "描述改动"
git push origin main

# ===== 路径 A：发布新版本（触发 CI 编译，仅后端）=====
git tag -a v0.3.X -m "版本说明"
git push origin v0.3.X

# ===== 路径 B：本地交叉编译热修复（5分钟部署）=====
# 1. 本地编译
cd D:\机场搭建\yundu-src\apps\node-service
$env:GOOS="linux"; $env:GOARCH="amd64"; $env:CGO_ENABLED="0"
go build -o D:\机场搭建\tmp\node-service-new ./cmd/api/
# 2. SCP 上传
scp -i "D:\机场搭建\key\190key.pem" D:\机场搭建\tmp\node-service-new root@43.135.147.190:/tmp/
# 3. SSH 部署（chmod +x 必做！）
ssh -i "D:\机场搭建\key\190key.pem" root@43.135.147.190 'chmod +x /tmp/node-service-new && systemctl stop yundu-node-service && cp /tmp/node-service-new /opt/yundu/bin/node-service && systemctl start yundu-node-service && systemctl is-active yundu-node-service'
# 4. 验证日志
ssh -i "D:\机场搭建\key\190key.pem" root@43.135.147.190 'tail -10 /opt/yundu/logs/node-service.log'

# ===== 路径 B：数据库 migration（手动执行）=====
scp -i "D:\机场搭建\key\190key.pem" D:\机场搭建\yundu-src\migrations\0000NN.sql root@43.135.147.190:/opt/yundu/migrations/
ssh -i "D:\机场搭建\key\190key.pem" root@43.135.147.190 'docker cp /opt/yundu/migrations/0000NN.sql yundu-postgres:/tmp/m.sql && docker exec yundu-postgres psql -U app -d airport -f /tmp/m.sql && docker exec yundu-postgres psql -U app -d airport -c "INSERT INTO goose_db_version (version_id, is_applied, tstamp) VALUES (NN, true, NOW()) ON CONFLICT DO NOTHING;"'

# ===== DB 查询（避免引号问题用 heredoc+psql -f）=====
ssh -i "D:\机场搭建\key\190key.pem" root@43.135.147.190 'cat > /tmp/q.sql << "SQLEOF"
SELECT s.code, n.code, n.metadata->>"_dispatch_status" FROM nodes n JOIN runtimes r ON n.runtime_id=r.id JOIN servers s ON r.server_id=s.id WHERE n.deleted_at IS NULL;
SQLEOF
docker cp /tmp/q.sql yundu-postgres:/tmp/q.sql && docker exec yundu-postgres psql -U app -d airport -f /tmp/q.sql'

# ===== 前端发布（CI 不管前端，单独执行）=====
pnpm --filter admin-web build
tar -czf tmp/admin-web-dist.tar.gz -C apps/admin-web/dist .
scp -i "d:\机场搭建\key\190key.pem" tmp/admin-web-dist.tar.gz root@43.135.147.190:/tmp/
# 再按「十一」节 ssh 单引号命令部署到 /www/wwwroot/6.tiktokplay.na.am

# ===== VPS 上安装/升级 agent ======
curl -fsSL https://github.com/energie2008/yundu/raw/main/install.sh | bash -s -- agent --token=TOKEN --endpoint=https://panel.com
curl -fsSL https://github.com/energie2008/yundu/raw/main/install.sh | bash -s -- upgrade agent

# ===== VPS 上升级面板 ======
curl -fsSL https://github.com/energie2008/yundu/raw/main/install.sh | bash -s -- upgrade panel

# ===== 查看最新版本 ======
curl -s https://api.github.com/repos/energie2008/yundu/releases/latest | jq .tag_name
```

> **密钥对照**：`190key.pem` → vps190 (43.135.147.190, root, 面板) | `206key` → vps206 (158.101.13.206, ubuntu, 节点) | `81key` → vps81 (132.226.156.81, ubuntu, 节点)
> **DB 连接**：`docker exec yundu-postgres psql -U app -d airport`（用户 app，库 airport，迁移表 goose_db_version）

---

## 十二、2026-08-16/17 发版与部署实录增补（v0.8.3 全流程验证）

> 本节为最近一轮真实发版/部署的实操存档，命令全部实际执行并验证过，遇到的新坑已标注。

### 12.1 正式发版（tag → CI → Release）实测 SOP

```bash
# 1) 发版前本地门禁（CI test job 会卡这两项，先本地跑绿）
bash scripts/check_doc_refs.sh        # 文档 file:line 引用校验（根目录+docs所有md都会扫）
cd apps/node-agent && go test ./internal/hotdiff/... ./internal/pipeline/... ./internal/validator/... ./internal/warp/... ./internal/sysmetrics/...
cd ../identity-service && go test ./internal/...

# 2) 提交推送 + 打 tag（tag 打在最新 commit 上）
git push origin main
git tag v0.8.3 && git push origin v0.8.3

# 3) 监控 CI（gh CLI）
gh run list --repo energie2008/yundu --limit 3
gh run watch <run-id>   # 或 gh run view <id> 轮询

# 4) 验证产物（7组件×2架构=14个）
curl -s https://api.github.com/repos/energie2008/yundu/releases/latest | grep -m1 tag_name
curl -s https://api.github.com/repos/energie2008/yundu/releases/latest | grep -c browser_download_url
```

**v0.8.3 实测数据**：CI 全链 9 分钟（Go Tests 34s → Frontend 33s → amd64 4m05s → arm64 4m33s → Publish 23s）。

**发版坑（v0.8.3 实录）**：根目录历史审计文档含裸文件名引用（如 `apps/node-service/internal/handler/agent_handler.go:193`），
check_doc_refs.sh 判 MISSING FILE 卡死 CI test job——v0.7.31 之后新增的文档首次过发版校验才会暴露。
处理：非必要文档移出仓库（本机 docs-archive 归档），保留文档补全仓库相对路径。

### 12.2 本地交叉编译标准命令集（全部实测）

```bash
# 环境变量（Git Bash）
export GOOS=linux GOARCH=amd64 CGO_ENABLED=0 GOTOOLCHAIN=local

# node-agent（必须 6 标签 + 版本注入；运行中二进制 Text file busy，必须先 stop 再 cp）
cd apps/node-agent && go build -tags "with_utls,with_wireguard,with_gvisor,with_quic,with_grpc,with_clash_api" \
  -ldflags "-s -w -X main.AgentVersion=v0.8.3" -o dist/node-agent ./cmd/agent/

# 纯 Go 服务（无需标签）：node-service / identity-service / subscription-service / traffic-service / api-gateway
cd apps/identity-service && go build -ldflags "-s -w" -o dist/identity-service ./cmd/api

# yunductl（部署到所有VPS的 /usr/local/bin/）
go build -ldflags "-s -w" -o dist/yunductl ./apps/yunductl/

# 前端（pnpm；产物在 dist/，含 public 静态资源）
cd apps/user-web && pnpm install --frozen-lockfile && pnpm run build   # admin-web 同理
```

### 12.3 部署命令模板（带哈希校验 + 备份，全部实测）

```bash
# 后端服务（VPS190，§4.5 标准路径）
scp -C -i "d:/机场搭建/key/190key.pem" dist/<svc> root@43.135.147.190:/tmp/<svc>-new
ssh -i "d:/机场搭建/key/190key.pem" root@43.135.147.190 '
  sha256sum /tmp/<svc>-new &&                      # 与本地 sha256sum 比对
  systemctl stop yundu-<svc> &&
  cp /opt/yundu/bin/<svc> /opt/yundu/bin/<svc>.bak.<标记>.$(date +%Y%m%d%H%M%S) &&
  cp /tmp/<svc>-new /opt/yundu/bin/<svc> && chmod +x /opt/yundu/bin/<svc> &&
  systemctl start yundu-<svc> && sleep 3 && systemctl is-active yundu-<svc>'

# node-agent（节点 VPS206/81；注意 -F /dev/null 绕过旧 ssh config）
scp -C -i "d:/机场搭建/key/206.key" -F /dev/null -o IdentitiesOnly=yes dist/node-agent ubuntu@158.101.13.206:/tmp/
ssh -i "d:/机场搭建/key/206.key" -F /dev/null -o IdentitiesOnly=yes ubuntu@158.101.13.206 '
  sudo systemctl stop yundu-node-agent &&
  sudo cp /opt/yundu/bin/node-agent /opt/yundu/bin/node-agent.bak.<标记>.$(date +%Y%m%d%H%M%S) &&
  sudo cp /tmp/node-agent /opt/yundu/bin/node-agent && sudo chmod +x /opt/yundu/bin/node-agent &&
  sudo systemctl start yundu-node-agent'

# 前端（备份旧产物 → 整体替换 assets/index.html；pay/cities 等目录来自 public 一并更新）
ssh ... 'cp -r /www/wwwroot/7.tiktokplay.na.am/assets /www/wwwroot/7.tiktokplay.na.am/index.html /www/wwwroot/7.tiktokplay.na.am.bak.$(date +%Y%m%d%H%M%S)/ 2>/dev/null;
  rm -rf /www/wwwroot/7.tiktokplay.na.am/assets && cp -r /tmp/dist-new/* /www/wwwroot/7.tiktokplay.na.am/'
curl -sk https://7.tiktokplay.na.am/ | grep -o "index-[A-Za-z0-9_-]*\.js"   # 验证新 bundle 上线
```

**验证要点**：服务 active + 日志无 ERROR（/opt/yundu/logs/<svc>.log，非 journalctl）+ 业务接口 200。

### 12.4 system_settings 类配置的热更新部署（渠道池等实测）

直接改库后需重启对应服务加载（或走后台 API 自动热加载）：

```bash
# 写入（pg_read_file 只能读 postgres 容器内文件，宿主机文件先 docker cp 进容器）
docker cp /tmp/xx.json yundu-postgres:/tmp/ && docker exec yundu-postgres psql -U app -d airport \
  -c "INSERT INTO system_settings (...) SELECT ..., pg_read_file('/tmp/xx.json')::jsonb, ..."
systemctl restart yundu-identity-service   # 或对应服务
```

⚠️ jsonb 数组下标更新注意元素顺序（`{channels,0,...}` 按 SELECT 顺序核对，v2版易支付接入时
索引写错更新到隔壁渠道，发现后立即修正）。

### 12.5 新 VPS 接入 + 版本收敛（2026-08-16 验证）

```bash
# 面板"服务器管理→添加服务器"复制安装命令（已修复为 GitHub install.sh + --runtime=sing-box），
# 或手动（install.sh 默认 runtime 已翻转为 sing-box，无需显式指定）：
curl -fsSL https://github.com/energie2008/yundu/raw/main/install.sh | bash -s -- agent \
  --token=<面板token> --endpoint=https://面板域名

# 版本收敛：install.sh --version=latest 拉 GitHub 最新 Release（v0.8.3 起 Release 含全部修复）；
# 存量节点用 面板→系统设置→Agent版本管理 推自升级（面板自托管二进制通道，不依赖GitHub）
curl -s https://api.github.com/repos/energie2008/yundu/releases/latest | grep -m1 tag_name
```

TCP/CDN 节点需预装 nginx（agent 不自动安装，纯 UDP 节点不需要）；云安全组放行
80/443、30000-30399/tcp、40020-40299/udp；节点 SNI 域名解析到新 VPS。
