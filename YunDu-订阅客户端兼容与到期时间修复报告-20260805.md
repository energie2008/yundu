# YunDu 订阅客户端兼容（karing/hiddify）与到期时间修复报告

> 日期：2026-08-05（两轮修复）
> 范围：订阅面（subscription-service）+ 控制面（identity-service）
> 涉及客户端：karing、hiddify、shadowrocket、v2rayN、throne
> 涉及订阅地址：`https://ad.tiktokplay.na.am/sub/06ffd42606394c00206fdf650ef6252415ed66bf318796d8893804e6187df7de`

---

## 一、背景与订阅链路

订阅服务 `subscription-service` 按 UA 识别客户端（`internal/client/detector.go`）并映射渲染器：
karing/hiddify → sing-box JSON；shadowrocket/v2rayN/throne → base64 URI。
生产 sing-box JSON 由数据库模板 `subscribe_templates.singbox` 为基础、注入节点 outbound 后下发。

### 节点概况（该订阅用户当前可见）

| 视角 | 节点数 | 说明 |
|---|---|---|
| URI（v2rayN/throne/shadowrocket） | 26 | vless×16、trojan×5、hy2×2、anytls×2、vmess×1 |
| sing-box JSON（karing/hiddify） | 21 | 11 vless + 5 trojan + 2 hy2 + 2 anytls + 1 vmess |
| 两者差异 | 5 | 均为 XHTTP+REALITY / split-mode 节点（P09/P17/P07/P15/vps206-p22），sing-box 内核不支持，只能由 xray 系客户端使用 |

---

## 二、问题一：karing / hiddify 订阅不可用

### 第一轮根因（已修复）：sing-box 模板含非法 `match` 字段

`subscribe_templates.singbox` 的 `route.rules` 含 `{"match": true, ...}`，sing-box 对未知字段严格拒绝，
整份 JSON 无法加载（`route.rules[4].match: json: unknown field "match"`），导致 hiddify「配置失败」、
karing 解析异常。已从模板与内置渲染器移除，并补 `default_domain_resolver`；
在线订阅已通过 sing-box v1.12/1.13 `check`。

### 第二轮根因（本次修复，用户仍失败的真凶）：karing/hiddify 真实 UA 被误判为 Clash Meta

用户 karing 真实 UA 是：

```
Karing/1.2.22.2502 platform/windows;mihomo/1.19.27;clash-verge;FLClash;ClashMeta;
v2ray;sing-box 1.13.0;NekoBox/Android/1.4.1 (Prefer ClashMeta Format);HiddifyNext
```

hiddify 真实 UA 是：`HiddifyNext/4.1.1 (windows) like ClashMeta v2ray sing-box`

`internal/client/detector.go` 的 switch **先匹配 `mihomo`/`clash-verge`/`clashmeta`，后匹配 `karing`/`hiddify`**，
而这两个 UA 里都带有 mihomo/clash 等兼容 token → 被误判为 **Clash Meta** → 返回 **Clash YAML**（`text/yaml`），
不是 karing/hiddify 原生的 sing-box JSON。karing 导入该 Clash YAML 时 vless 节点大量丢失（只剩 11 个）。

**修复**：把 `karing`、`hiddify-next`、`hiddify` 检测提前到 clash/mihomo 之前；
新增单测锁定该行为（`internal/client/detector_test.go`）。
修复后实测：karing/hiddify 真实 UA 均返回 `application/json`（sing-box JSON，21 节点），
通过 sing-box 1.13 `check`。

### 关于用户怀疑的“链式代理套娃”

已核查节点库：该用户可见的 26 个节点 **均无链式代理配置**（config_json/metadata 无 chain 字段），
节点名里的“中转”指 CDN 中转（Cloudflare），不是代理套娃。**显示不全的真因是上述 UA 误判**。

### 关于 karing「更新失败 / download profile failed / connection terminated during handshake」

服务端日志确认 karing 的订阅请求**实际到达并返回 200**（含 windows 直连 111.58.46.148、
iOS 经 vps81 中转 132.226.156.81）。iOS 报「connection terminated during handshake」是 karing
**通过其当前所选节点代理下载订阅**时的网络层失败（karing 默认 preferProxy）；
请把 karing 订阅的「下载方式」改为「优先直连」或先选一个可用节点再更新。

---

## 三、问题二：订阅到期时间显示 1970/1/1

**根因**：用户 active 订阅（`source='admin_change'`，66g 月付）`expires_at` 为 NULL——
`AdminChangePlan`（管理端改套餐）建订阅时未写到期时间。
**修复**：代码按 `plan.duration_days` 写 `expires_at`（已部署）；
2 条受影响订阅补 `expires_at = started_at + 30天`。
**验证**：`Subscription-Userinfo ... expire=1788385425` → 2026-09-03 05:43:45 ✅

---

## 四、问题三：订阅名称默认显示「云渡 YunDu」（管理后台）

**根因**：`subscription_handler.go` 硬编码 `Profile-Title: YunDu`；
且生产 `SUB_BASE_URL=https://6.tiktokplay.na.am`（管理后台域名）。
**修复**：
1. `Profile-Title` 改为显示订阅站点：`SITE_NAME` 环境变量 → `SUB_BASE_URL` 的 host → 请求 Host；
   `Profile-Web-Page-URL` 使用固定的 `SUB_BASE_URL`。
2. 生产 `.env` 的 `SUB_BASE_URL` 改为 `https://ad.tiktokplay.na.am`（订阅地址网站）。
**验证**：所有客户端 `Profile-Title: ad.tiktokplay.na.am`，`Profile-Web-Page-URL: https://ad.tiktokplay.na.am`。

---

## 五、部署记录

| 项目 | 内容 |
|---|---|
| 代码提交 | `247e16b`（singbox 渲染器 + AdminChangePlan）、`479bccb`（UA 检测 + 订阅名称 + 单测） |
| 生产库 | `subscribe_templates.singbox` 更新；2 条订阅补 `expires_at` |
| 配置 | `/opt/yundu/config/.env` `SUB_BASE_URL=https://ad.tiktokplay.na.am`（已备份） |
| 二进制 | subscription-service 重建部署 vps190（旧版 `.bak.fix2_*`），identity-service 已部署 |
| 验证 | karing/hiddify → sing-box JSON 21 节点过 check；shadowrocket/v2rayN/throne → URI 26 节点；expire 正确；Profile-Title 正确 |

---

## 六、结论与遗留建议

1. karing/hiddify 不可用根因是 **UA 误判**（先匹配 mihomo/clash token），已修复——karing/hiddify 现收到
   原生 sing-box JSON（21 节点）。**请在 karing/hiddify 上重新更新订阅验证**。
2. sing-box 系客户端最多 21 个节点：其余 5 个 XHTTP+REALITY/split-mode 节点只能由 xray 系客户端使用。
3. karing 更新订阅建议用「优先直连」下载（preferDirect），避免走失效节点导致握手失败。
4. 到期时间与订阅名称均已修复。
5. 遗留：邮箱验证路径免费套餐到期时间与注册路径不一致（`try_plan_time`），建议后续对齐（未改以免影响免费用户）。

---

## 七、hiddify「添加配置文件失败 SqliteException profile_override」（客户端本地问题）

**结论：与订阅服务器无关**（服务器返回的 hiddify sing-box JSON 已通过 sing-box 校验）。
这是 hiddify 客户端本地 SQLite 数据库迁移失败导致的已知 bug：
- hiddify 的 `profile_entries` 表在 schema v5 加入 `profile_override`、v6 又移除；
- 用户从旧版本（如 2.x）跨版本升级到 4.x 时，本地数据库迁移未正确执行，
  表里缺少 `profile_override` 列，而当前 app 代码仍按含该列的语句 INSERT → 报
  `table profile_entries has no column named profile_override`。
- hiddify 官方 issue：#2310、#2219、#2078、#2066、#1999 均为同类迁移问题；
  官方给出的唯一可靠解法是**清掉本地数据库**让 app 重建。

### 用户侧修复步骤
1. **升级 hiddify 到最新版**（4.2+/最新 release；4.1.1 已知有此迁移 bug）。
2. 若仍报错，**清除 hiddify 本地数据库**（会丢失已存订阅，先备份）：
   - Windows：关闭 hiddify，删除 `%APPDATA%\hiddify\`（便携版为程序目录下 `hiddify_portable_data\`）后重启。
   - iOS：卸载重装 hiddify（沙盒数据只能通过卸载清除）。
   - Android：设置→应用→hiddify→清除数据，或卸载重装。
3. 重新添加订阅地址 `https://ad.tiktokplay.na.am/sub/...`。

---

## 八、iOS「connection refused errno=61 127.0.0.1:51867」（第三轮修复）

**根因**：sing-box 订阅模板自带 `inbounds`（mixed 监听 127.0.0.1:7890 + tun），
与 karing/hiddify 在 iOS 上自己管理的本地代理端口（如 127.0.0.1:51867）冲突——
客户端按自己的端口（51867）连接本地代理，但配置绑定的是 7890/由客户端接管 TUN，
导致连接被拒。订阅类 sing-box 配置本就不应下发 inbounds（监听端口/TUN 由客户端自己管理）。

**修复**：
1. 生产库 `subscribe_templates.singbox` 去掉 `inbounds` 与 `experimental(cache_file)`。
2. `packages/subscription/renderer/singbox.go`：不再下发 inbounds/experimental，
   `inbounds` 字段改 `omitempty`（提交 `57ba255`）。
3. 重新构建部署 subscription-service（vps190，旧版备份 `.bak.fix3_*`）。

**验证**：karing/hiddify 订阅现为 `dns/log/outbounds/route` 四段（无 inbounds），
21 节点，通过 sing-box 1.13 `check`；shadowrocket/v2rayN/throne 不受影响（URI 26 节点）。
