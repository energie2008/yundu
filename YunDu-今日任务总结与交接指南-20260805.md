# YunDu 机场 今日任务总结与交接指南（2026-08-05）

> 覆盖范围：订阅端 karing/hiddify 问题与修复、USDT-BEP20 设置与维护、
> Qiu-Pay 彩虹易支付（支付宝账单收款码）搭建问题与解决、避坑指南、接手人建议。
> 代码仓库：`D:\机场搭建\yundu-src`（main 分支，本日提交见文末清单）。

---

## 一、订阅端 karing / hiddify 问题与修复

### 1.1 现象（用户原始反馈）
- karing 只显示 11 个节点（实际 26 个），vless 节点全部不支持，
  显示 `unknow version 72`；只支持部分 trojan/hy/anytls 节点。
- hiddify 显示"配置失败"，iOS 端显示
  `connection refused errno=61 address 127.0.0.1, port=51867`。
- 订阅名称默认显示"云渡yundu"；到期时间解析为 `1970/1/1`。
- Windows karing 提示 `1.2.22.2502 window 更新失败 reloading`；
  iOS 提示 `download profile failed, http exception, connection terminated during handshake`。

### 1.2 根因与修复（按提交顺序）

| # | 根因 | 修复 | 提交 |
|---|------|------|------|
| 1 | sing-box 模板包含非法路由规则字段 `match`，sing-box 不识别该字段导致整个 JSON 加载失败（karing/hiddify 解析失败） | 移除 `match`，兜底改由 `route.final=漏网之鱼` 承担；补 `route.default_domain_resolver` 兼容 sing-box 1.12+ | `247e16b` |
| 2 | 管理端改套餐（AdminChangePlan）创建订阅时未写 `expires_at`，导致客户端显示 1970/1/1 | 按 `plan.duration_days` 设置 `expires_at` | `247e16b` |
| 3 | karing/hiddify 真实 UA 携带 mihomo/clash 兼容 token（如 `Karing/1.2.22.2502 platform/windows; mihomo/1.19.27;...;HiddifyNext`），原 switch 先命中 mihomo/clash，把 sing-box 内核客户端错误下发 Clash YAML → karing 导入后 vless 大量丢失只剩 11 个 | 将 karing/hiddify 检测提前到 clash/mihomo 之前，并新增 `detector_test.go` 锁定该行为 | `479bccb` |
| 4 | Profile-Title 硬编码 "YunDu"，订阅名显示不友好 | 改为显示订阅站点（SITE_NAME / SUB_BASE_URL host / 请求 Host），实际显示 `ad.tiktokplay.na.am` | `479bccb` |
| 5 | sing-box 订阅配置携带 `inbounds`(mixed:7890/tun) 与 `experimental(cache_file)`，与 karing/hiddify 自管本地代理端口（如 127.0.0.1:51867）冲突，iOS 连接拒绝 | 订阅类 sing-box 配置不再下发 inbounds/experimental，`inbounds` 改 `omitempty`；同步更新生产库 `subscribe_templates.singbox` | `57ba255` |

### 1.3 结论
- karing/hiddify 均为 sing-box 内核客户端，必须下发 **sing-box 格式** 订阅，
  且配置必须"纯净"（不能带 inbounds / 非法字段 / 本地端口）。
- 客户端 UA 检测顺序：**先识别 karing/hiddify，再识别 clash/mihomo**。

---

## 二、USDT-BEP20 设置与维护

### 2.1 当前生产配置（postgres `system_settings`，group=payment）

| 方式 | 状态 | 地址/合约 | 关键参数 |
|------|------|-----------|----------|
| USDT-ERC20（EVM 双通道） | 启用 | 收款地址统一 `0x92c08f0e24558858778b1E34910e4CE948A6CDFF`；networks=`["arbitrum","polygon"]` | Polygon USDT `0xc2132D05D31c914a87C6611C10748AEb04B58e8F`(chain 137, 6位小数)；Arbitrum One USDT `0xFd086bC7CD5C481DCC9C85ebE478A1C0b69FCbb9`(chain 42161, 6位小数)；min_confirmations=3；amount_tolerance=0.01；auto_activate=true |
| USDT-BEP20 | 启用 | 同上地址 `0x92c08f0e24558858778b1E34910e4CE948A6CDFF`；合约 `0x55d398326f99059fF775485246999027B3197955`(18位小数) | rpc_nodes：NodeReal `https://bsc-mainnet.nodereal.io/v1/64a9df0874fb4a93b9d0a3849de012d3` + `bsc-dataseed.binance.org` + `bsc-dataseed1.bnbchain.org`；min_conf=3 |
| USDT-TRC20 | 停用 | `TLAoiTwPNCtFXpJWmPrvgm8tRpC9ggP42H` | 未启用 |
| 汇率 | - | exchange_rate usdt_to_cny=7.2（auto_update=false） | - |

> 说明：EVM 系列（Polygon / Arbitrum One / BSC）收款地址可共用同一个地址
> （同一 EVM 私钥在所有 EVM 链上地址一致）。**ERC20 通道已下线 Ethereum 主网**
> （手续费过高），实际就是 Polygon + Arbitrum One 双通道 + BEP20 三选一展示。

### 2.2 关键修复点
- **地址 TrimSpace**：加载配置统一 `strings.TrimSpace(cfg.Address)`，修复
  地址前导空格导致链上交易匹配永不成功的问题（`b7c5ece`）。
- **BEP20 RPC**：默认加入可用 NodeReal 端点（支持按收款地址 topics 过滤查询
  Transfer 事件），后台可编辑 rpc_nodes。
- **支付方式可用性**：用户端支付方式接口只返回真实可用的方式：
  支付宝/微信需易支付已配置(pid+key+网关)，BEP20 需已配置地址。
- **结算提示**：USDT 各通道展示"请勿选错网络，USDT手续费极低真实可用，优惠码见系统公告"。

### 2.3 维护要点
- 链上到账自动激活：轮询 RPC 日志（Transfer 事件按收款地址过滤），
  确认数 >= min_confirmations 且金额匹配（容差 0.01）即激活订单；
  订单 6 小时过期。
- 换收款地址 = 后台支付配置改 address 即可（EVM 三链共用同一地址，
  任选一链收款都自动入账）。
- 排查到账：先看 identity-service 日志里 `EVM order paid` / `BEP20` 相关行，
  再看链上 tx hash 是否已认领（同一 tx 只认领同网络订单）。

---

## 三、搭建 Qiu-Pay 彩虹易支付（支付宝账单收款码）

### 3.1 架构
```
用户(支付宝/微信扫账单收款码)
   └─ YunDu user-web 结算页 → 易支付协议下单(epay mapi.php)
        └─ YunDu identity-service (epay_gateway.go)
             └─ https://100.tiktokplay.na.am/xpay/epay/  (nginx 8445 SSL 反代)
                  └─ qiu-pay 容器 (127.0.0.1:18000→8000, /opt/qiupay)
                       ├─ 记录订单 + 基准余额(alipay.data.bill.balance.query)
                       ├─ 余额轮询(1s, 60min 超时) 差值匹配订单(尾数+子集和)
                       └─ 支付成功 → 回调 YunDu notify_url → 激活订阅
```

### 3.2 部署信息（vps190）
- 镜像 `qiusheng26/qiu-pay:latest`，目录 `/opt/qiupay`，
  compose 端口 `127.0.0.1:18000:8000`，数据卷 `./data`、`./data/uploads`。
- **补丁挂载（重要）**：`./patches/order_service.py`、`./patches/payment_poller.py`
  以只读方式覆盖容器内文件（60 分钟过期等改动在这里，容器重建不丢失）。
- 域名 `https://100.tiktokplay.na.am`：BT 面板 nginx vhost（8445 ssl），
  acme.sh Let's Encrypt **DNS-01** 真实证书（CF token 覆盖 `tiktokplay.na.am`），
  自动续期。
- 后台：`https://100.tiktokplay.na.am/admin`，账号 `admin`，
  密码 `c34a1a22eb8961e4898dd6b6`（见 `/opt/qiupay/.env`，JWT_SECRET 同文件）。
- 商户：**pid=5（yund1，active=1）**，key=`1422cef754d26d7a5630b9bd53e40103`，
  凭证 verified；支付宝收款码 `https://qr.alipay.com/fkx16193dehudjlny5ktb51`。

### 3.3 YunDu 对接配置（system_settings）
- 支付宝：`gateway_url=https://100.tiktokplay.na.am/xpay/epay/`，
  pid=5，key=上面商户 key，pay_type=alipay，enabled=true，epay_configured=true。
- 微信：pid=1，pay_type=wxpay，同网关，当前 enabled=false（微信后续再启用）。
- 兼容：mapi.php 下单（JSON），submit.php 回退，`api.php?act=order&pid&key&out_trade_no`
  GET 查单（Qiu-Pay 风格）；notify_url/return_url 未配置时按
  `AGENT_PANEL_ENDPOINT` 自动补齐（修复支付成功无法回调激活）。

### 3.4 遇到的问题与解决
1. **支付成功但订单无响应/后台超时**：
   订单 `20260805044934070709076378`（外部 P202608051249311257，1.8 元）已收款
   但 qiu-pay 显示超时、回调未通知。
   - 根因：qiu-pay 原 10 分钟过期太短 + 余额轮询未及时匹配。
   - 处理：将该单手动标记 paid 并触发回调；YunDu 侧 `P202608051249311257`
     已 paid、订阅激活（66G，到期 2026-11-02，流量已重置）。
   - 根治：`payment_poller.py` / `order_service.py` 过期时间 10min → **60min**，
     补丁持久化到 `/opt/qiupay/patches/` 并挂载。
2. **submit.php 返回 HTML 无法解析**：易支付网关改走 `mapi.php`（JSON API），
   并兼容 code/msg/message/data/redirect 多种响应格式（`2418792`、`b7c5ece`）。
3. **支付宝扫码不弹金额（免输金额）**：
   - 调研结论：官方唯一方案 = 当面付 `alipay.trade.precreate` 返回固定金额
     `qr_code`，配合 `alipay.trade.query` 轮询。
   - 实测：签名已打通（关键：**charset 必须放在 URL query string**，
     响应可能是 GBK，需 `decode('utf-8', errors='ignore')` 容错），
     但返回 `isv.insufficient-isv-permissions` —— 商户 5 未签约当面付。
   - **当面付需要营业执照/企业资质，用户决定暂不启用**，维持现有
     "扫账单收款码 + 手输金额 + 余额轮询(60min)" 方案，够用即可。
4. **支付金额/币种显示错乱**：曾出现同单同时显示 8USDT 与 8RMB；
   订单金额被算成 15USDT（实为 2.08）。已修复为 CNY/USDT 分币种显示、
   按商品价格正确计算（`4797d91`）。
5. **二维码样式/交互**：`user-web/src/components/QRCode.tsx` 重写为本地
   `qrcode` 库生成经典白底黑格（errorCorrectionLevel H，中央支付方式 logo）；
   `qr.alipay.com` 收款码链接渲染为二维码供扫一扫；去掉误导的"去支付"按钮
   （浏览器打开是支付宝公告页 `render.alipay.com/...`）（`99764d1`、`64ea2ee`）。
6. **网页打不开**：100.tiktokplay.na.am 是支付网关，不是用户面板；
   面板在 6/7.tiktokplay.na.am，避免把它当节点或网站误处理。

---

## 四、避坑指南

1. **Windows scp 竞态（本次踩坑）**：scp 大文件（50MB+）时不要在中途对目标
   文件做 `mv`，否则会搬走"写了一半"的文件导致二进制损坏
   （`file` 报 `too large section header offset`、systemd 203/EXEC）。
   正确姿势：scp 到唯一临时名 → `stat -c %s` 比对字节数（或 md5）一致 → 再 mv。
2. **PowerShell 引号/CRLF**：多行 bash 脚本先写成 `.py` 生成 `.sh`（LF 换行）
   再 scp 执行，避免 `\"` 嵌套和 CRLF 破坏；docker exec 内嵌 python 也建议
   写成 .py 文件 `docker cp` 后执行。
3. **qiu-pay 补丁必须挂载**：容器内直接改文件在 `docker compose up` 重建后会丢，
   必须写进 `/opt/qiupay/patches/` 并通过 volumes 挂载（compose 已配好）。
4. **支付宝签名**：`charset` 参数要放 URL query string（`gateway.do?charset=utf-8`），
   签名串用原始值（不 URL 编码）；响应可能是 GBK，JSON 解析前做编码容错。
5. **当面付门槛**：`alipay.trade.precreate` 需签约当面付（营业执照/企业资质），
   个人支付宝只能用"收钱码 + 余额轮询"方案。
6. **karing/hiddify UA 陷阱**：UA 里带 mihomo/clash 兼容 token，检测顺序必须先
   karing/hiddify 再 clash/mihomo；sing-box 订阅严禁下发 inbounds/experimental。
7. **BEP20/链上检测**：地址必须 TrimSpace；Binance 公共 RPC 对日志查询不稳，
   优先 NodeReal（带收款地址 topics 过滤）；确认数/容差参数在后台可调。
8. **psql 查询**：容器内 `psql -f /tmp/x.sql`（文件先进容器），避免命令行
   引号地狱；Windows PowerShell 下 `\"` 会被拆坏。
9. **二进制升级**：替换前先 `cp -a` 留 .bak（带时间戳），`systemctl restart`
   后确认 `is-active` 与日志，再清 .bak。
10. **续费逻辑（今日新规）**：续费=完全按新套餐起算新周期，**不延长原到期时间**，
    流量重置；同套餐续费也按新购处理（替换旧订阅）。

---

## 五、接手人建议（交接清单）

### 5.1 先读这些文档（都在 `D:\机场搭建\yundu-src\`）
- `项目360度全方位调查分析.md`（项目全景）
- `YunDu-830项目分析报告-20260803.md`
- `USDT支付链路检查与中间件选型分析-20260802.md`
- `YunDu-qiu-pay彩虹易支付部署-20260805.md`（Qiu-Pay 详细部署）
- `YunDu-支付配置与BEP20可用性修复报告-20260805.md`
- `YunDu-订阅客户端兼容与到期时间修复报告-20260805.md`
- 本文件（今日总结 + 交接）

### 5.2 机器与密钥
| 机器 | 用途 | 登录 | 密钥 |
|------|------|------|------|
| vps190 43.135.147.190 | 面板 + 全部微服务 + Qiu-Pay + 数据库 | root | `D:\机场搭建\key\190key.pem` |
| vps206 158.101.13.206 | 节点（ubuntu） | root | `D:\机场搭建\key\206key` |
| vps81 132.226.156.81 | 节点（ubuntu） | root | `D:\机场搭建\key\81key` |

- 面板：admin-web `https://6.tiktokplay.na.am`，user-web `https://7.tiktokplay.na.am`
  （订阅 `https://ad.tiktokplay.na.am/sub/<token>`）。
- 数据库：`docker exec yundu-postgres psql -U app -d airport`
  （127.0.0.1:5433，密码见 /opt/yundu/config/.env）。

### 5.3 日常运维命令
```bash
# 服务状态
systemctl status yundu-{api-gateway,identity-service,node-service,subscription-service,traffic-service}
# 服务日志
tail -f /opt/yundu/logs/identity-service.log
# Qiu-Pay
docker logs -f qiu-pay
# 查支付配置（生产）
docker exec yundu-postgres psql -U app -d airport -c "SELECT setting_key,value_json FROM system_settings WHERE setting_group='payment';"
# 查 qiu-pay 订单
docker exec -w /app qiu-pay python -c "import sqlite3;db=sqlite3.connect('data/qiupay.db');print(db.execute('SELECT trade_no,status,callback_status,money FROM orders ORDER BY id DESC LIMIT 10').fetchall())"
```

### 5.4 发布流程（Go 服务）
1. 本地：`cd D:\机场搭建\yundu-src\apps\<service>`
   `$env:GOOS="linux"; $env:GOARCH="amd64"; $env:CGO_ENABLED="0"; go build -o D:\机场搭建\dist\<bin> ./cmd/api/`
2. 上传：scp 到 `/opt/yundu/bin/<bin>.new`（唯一临时名）
3. 校验：`stat -c '%s'` 与本地字节数一致，`file` 确认 ELF 正常
4. 替换：`cp -a <bin> <bin>.bak.<ts> && mv <bin>.new <bin> && chmod +x`
5. 重启：`systemctl restart yundu-<service> && systemctl is-active`，看日志
6. 前端（user-web）：`pnpm --filter user-web build` → 产物打包 → 部署 7.tiktokplay.na.am

### 5.5 支付核查与对账
- 用户"付了钱没到账"排查顺序：
  1. qiu-pay 后台/DB 订单状态（`status`：0 待付/1 已付/2 超时；`callback_status`）
  2. identity-service 日志 `EVM order paid` / `Payment poll` / `callback`
  3. 链上（BSC/Polygon/Arbitrum）tx 确认数与收款地址是否一致
  4. 若 qiu-pay 已收款但超时：手动置 paid + 触发回调（参考 3.4-1），
     并把过期时间保持 60min 以上
- 支付宝余额：qiu-pay 后台"余额检测"实时显示（当前约 5 元）；余额差值
  子集和匹配待支付订单，尾数机制防串单。

### 5.6 安全与保密
- `/opt/qiupay/.env`（admin 密码、JWT_SECRET）、CF token
  `[REDACTED - 已轮换]`、
  支付宝应用私钥、收款地址私钥 = **最高机密，切勿外泄/提交到 git**。
- 后台密码、DB 密码在 `/opt/yundu/config/.env`，改动后重启对应服务。
- git 提交前检查 `git status`，不要误提交密钥/证书。

### 5.7 遗留事项（下一步可做）
- 微信支付（pid=1 已建，enabled=false）：如启用需在 qiu-pay 配微信收款码/凭证，
  并在 YunDu 后台把 wechat.epay 开启。
- 免输金额（当面付）：需营业执照，用户暂缓；若将来开通，测试脚本
  `/opt/qiupay/patches/../qp_test_precreate_fixed.py`（容器内）已验证签名链路，
  可在此基础上改 qiu-pay 走 precreate。
- 本日代码提交见 `git log --oneline -14`（main，12+ 个功能/修复提交），
  确认无敏感信息后可 `git push origin main`。

---

## 附：本日提交清单（main）
```
b56cd4d fix(payment): 续费订阅按新周期起算，不延长原到期时间
64ea2ee feat(user-web): 二维码本地生成经典白底样式 + 中央支付方式logo
99764d1 fix(user-web): 支付宝/微信收款码按二维码展示并去掉误导的去支付
4797d91 fix(payment): 续费重置流量 + 订单详情金额按币种显示
4fb49ff fix(user-web): 支付宝/微信订单不再重复显示USDT支付卡
0491060 feat(payment): 兼容 Qiu-Pay 查单(GET)与收款码图片展示
531116c feat(payment): 易支付接口路径可配置 + mapi失败自动回退submit解析跳转
31f0388 fix(user-web): 结算下单失败显示错误提示（修复点确认支付无响应）
2418792 fix(payment): 易支付错误透传友好提示 + 非JSON响应兼容
b7c5ece fix(payment): 支付方式可用性/易支付网关/BEP20 RPC/结算提示
57ba255 fix(subscription): sing-box 订阅不再下发 inbounds，修复 iOS 本地代理端口冲突
479bccb fix(subscription): karing/hiddify 真实 UA 被误判为 Clash Meta + 订阅名称显示站点
247e16b fix(subscription): 修复 karing/hiddify 订阅解析失败与到期时间 1970/1/1
```

---

## 附 2：2026-08-06 追加 —— ad 订阅域名无法更新故障修复记录

### 故障现象
- 所有客户端通过 `https://ad.tiktokplay.na.am/sub/<token>` 更新订阅失败，
  提示"无更新服务器 / 更新失败"。

### 根因（两层）
1. **nginx 缺少 `ad.tiktokplay.na.am` 的 vhost**（vhost 目录与 wwwroot 均无历史文件，
   推测在历次前端部署/面板操作中被误删）。请求落到 default server
   （`server_name _`）→ 反代到 node-service(8082) → 返回**前端 HTML**，
   客户端无法解析 → 显示"无更新服务器"。
2. 证书目录也无 `ad.tiktokplay.na.am`，即使有 vhost 也没有匹配证书。

### 修复动作
1. 用 acme.sh + CF DNS-01 为 `ad.tiktokplay.na.am` 签发 Let's Encrypt 正式证书
   （EC-256，有效期至 2026-11-04），安装到
   `/www/server/panel/vhost/cert/ad.tiktokplay.na.am/`，并补 `Le_ReloadCmd`
   （续期后自动 `nginx -s reload`，与 100 域名一致；acme cron 每天 1:38 检查）。
2. 新建 `/www/server/panel/vhost/nginx/ad.tiktokplay.na.am.conf`：
   - `listen 80` + `listen 8445 ssl http2`（与 6/7 一致，配合 443 SNI 分流）
   - `location ~ ^/(sub|s)/` → `proxy_pass http://127.0.0.1:8083`（subscription-service）
   - 其它路径 302 跳转到 `https://7.tiktokplay.na.am/`（ad 专注订阅，避免成为第二个面板入口）
   - `nginx -t` 通过并 reload。

### 验证
- `https://ad.tiktokplay.na.am/sub/<token>` 各 UA 均返回 200 正确格式：
  karing/hiddify/sing-box → sing-box JSON；clash/stash → Clash YAML；
  shadowrocket/v2rayNG → URI 列表。
- 响应头 `Cache-Control: private, no-store`（CF 不缓存）、
  `Profile-Title: ad.tiktokplay.na.am`、`Subscription-Userinfo` 含正确到期时间。
- subscription-service 本身无需改动（源码/二进制均正常）。

### 经验
- 排查"订阅更新失败"先分三层：DNS/CF 入口 → nginx vhost → subscription-service
  源站。本机 curl 拿到的若是 HTML，多半是 nginx 把 /sub/ 路由到了前端而非 8083。
- 测试源站时注意 shell 变量展开（PowerShell 会吞掉 `$TOKEN` 导致请求 `/sub/` 404，
  应写成 .sh 文件上传执行）。

---

## 附 3：2026-08-06 追加 —— 域名证书矩阵 / tiktokplay.na.am 指向 / 零 SSH 架构说明

### 3.1 各域名证书情况（vps190）

| 域名 | 用途 | 源站证书（nginx 8445） | 签发方式 | 到期 | 边缘证书（CF） |
|------|------|----------------------|----------|------|---------------|
| 6.tiktokplay.na.am | admin-web | 自签名（RSA） | BT 默认自签 | - | CF Universal SSL（GTS） |
| 7.tiktokplay.na.am | user-web | Let's Encrypt ECC | acme.sh DNS-01 | 8-05 起 90 天 | CF Universal SSL |
| 100.tiktokplay.na.am | Qiu-Pay 支付 | Let's Encrypt ECC | acme.sh DNS-01 | 8-05 起 90 天 | CF Universal SSL |
| tiktokplay.na.am | 独立 Typecho 站 | Let's Encrypt RSA | BT 面板 HTTP 验证 | 10-07（自动续期） | CF Universal SSL |
| ad.tiktokplay.na.am | 订阅 | Let's Encrypt ECC | acme.sh DNS-01 | 11-04（acme cron 自动续期+reload） | CF Universal SSL（GTS，subject=tiktokplay.na.am 通配覆盖） |

> **为什么 ad"之前有证书、现在又需要证书"**：两层证书概念。
> - 边缘证书：所有 tiktokplay 子域都走 CF 橙云，CF Universal SSL 一直覆盖
>   ad.tiktokplay.na.am（实测 issuer=Google Trust Services WE1，subject=tiktokplay.na.am），
>   浏览器访问 https://ad... 一直有锁头 —— 这就是"之前有证书"。
> - 源站证书：CF→源站回源（443 SNI→8445）需要 vps190 nginx 上 ad 自己的证书。
>   之前 ad 没有 nginx vhost（请求落 default server），证书目录里无 ad；
>   补 vhost 后 8445 的 ad server 需要源站证书 → 签了 Let's Encrypt（YE2，CN=ad.tiktokplay.na.am）。
> - 结论：边缘证书 ≠ 源站证书，二者不冲突；"又需要"的是源站证书。

### 3.2 tiktokplay.na.am 指向问题（重要）

- CF DNS：A → 43.135.147.190（vps190）proxied=True —— 解析正确。
- BT 面板：tiktokplay.na.am 是**独立 PHP 站点**（id=2，2026-03-13 创建），
  root=/www/wwwroot/tiktokplay.na.am，nginx vhost 为标准 PHP 配置（enable-php-74.conf）。
- 关联 MySQL 库 `sql_tiktokplay_na_am`（账号 sql_tiktokplay_na_am / tiktok6668888）——
  **Typecho + Handsome 主题数据完整**：typecho_contents=18、typecho_users=3、
  typecho_options（siteUrl=https://tiktokplay.na.am、theme=handsome、rewrite=0）。
- **问题**：Typecho **程序文件被 yundu 前端覆盖丢失**（wwwroot 里只剩
  Airport Panel 的 index.html+assets 和 .well-known/acme-challenge，index.php 不存在；
  时间戳 7-09，应是某次前端部署误写入了该目录）。
- 现状访问 https://tiktokplay.na.am 显示 yundu 前端（Airport Panel），不是 Typecho。
- **恢复方式**：Typecho 程序包 + Handsome 主题解压到
  /www/wwwroot/tiktokplay.na.am/，写 config.inc.php 连现有库（数据自动回来）。
  /www/backup/site/ 为空，无整站备份；Handsome 为付费主题需用户提供程序包。

### 3.3 零 SSH 架构（agent 是否覆盖手动改动）

- 面板机 vps190：运行 node-service（面板侧）+ BT 面板，**未运行 node-agent**。
  BT 面板的 nginx vhost（/www/server/panel/vhost/nginx/*.conf，含手动新建的 ad）
  完全由 BT 面板管理。
- 节点机 vps206/vps81：运行 node-agent，管理节点侧 BT nginx 的 yundu snippet
  （yundu-stream-snippet.conf / yundu-https-snippet.conf）与节点证书
  （/www/server/panel/vhost/cert/<domain>/，acme 自动续期），**不管理面板机 vhost**。
- node-service 的证书管理走 tls_certificates 表（存 DB）+ 部署到节点，
  **不写面板 BT vhost/证书目录**。
- 结论：手动建的 ad vhost + 手签的 ad 证书**不会被自动更新覆盖**。
  注意点：① 若未来在 BT 面板把 ad 加为"网站"并操作，可能重写 vhost；
  ② 若把 node-agent 部署到 vps190 并开启面板 nginx 接管，才可能冲突；目前无此配置。

---

## 附 4：2026-08-06 追加 —— 注册"rate limit exceeded"与注册 500 故障修复

### 故障现象
- 用户注册新号：第一次提交显示"服务错误"，第二次起显示"注册失败 rate limit exceeded"。
- 用户质疑"才申请验证码 2 次，为何计数 11 次"。

### 调查结论
1. **计数 11 次是真实的 register 请求数**（非申请验证码次数）：
   api-gateway 日志显示同一 IP(163.123.203.113) 在 19:04~19:07 提交了 11 次
   `POST /api/v1/user/auth/register`：第 1 次 500、第 2/3 次 400、之后连续 8 次 429。
   前端 onSubmit 为单次提交（无自动重试），11 次 = 用户因反复失败而多次点击提交。
2. **第一次 500 根因**：用户邮箱 `energie2008@gmail.com` 在 users 表存在但**被软删除**
   （status=banned, deleted_at=2026-07-14）。`GetByEmail` 只查 `deleted_at IS NULL`
   → 查不到 → 认为可注册 → 发验证码成功 → 验证码校验通过（**一次性，校验即删除**）
   → `users.Create` 撞 **UNIQUE(email) 约束（软删除行仍占用）** → DB 错误未映射 → **500**。
   随后再提交因验证码已被删除 → 400；反复提交触发 api-gateway 注册限流（3 次/小时）→ 429。
3. **限流阈值过严**：注册 3 次/小时/IP 对真实用户太苛刻，且 api-gateway 与
   identity-service 各有一层（都 3 次/小时）。

### 修复
1. **api-gateway** `rate_limit.go`：注册限流 `registerLimit 3 → 10`（每 IP 每小时 10 次）。
2. **identity-service**：
   - `user_repo.go` 新增 `GetByEmailAny`（含软删除、大小写不敏感 `LOWER(email)`）。
   - `user_service.go` Register / SendEmailCode 改用 `GetByEmailAny`：
     邮箱已存在（含软删除）→ 明确返回 409 "user already exists"，
     不再走 Create 撞唯一约束返回 500，也不对已占用邮箱"假发码"。
3. 清理 Redis 中被限流的 key（`rl:register:*`、`register_ip:*`）。
4. 按《编译推送与安装升级维护教程》部署：备份 → 替换 → chmod +x → restart → is-active。

### 验证
- 软删除邮箱注册 → 409 "user already exists"（原 500）✅
- 软删除邮箱申请验证码 → 409（不再假发送）✅
- 新邮箱注册 → 201 成功（含订阅 token）✅

### 运维提示
- 该邮箱如需重新使用，需管理员处理原账号（恢复 deleted_at 或改邮箱释放唯一约束）。
- 正常注册请使用全新邮箱；已注册邮箱会明确提示"user already exists"。

---

## 附 5：2026-08-06 追加 —— 优惠券"只能用一张/再用报错"修复

### 故障现象
- 管理员设置多张优惠券（如 ZEVKMKE8：每人限用 5 次 limit_use_by_user=5，
  可用套餐 66G/156G），但用户用一次后再次使用提示错误。

### 根因
- `coupon_service.go` 与 `payment_service.go` 两处校验都写了硬编码逻辑：
  ```go
  if !coupon.IsRepeatable && coupon.UsedCount > 0 {
      return ErrCouponNotRepeatable   // "一次性券：全局已用过即拒绝"
  }
  ```
  即只要**整张券**被任意用户用过 1 次（used_count>0），所有用户都不可再用——
  完全覆盖了管理员设置的 `limit_use_by_user=5`（每人 5 次），导致"只能用一张"。

### 修复
- 两处校验统一改为**按"每用户"限制**：
  - `limit_use_by_user > 0`：按该值限制（每人 N 次）
  - `limit_use_by_user = 0` 且 `is_repeatable = false`：每人限用 1 次
  - 不再按全局 used_count 拒绝（used_count 仍用于 max_uses 全局上限）
- 已排查其它"硬编码优惠码"：admin handler、前端 Checkout、coupon repo 均无
  硬编码优惠码，唯一硬编码即上述全局一次性判断。

### 部署与验证
- 按《编译推送与安装升级维护教程》部署 identity-service（active）。
- 实测：新用户连续两次校验 ZEVKMKE8 → 均返回 valid=true、discount=6.20
  （limit_use_by_user=5 生效）。

---

## 附 6：2026-08-07 追加 —— 新建套餐 500 + 营收把优惠券算进去 修复

### 1. admin-web 新建套餐 internal error
- 现象：新建套餐 POST /api/v1/admin/plans 返回 500（latency 2-6ms 快速失败）。
- 根因：`plan_repo.go` Create/Update 把 `feature_flags`（[]byte）直接传给
  `plans.feature_flags`（jsonb 列）。pgx 对 []byte 默认按 bytea 编码，
  jsonb 列不接受 bytea → 类型不匹配 → DB 报错 → 未映射 → 500。
- 修复：`plan_repo.go` Create/Update 将 feature_flags 转 `string` 传入
  （text 可隐式 cast 为 jsonb）。
- 验证：admin token 调 POST /api/v1/admin/plans → 201 成功。

### 2. 仪表盘营收把优惠券折扣算进去了
- 现象：当日营收显示 20 元，但该订单用 100% 优惠券实际收入 0 元；
  当月营收同样把优惠券折扣计入。
- 根因：`payment_order_repo.go GetRevenueStats` 的 SQL 里
  CNY 营收用 `amount_cny`（折扣前原始金额），未用实付金额。
- 修复：按币种使用 `final_amount`（实付，已扣优惠券折扣，等价 xboard 的
  order_amount）：
  - CNY 营收 = 法币订单(alipay/wechat) final_amount 之和
  - USDT 营收 = 加密订单(pay_currency LIKE 'USDT%') final_amount 之和
  - 0 元订单（100% 券，final_amount=0）不计入营收但计入订单数
- 验证：GET /api/v1/admin/orders/stats →
  today_revenue_cny=0（原 20）、today_order_count=1、month_revenue_cny=5。

> 追加修复（同日晚间）：GetRevenueStats 新 SQL 的 SELECT 列顺序与
> RevenueStats 结构体 Scan 顺序错位（today_cny 放第 1 列但结构体首字段是
> TodayRevenueUSDT），导致 USDT/CNY 营收对调显示（1.8 显示在 USDT 列）。
> 已调整 SQL 列为 today_usdt, today_cny, today_count, month_usdt, month_cny, month_count。
> 验证：month_revenue_usdt=5（真 USDT 订单）、month_revenue_cny=1.8（法币订单）。

---

## 附 7：2026-08-07 追加 —— 测试 USDT 订单处理 + 新建套餐 500 真正根因

### 1. 测试订单处理
- 8-02 的 3 笔测试 USDT-Arbitrum One 订单（P202608020642395362 / ...44042976 /
  ...44419618，合计 5.0 USDT）已标记 status=canceled。
- 验证：GET /api/v1/admin/orders/stats → month_revenue_usdt=0、month_revenue_cny=1.8。

### 2. 新建套餐 500（真正根因）
- 上一轮修的 jsonb 编码只是其中一处；用户仍 500 的根因是：
  - 前端 `slugify` 对纯中文套餐名（如"免费套餐"）把所有非 ASCII 字符替换后
    为空，**回退固定 code='plan'**；
  - `plans.code` 唯一约束**包含软删行**（库里有 9 条软删套餐，含 code=plan、
    free 等），而 `GetByCode` 只查 `deleted_at IS NULL` → 查不到 →
    INSERT 撞 UNIQUE(code) → 500（与注册邮箱软删 bug 同类）。
- 修复：
  - 后端 `plan_repo.GetByCodeAny`（含软删）+ `PlanService.Create` 改用 → 409
    "plan code already exists" 明确提示；
  - 前端 slugify 空回退改为 `plan-<时间戳>` 唯一 code，避免中文名套餐都撞 plan。
- 验证：code=plan/free（软删）→ 409；新 code → 201。
- 已部署 identity-service + admin-web（6.tiktokplay.na.am），提交 c640872。

---

## 附 8：2026-08-07 追加 —— 套餐 code 软删释放（解决 "plan code already exists" 无法重建）

- 现象：删除过的套餐（软删记录仍占用 code 唯一约束）导致同名套餐无法重建，
  报 "plan code already exists"（此前为 500）。
- 修复：
  - `PlanRepo.Delete` 软删时把 code 追加 `:del:` 后缀释放原 code；
  - 一次性把既有 9 条软删记录 code 改为 `xxx:del:<id前8>`（含 plan/free/basic/pro 等）。
- 验证：重建 code=plan / free → 201；删除后 code=plan:del:xxx。
- 部署 identity-service（reldel），提交 9c69317。
