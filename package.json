{
  "name": "airport-panel",
  "private": true,
  "version": "0.1.0",
  "description": "新一代机场管理系统 - 三平面架构",
  "scripts": {
    "dev:admin": "pnpm --filter admin-web dev",
    "dev:user": "pnpm --filter user-web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
