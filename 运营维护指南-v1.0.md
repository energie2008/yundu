# 云渡（YunDu）机场系统 — 完整运营维护指南

> **版本**: v1.0  
> **日期**: 2026-08-07  
> **代码基线**: `main`（v0.7.21+）  
> **验证方式**: 本文档所有命令、路径、端口均经过 **3台VPS实际验证**，以运行代码为唯一验收标准  
> **目标**: 项目零SSH运营；运营遇到问题时根据本指南人工维护

---

## 目录

1. [系统架构总览](#1-系统架构总览)
2. [基础设施清单](#2-基础设施清单)
3. [服务端口矩阵](#3-服务端口矩阵)
4. [日常运营操作（零SSH）](#4-日常运营操作零ssh)
5. [面板运维操作](#5-面板运维操作)
6. [SSH应急操作](#6-ssh应急操作)
7. [故障排查手册](#7-故障排查手册)
8. [备份与恢复](#8-备份与恢复)
9. [安全加固](#9-安全加固)
10. [监控与告警](#10-监控与告警)
11. [常见问题速查](#11-常见问题速查)

---

## 1. 系统架构总览

### 1.1 三平面架构

```
                        ┌──────────────────────────────────────────────┐
                        │              用户浏览器 / 客户端               │
                        └──────────┬──────────────────┬────────────────┘
                                   │                  │
                   ┌───────────────▼──────┐  ┌────────▼───────────────┐
                   │  admin-web (SPA)     │  │  user-web (SPA)        │
                   │  6.tiktokplay.na.am  │  │  7.tiktokplay.na.am    │
                   │  管理面板             │  │  用户面板               │
                   └───────────┬──────────┘  └────────┬───────────────┘
                               └──────────┬───────────┘
                                   ┌──────▼───────┐
                                   │ api-gateway  │  (8090, REST + gRPC)
                                   └──────┬───────┘
        ┌──────────────┬──────────────┬───┴──────────┬───────────────┐
   ┌────▼────┐   ┌─────▼────┐   ┌────▼─────┐   ┌────▼──────────┐
   │identity │   │  node    │   │subscription│ │  traffic     │
   │ (8081)  │   │ (8082)   │   │  (8083)   │ │  (8084)      │
   └─────────┘   └────┬─────┘   └────────────┘ └──────────────┘
                      │ WS/HTTP 通道（AgentChannel）
        ┌─────────────┼─────────────────┐
   ┌────▼────┐   ┌────▼────┐   ┌───────▼───┐
   │ VPS206  │   │  VPS81  │   │  VPS190   │
   │ node-   │   │ node-   │   │ (控制面板) │
   │ agent   │   │ agent   │   │           │
   │(双核)   │   │(双核)   │   │           │
   └─────────┘   └─────────┘   └───────────┘
```

### 1.2 核心组件说明

| 组件 | 语言 | 功能 | 部署位置 |
|------|------|------|---------|
| api-gateway | Go | 统一入口、认证、路由、限流 | VPS190 |
| identity-service | Go | 用户/管理员/RBAC/JWT/套餐/订单 | VPS190 |
| node-service | Go | 节点管理/配置渲染/证书下发/Agent通信 | VPS190 |
| subscription-service | Go | 订阅渲染/客户端兼容/负载均衡 | VPS190 |
| traffic-service | Go | 流量统计/额度控制/在线会话 | VPS190 |
| node-agent | Go | 双内核运行时/心跳/配置应用/流量上报 | VPS206/VPS81 |
| admin-web | React | 管理后台 | VPS190 (nginx静态) |
| user-web | React | 用户面板 | VPS190 (nginx静态) |
| PostgreSQL 16 | Docker | 主数据库 | VPS190 |
| Redis 7 | Docker | 热数据/缓存/会话 | VPS190 |
| NATS 2 | Docker | 消息队列（可选） | VPS190 |
| nginx | 系统包 | 反向代理/SNI分流/静态文件 | 全部VPS |

### 1.3 双内核架构（节点端）

```
用户 → nginx:443 (ssl_preread读取SNI, 不终止TLS)
         │
         ├─ SNI=node1.domain → 127.0.0.1:30010 (sing-box 主内核)
         ├─ SNI=node2.domain → 127.0.0.1:30011 (sing-box)
         └─ SNI=node3.domain → 127.0.0.1:30003 (xray 辅内核 XHTTP)
```

- **主内核**: sing-box v1.13.14（常驻，处理 AnyTLS/VLESS WS/Hysteria2/TUIC）
- **辅内核**: xray-core（仅 XHTTP/REALITY 节点时懒加载）
- **通信**: WS 通道（端口10000控制面 + API `/api/v1/agent/`）

---

## 2. 基础设施清单

### 2.1 VPS 清单（已验证 2026-08-07）

| VPS | IP | 系统 | 架构 | RAM | 磁盘 | 角色 | SSH用户 | 密钥 |
|-----|-----|------|------|-----|------|------|---------|------|
| VPS190 | 43.135.147.190 | Debian 12 | x86_64 | 1.9G | 40G (91%⚠️) | 控制面板 | root | 190key.pem |
| VPS206 | 158.101.13.206 | Ubuntu 22.04 | x86_64 | 952M | 45G (46%) | 节点(双核) | ubuntu | 206.key |
| VPS81 | 132.226.156.81 | Ubuntu 22.04 | x86_64 | 952M | 45G (52%) | 节点(双核) | ubuntu | 81.key |

### 2.2 域名清单

| 域名 | 用途 | 指向 | SSL |
|------|------|------|-----|
| 6.tiktokplay.na.am | 管理面板 | VPS190 (CF Proxy) | Let's Encrypt |
| 7.tiktokplay.na.am | 用户面板 | VPS190 (CF Proxy) | Let's Encrypt |
| ad.tiktokplay.na.am | 订阅域名 | VPS190 (CF Proxy) | Let's Encrypt |
| 100.tiktokplay.na.am | 彩虹易支付 | VPS190 (CF Proxy) | Let's Encrypt |
| tiktokplay.na.am | 主站/博客 | VPS190 (CF Proxy) | Let's Encrypt |
| *.dannelblog.na.am | 节点SNI域名 | VPS206/VPS81 | 自签/不需要 |

### 2.3 Docker 容器（VPS190）

| 容器名 | 端口映射 | 用途 | 状态 |
|--------|---------|------|------|
| yundu-postgres | 127.0.0.1:5433→5432 | PostgreSQL 16 | ✅ healthy |
| yundu-redis | 127.0.0.1:6380→6379 | Redis 7 | ✅ healthy |
| yundu-nats | 127.0.0.1:4223→4222 | NATS 2 | ✅ healthy |
| yundu-prometheus | 127.0.0.1:9090 | Prometheus | ✅ running |
| yundu-grafana | 127.0.0.1:3000 | Grafana | ✅ running |
| yundu-loki | 127.0.0.1:3100 | Loki | ✅ running |
| yundu-otel | 127.0.0.1:4317-4318 | OTel Collector | ✅ running |
| yundu-promtail | - | Promtail | ✅ running |
| qiu-pay | 127.0.0.1:18000→8000 | 彩虹易支付 | ✅ running |
| nanoroute | 0.0.0.0:30128 | NanoRoute代理 | ✅ healthy |
| warp_socks | 0.0.0.0:9091 | WARP SOCKS5 | ✅ running |
| xboard-mariadb | - | XBoard遗留DB | ✅ running |

### 2.4 systemd 服务（VPS190）

| 服务名 | 端口 | 二进制 | 状态 |
|--------|------|--------|------|
| yundu-api-gateway | 8090 | /opt/yundu/bin/api-gateway | ✅ active |
| yundu-identity-service | 8081 | /opt/yundu/bin/identity-service | ✅ active |
| yundu-node-service | 8082 | /opt/yundu/bin/node-service | ✅ active |
| yundu-subscription-service | 8083 | /opt/yundu/bin/subscription-service | ✅ active |
| yundu-traffic-service | 8084 | /opt/yundu/bin/traffic-service | ✅ active |

### 2.5 systemd 服务（VPS206/VPS81 节点）

| 服务名 | 端口 | 二进制 | 状态 |
|--------|------|--------|------|
| yundu-node-agent | 10000 | /usr/local/bin/node-agent | ✅ active |

---

## 3. 服务端口矩阵

### 3.1 VPS190 端口

| 端口 | 服务 | 暴露范围 | 用途 |
|------|------|---------|------|
| 80 | nginx | 公网 | HTTP入口/ACME验证 |
| 443 | nginx | 公网 | HTTPS入口(CF Proxy) |
| 888 | nginx | 本地 | phpMyAdmin |
| 8080 | api-gateway | 内部 | REST API |
| 8081 | identity-service | 内部/DMZ | 身份服务 |
| 8082 | node-service | 内部/DMZ | 节点服务 |
| 8083 | subscription-service | 内部 | 订阅服务 |
| 8084 | traffic-service | 内部 | 流量服务 |
| 8090 | api-gateway | 内部 | API网关主入口 |
| 5433 | postgres | 127.0.0.1 | 数据库 |
| 6380 | redis | 127.0.0.1 | 缓存 |
| 4223 | nats | 127.0.0.1 | 消息队列 |
| 9090 | prometheus | 127.0.0.1 | 监控 |
| 3000 | grafana | 127.0.0.1 | 仪表盘 |
| 3100 | loki | 127.0.0.1 | 日志 |
| 4317 | otel | 127.0.0.1 | OpenTelemetry |
| 18000 | qiu-pay | 127.0.0.1 | 易支付 |
| 30128 | nanoroute | 公网 | 代理出口 |
| 9091 | warp_socks | 公网 | WARP SOCKS5 |

### 3.2 VPS206/VPS81 节点端口

| 端口 | 服务 | 用途 |
|------|------|------|
| 443 | nginx stream | SNI分流入口 |
| 80 | nginx | HTTP/ACME验证 |
| 8445 | nginx | HTTPS回源 |
| 10000 | node-agent | 控制面API |
| 10085 | xray API | 流量统计 |
| 30000-30200 | sing-box/xray | 直连TCP/REALITY节点 |
| 40020-40200 | sing-box | Hysteria2 UDP |
| 40210-40299 | sing-box | TUIC UDP |
| 20530-20699 | sing-box | Tunnel回源 |

---

## 4. 日常运营操作（零SSH）

### 4.1 通过管理面板操作

管理面板地址: `https://6.tiktokplay.na.am`

#### 4.1.1 节点管理
- **查看节点状态**: 面板 → 节点列表 → 查看在线/离线状态、流量统计
- **新增节点**: 面板 → 节点管理 → 添加节点 → 填写协议/传输/安全配置
- **修改节点配置**: 面板 → 节点列表 → 编辑 → 修改后点击"同步"
- **删除节点**: 面板 → 节点列表 → 删除

#### 4.1.2 用户管理
- **查看用户**: 面板 → 用户列表
- **封禁/解封用户**: 面板 → 用户列表 → 操作
- **重置用户流量**: 面板 → 用户详情 → 重置流量
- **查看在线用户**: 面板 → 监控 → 在线用户

#### 4.1.3 套餐管理
- **新建套餐**: 面板 → 套餐管理 → 添加套餐
  - ⚠️ 注意: 纯中文套餐名会自动生成唯一code，避免唯一约束冲突
  - 删除过的套餐code会追加 `:del:` 后缀释放原code
- **修改套餐**: 面板 → 套餐列表 → 编辑
- **删除套餐**: 软删，code保留

#### 4.1.4 订单与支付
- **查看订单**: 面板 → 订单列表
- **手动激活订单**: 面板 → 订单详情 → 确认激活
- **支付设置**: 面板 → 系统设置 → 支付配置

#### 4.1.5 订阅管理
- **订阅模板**: 面板 → 订阅模板管理
- **客户端兼容**: 面板 → 客户端兼容设置

### 4.2 通过 Agent 自升级（零SSH）

Agent 升级不需要 SSH，通过面板操作：

1. 在 VPS190 上准备新版本二进制:
   - 二进制位置: `/opt/yundu/agent-upgrade/`
   - 下载端点: `/api/v1/agent/download/`

2. 在管理面板中:
   - 进入 系统设置 → Agent版本管理
   - 填写 version / download_url / sha256
   - 发布后全部节点自动升级

3. 验证:
   - 面板 → 节点列表 → 查看 Agent 版本号
   - 心跳日志: `journalctl -u yundu-node-agent | grep agent_version`

### 4.3 配置推送（零SSH）

所有配置变更通过面板推送：

1. 修改节点配置 → 点击"同步"
2. node-service 自动生成 config_version
3. 通过 WS 通道推送到 node-agent
4. agent 自动执行 HotDiff / Reload / Restart

### 4.4 证书管理（零SSH）

1. **自签证书**: 面板 → 证书管理 → 自签 → 选择域名
2. **ACME证书**: 面板 → 证书管理 → ACME申请 → 选择DNS-01/HTTP-01
3. **四级回退**: cert_bundle_id → cert_bundles SAN → tls_certificates SNI → 自签兜底
4. **自动续期**: 15天自动续期，atomic.Pointer零中断热替换

---

## 5. 面板运维操作

### 5.1 Grafana 监控

地址: `https://6.tiktokplay.na.am/grafana/`

默认账号: `admin / admin` (生产环境请修改)

可用仪表盘:
- **Airport Overview**: 业务总览
- **Airport Business**: 营收/订单统计

### 5.2 Prometheus

地址: `http://127.0.0.1:9090` (VPS190本地)

关键指标:
- `config_render_hash_churn`: 配置渲染抖动
- `node_agent_heartbeat_total`: Agent心跳计数
- `node_agent_traffic_bytes`: 节点流量

### 5.3 Loki 日志

地址: `http://127.0.0.1:3100` (VPS190本地)

在 Grafana 中切换到 Loki 数据源可查询:
- 各微服务日志
- Agent 心跳日志
- nginx 访问日志

### 5.4 数据库操作

如需直接操作数据库（通过SSH）:

```bash
# 连接数据库
docker exec -it yundu-postgres psql -U app -d airport

# 常用查询
SELECT count(*) FROM users WHERE created_at > now() - interval '24 hours';
SELECT * FROM nodes ORDER BY created_at DESC LIMIT 10;
SELECT sum(download_bytes + upload_bytes) FROM traffic_logs WHERE created_at > now() - interval '24 hours';
```

---

## 6. SSH应急操作

### 6.1 SSH 连接方式

```bash
# VPS190 (控制面板)
ssh -i "D:\机场搭建\key\190key.pem" root@43.135.147.190

# VPS206 (节点) - 注意: 必须用 -F /dev/null 绕过旧config
ssh -i "D:\机场搭建\key\206.key" -F /dev/null -o IdentitiesOnly=yes ubuntu@158.101.13.206

# VPS81 (节点)
ssh -i "D:\机场搭建\key\81.key" -F /dev/null -o IdentitiesOnly=yes ubuntu@132.226.156.81
```

### 6.2 服务管理

```bash
# VPS190 微服务
systemctl status|restart|stop|start yundu-api-gateway
systemctl status|restart|stop|start yundu-identity-service
systemctl status|restart|stop|start yundu-node-service
systemctl status|restart|stop|start yundu-subscription-service
systemctl status|restart|stop|start yundu-traffic-service

# VPS206/VPS81 Agent
systemctl status|restart|stop|start yundu-node-agent

# Docker 容器
docker ps
docker restart yundu-postgres|yundu-redis|yundu-nats
docker logs -f yundu-postgres|yundu-redis

# nginx
nginx -t                    # 测试配置
nginx -s reload             # 热重载
systemctl restart nginx     # 重启
```

### 6.3 查看日志

```bash
# 微服务日志
journalctl -u yundu-node-service -f --since "10 min ago"
journalctl -u yundu-identity-service -f --since "10 min ago"

# Agent 日志
journalctl -u yundu-node-agent -f --since "10 min ago"

# nginx 日志
tail -f /www/wwwlogs/6.tiktokplay.na.am.error.log
tail -f /var/log/nginx/error.log

# 内核日志 (xray/sing-box)
tail -f /var/log/xray-yundu/xray-start.log
```

### 6.4 磁盘清理（紧急⚠️）

**当前 VPS190 磁盘 91% 满，需紧急清理:**

```bash
# 1. 清理 /tmp 旧二进制 (可释放 ~2.7GB)
rm -f /tmp/node-agent-*.bin /tmp/node-agent-* /tmp/node-service-*

# 2.清理 /opt/yundu/bin 备份文件 (可释放 ~3.5GB)
cd /opt/yundu/bin
rm -f node-service.bak.* node-service-old node-service-new* node-service-fixed
rm -f node-service_pre-p5-backup node-service.session2-cfgsync
rm -f node-service_v027 node-service_v029_linux node-service_v030
rm -f identity-service.bak.* identity-service.bak.couponfix.
rm -f subscription-service.bak.* traffic-service.bak.* api-gateway.bak.*

# 3. 清理 journal 日志 (可释放 ~1GB)
journalctl --vacuum-time=3d
journalctl --vacuum-size=500M

# 4. 清理 Docker 未使用资源
docker system prune -af --volumes

# 5. 验证
df -h /
```

### 6.5 服务重启顺序

```bash
# 正确重启顺序（VPS190）
docker restart yundu-postgres yundu-redis yundu-nats
sleep 3
systemctl restart yundu-identity-service
systemctl restart yundu-node-service
systemctl restart yundu-subscription-service
systemctl restart yundu-traffic-service
systemctl restart yundu-api-gateway
nginx -s reload
```

### 6.6 服务回滚

```bash
# 回滚 node-service
systemctl stop yundu-node-service
cp /opt/yundu/bin/node-service /opt/yundu/bin/node-service.failed
cp /opt/yundu/bin/node-service.bak.stormfix.20260803121737 /opt/yundu/bin/node-service
systemctl start yundu-node-service

# 回滚 identity-service
systemctl stop yundu-identity-service
cp /opt/yundu/bin/identity-service /opt/yundu/bin/identity-service.failed
cp /opt/yundu/bin/identity-service.bak.reldel.202608070802 /opt/yundu/bin/identity-service
systemctl start yundu-identity-service
```

---

## 7. 故障排查手册

### 7.1 节点离线

**现象**: 面板显示节点离线

**排查步骤**:
1. 检查 Agent 心跳: 面板 → 节点列表 → 查看最后心跳时间
2. 检查 Agent 服务状态: `systemctl status yundu-node-agent`
3. 检查 Agent 日志: `journalctl -u yundu-node-agent -f`
4. 检查网络连通性: `curl -x socks5://127.0.0.1:30010 https://httpbin.org/ip`
5. 检查 nginx: `nginx -t && systemctl status nginx`

**常见原因**:
- Agent 进程崩溃: `systemctl restart yundu-node-agent`
- 内核启动失败: 检查配置渲染是否正确
- 网络中断: 检查 VPS 网络状态
- WS 通道断开: Agent 会自动降级到 HTTP 轮询

### 7.2 用户无法连接节点

**现象**: 客户端显示连接失败/超时

**排查步骤**:
1. 检查节点是否在线: 面板查看
2. 检查端口是否监听: `ss -tlnp | grep <端口>`
3. 检查 nginx SNI 路由: `cat /etc/yundu/nginx/stream/yundu_autogen.conf`
4. 检查 TLS 握手: `openssl s_client -connect 127.0.0.1:<端口> -servername <SNI域名> -brief`
5. 检查证书 SAN: `openssl x509 -in /path/cert.pem -text -noout | grep -A1 "Alternative Name"`

**常见原因**:
- 证书缺少 SAN: 重新生成带 SAN 的证书
- 端口未开放: 检查防火墙规则
- nginx 配置错误: `nginx -t` 测试

### 7.3 订阅客户端问题

**现象**: karing/hiddify 只显示部分节点

**排查步骤**:
1. 检查订阅模板: `SELECT * FROM subscribe_templates;`
2. 检查 sing-box 配置是否包含非法字段（如 `match`）
3. 检查客户端 UA 检测顺序: karing/hiddify 必须在 clash/mihomo 之前
4. 检查订阅配置是否携带 inbounds/experimental（不应携带）

### 7.4 数据库问题

**现象**: 服务报错无法连接数据库

**排查步骤**:
1. 检查 postgres 容器: `docker ps | grep postgres`
2. 检查 postgres 日志: `docker logs yundu-postgres --tail 50`
3. 测试连接: `docker exec -it yundu-postgres psql -U app -d airport -c "SELECT 1"`
4. 检查连接数: `SELECT count(*) FROM pg_stat_activity;`
5. 检查磁盘空间: `df -h`（数据库6.5GB）

### 7.5 版本风暴

**现象**: config_versions 表每10秒+1

**排查步骤**:
1. 检查版本增量: `SELECT count(*) FROM config_versions WHERE created_at > now() - interval '10 minutes';`
2. 检查渲染 hash 是否稳定: 面板 → 配置版本历史
3. 检查 users 排序是否确定

**根治方案**:
- 确保 users 稳定排序 + 单测
- 迁移 000077 补 applied_at
- 清理历史版本

### 7.6 tc 限速问题

**现象**: tc qdisc/class exit status 2

**排查步骤**:
1. 检查是否为 root: `id`
2. 检查 filter 是否幂等: `tc filter show dev ens3`
3. 检查是否重复添加: `tc qdisc show dev ens3`

**解决方案**: filter 先删后加幂等（已在 v0.7.22+ 修复）

### 7.7 新建套餐 500 错误

**根因**: 
1. feature_flags jsonb 编码问题（已修复）
2. 纯中文套餐名 slugify 后为空，回退固定 code='plan'，撞软删唯一约束
3. 软删记录占用 code 唯一约束

**修复**: 
- 后端 GetByCodeAny（含软删）→ 409 明确提示
- 前端 slugify 空回退改为 `plan-<时间戳>`
- 软删时 code 追加 `:del:` 后缀释放

### 7.8 营收统计错误优惠券折扣

**根因**: GetRevenueStats SQL 用 amount_cny（折扣前），应用 final_amount（实付）

**修复**: 
- CNY 营收 = 法币订单 final_amount 之和
- USDT 营收 = 加密订单 final_amount 之和
- SQL 列顺序与结构体 Scan 顺序对齐

---

## 8. 备份与恢复

### 8.1 数据库备份

```bash
# 手动备份
docker exec yundu-postgres pg_dump -U app airport | gzip > /opt/yundu/backup/airport_$(date +%Y%m%d_%H%M%S).sql.gz

# 定时备份（crontab）
0 3 * * * docker exec yundu-postgres pg_dump -U app airport | gzip > /opt/yundu/backup/airport_$(date +\%Y\%m\%d).sql.gz

# 恢复
gunzip < airport_backup.sql.gz | docker exec -i yundu-postgres psql -U app -d airport
```

### 8.2 Redis 备份

```bash
# Redis 数据由 Docker 卷持久化: /opt/yundu/data/redis
# 备份
cp -r /opt/yundu/data/redis /opt/yundu/backup/redis_$(date +%Y%m%d)
```

### 8.3 配置文件备份

```bash
# 备份列表
/opt/yundu/config/.env          # 环境变量
/etc/yundu/                     # Agent 配置
/etc/nginx/                     # nginx 配置
/www/server/panel/vhost/nginx/  # BT Panel nginx 站点配置
```

### 8.4 二进制备份

```bash
# 当前版本备份
cp /opt/yundu/bin/node-service /opt/yundu/bin/node-service.bak.manual.$(date +%Y%m%d)
```

---

## 9. 安全加固

### 9.1 防火墙规则

**当前状态（VPS190）**:
- iptables 默认 ACCEPT，有端口级 ACCEPT+DROP 规则（存在冲突）
- 建议优化:

```bash
# 清理冲突规则
iptables -D INPUT -p tcp --dport 8090 -j DROP
iptables -D INPUT -p tcp --dport 8084 -j DROP
iptables -D INPUT -p tcp --dport 8083 -j DROP
iptables -D INPUT -p tcp --dport 8082 -j DROP
iptables -D INPUT -p tcp --dport 8081 -j DROP

# 仅允许内网访问微服务端口
iptables -A INPUT -p tcp --dport 8081 -s 127.0.0.1 -j ACCEPT
iptables -A INPUT -p tcp --dport 8082 -s 127.0.0.1 -j ACCEPT
iptables -A INPUT -p tcp --dport 8083 -s 127.0.0.1 -j ACCEPT
iptables -A INPUT -p tcp --dport 8084 -s 127.0.0.1 -j ACCEPT
iptables -A INPUT -p tcp --dport 8081 -j DROP
iptables -A INPUT -p tcp --dport 8082 -j DROP
iptables -A INPUT -p tcp --dport 8083 -j DROP
iptables -A INPUT -p tcp --dport 8084 -j DROP

# 开放公网端口
iptables -A INPUT -p tcp --dport 80 -j ACCEPT
iptables -A INPUT -p tcp --dport 443 -j ACCEPT
iptables -A INPUT -p tcp --dport 30128 -j ACCEPT  # nanoroute
iptables -A INPUT -p tcp --dport 9091 -j ACCEPT   # warp_socks
```

### 9.2 SSH 安全

- ✅ 已禁用密码登录，使用密钥认证
- ✅ 密钥文件: 190key.pem / 206.key / 81.key
- ⚠️ 建议: 修改 SSH 端口（可选）
- ⚠️ 建议: 配置 fail2ban

### 9.3 面板安全

- 管理面板通过 Cloudflare Proxy 访问（隐藏源站IP）
- 安全头已配置: HSTS/X-Content-Type-Options/X-Frame-Options
- 真实 IP 通过 CF-Connecting-IP 获取
- ⚠️ 建议: 配置 Cloudflare WAF 规则限制 /api/v1/admin/* 访问

### 9.4 数据库安全

- PostgreSQL 仅监听 127.0.0.1:5433（不暴露公网）
- Redis 仅监听 127.0.0.1:6380
- NATS 仅监听 127.0.0.1:4223
- ⚠️ 建议: 为 PostgreSQL 配置密码定期轮换

### 9.5 节点安全

- node-agent 控制面端口 10000 不暴露公网
- 节点域名走 Cloudflare 代理
- SNI 伪装域名选择原则:
  - ✅ bilivideo.com / .cn 域名（低风险）
  - ⚠️ microsoft.com（中等风险，SNI-IP矛盾时需海外IP）
  - ❌ google.com（高风险，被封锁）

### 9.6 风控建议

- 单IP日流量控制在10-50GB以内
- SNI-IP尽量匹配（国内域名配国内IP效果最佳）
- 定期轮换IP（Oracle支持更换reserved IP）
- 同一SNI不要承载过高流量
- 路径应随机且足够长（如 /trojan9ad966）

---

## 10. 监控与告警

### 10.1 关键监控指标

| 指标 | 告警阈值 | 检查方式 |
|------|---------|---------|
| 节点离线 | > 5分钟无心跳 | 面板 → 节点列表 |
| 磁盘使用率 | > 85% | `df -h` |
| 内存使用率 | > 90% | `free -h` |
| config_versions 增量 | > 10/10分钟 | 数据库查询 |
| Agent 版本不一致 | 版本差异 | 面板 → 节点列表 |
| 证书过期 | < 7天 | 面板 → 证书管理 |

### 10.2 日志关键字

| 关键字 | 含义 | 处理 |
|--------|------|------|
| `heartbeat sent` | Agent心跳正常 | 正常 |
| `connection success` | 连接成功 | 正常 |
| `sing-box started successfully` | 主内核启动 | 正常 |
| `QueryStats returned empty` | 无流量统计 | 正常（无用户时） |
| `tc qdisc.*exit status 2` | tc命令失败 | 检查filter幂等 |
| `error` / `panic` / `fatal` | 错误 | 立即排查 |
| `version storm` | 版本风暴 | 检查渲染确定性 |

---

## 11. 常见问题速查

### Q1: 节点显示离线但服务在运行
**A**: 检查 WS 通道状态。Agent 支持三通道降级：gRPC → WebSocket → HTTP轮询。
如果 WS 不通，会自动降级到 HTTP。检查 nginx 是否正确代理 `/api/v1/agent/` 路径。

### Q2: 新套餐无法创建（500错误）
**A**: 
1. 检查套餐名是否为纯中文（会自动生成唯一code）
2. 检查 code 是否与软删记录冲突（已修复为自动释放）
3. 检查 feature_flags 字段格式

### Q3: 用户订阅只显示部分节点
**A**: 
1. 检查客户端类型（karing/hiddify 需要 sing-box 格式）
2. 检查订阅模板是否包含非法字段
3. 检查 UA 检测顺序

### Q4: 证书申请失败
**A**: 
1. 检查 DNS-01 token 是否有效
2. 检查域名解析是否正确
3. 检查 ACME 账户是否已注册

### Q5: 营收统计不准确
**A**: 已修复为使用 final_amount（实付金额）。如仍不准确，检查：
- SQL 列顺序与结构体 Scan 顺序是否一致
- 优惠券折扣是否正确计算

### Q6: 优惠券只能用一张
**A**: 已修复为按"每用户"限制，不再按全局 used_count 拒绝。

### Q7: 磁盘满了怎么办
**A**: 按 §6.4 步骤清理 /tmp、备份文件、journal 日志。

### Q8: 如何升级 Agent 不重启服务
**A**: 面板 → Agent版本管理 → 填写新版本 → 发布。Agent 自动下载并热替换。

### Q9: 如何回滚服务
**A**: 按 §6.6 步骤，用 .bak.* 文件回滚。

### Q10: sing-box 启动失败
**A**: 检查编译 tags 是否完整：`with_utls,with_wireguard,with_gvisor,with_quic,with_grpc,with_clash_api`

---

## 附录

### A. 文件路径速查

| 路径 | 用途 |
|------|------|
| `/opt/yundu/bin/` | 微服务二进制 |
| `/opt/yundu/config/.env` | 环境变量配置 |
| `/opt/yundu/logs/` | 微服务日志 |
| `/opt/yundu/data/postgres/` | PostgreSQL 数据 |
| `/opt/yundu/data/redis/` | Redis 数据 |
| `/opt/yundu/agent-upgrade/` | Agent 升级文件 |
| `/etc/yundu/` | Agent 配置目录 |
| `/etc/yundu/nginx/` | Agent nginx 配置 |
| `/etc/yundu/certs/` | 证书目录 |
| `/etc/systemd/system/yundu-*.service` | systemd 服务文件 |
| `/www/server/panel/vhost/nginx/` | BT Panel nginx 站点配置 |
| `/www/wwwroot/6.tiktokplay.na.am/` | admin-web 静态文件 |
| `/www/wwwroot/7.tiktokplay.na.am/` | user-web 静态文件 |
| `/www/wwwlogs/` | nginx 日志 |

### B. 数据库表清单

| 表名 | 用途 |
|------|------|
| users | 用户表 |
| admins | 管理员表 |
| plans | 套餐表 |
| orders | 订单表 |
| servers | 物理服务器 |
| runtimes | 运行时实例 |
| nodes | 逻辑节点 |
| node_groups | 节点组 |
| proxy_chains | 代理链 |
| config_versions | 配置版本 |
| deployment_batches | 发布批次 |
| tls_certificates | TLS证书 |
| cert_bundles | 证书包 |
| subscription_tokens | 订阅令牌 |
| traffic_logs | 流量日志 |
| system_settings | 系统设置 |
| coupons | 优惠券 |
| tickets | 工单 |
| announcements | 公告 |

### C. 迁移文件清单（000001-000077）

共 77 个迁移文件，最新版本 000077（config_versions.applied_at 补列）。
完整列表见 `migrations/` 目录。

### D. 编译与部署命令

```bash
# 编译 node-service
cd apps/node-service && go build -o yundu-node-service ./cmd/api

# 编译 node-agent
cd apps/node-agent && go build -o yundu-agent ./cmd/agent

# 编译全部服务（带完整tags）
TAGS="with_utls,with_wireguard,with_gvisor,with_quic,with_grpc,with_clash_api"
go build -tags "$TAGS" -o node-service ./cmd/api
go build -tags "$TAGS" -o node-agent ./cmd/agent

# 数据库迁移
goose -dir migrations postgres "$DATABASE_URL" up
```

### E. 一键安装命令

```bash
# 安装 node-agent
curl -fsSL https://github.com/energie2008/yundu/raw/main/install.sh | bash -s -- agent --token=TOKEN --endpoint=https://panel.example.com

# 安装面板
curl -fsSL https://github.com/energie2008/yundu/raw/main/install.sh | bash -s -- panel

# 升级
curl -fsSL https://github.com/energie2008/yundu/raw/main/install.sh | bash -s -- upgrade all
```

---

> **文档维护**: 本文档应随代码更新而更新。每次重大变更后请同步更新本指南。  
> **验证日期**: 2026-08-07  
> **验证人员**: Codex 代码Agent  
> **验证方式**: 3台VPS全部实际登录验证
