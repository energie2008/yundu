# USDT 支付链路检查与中间件选型分析

日期：2026-08-02
范围：YunDu 项目自研 USDT 支付轮询链路 + 结算/支付页改造 + 支付中间件选型

## 一、现有支付链路检查结果（基于生产代码与日志）

YunDu 未接入任何现成支付中间件，采用的是自研链上轮询方案：

- TRC20：`TronGrid API` + 60 秒轮询 + 地址/金额/确认数匹配，代码入口 `payment_service.go:matchTRC20`
- EVM（ERC20/Polygon）：`Etherscan 风格 API` + 60 秒轮询，代码入口 `payment_service.go:matchERC20`
- 生产配置：TRC20 已启用且地址正确；ERC20 配置为 `enabled=false, network=ethereum`，地址是用户提供的 Polygon 地址

### 检查发现

| 项目 | 状态 | 说明 |
|---|---|---|
| TRC20 轮询 | 可用 | TronGrid 无需 Key，生产日志无 TRC20 报错，实测返回正常 JSON |
| ERC20/Polygon 轮询 | 不可用 | 每 60 秒报 `json: cannot unmarshal string ...`；且 `api.polygonscan.com` V1 已 301 弃用，Etherscan V2 必须 API Key |
| 重复激活风险 | 存在 | `UpdateStatus` 无 `status='pending'` 条件；同金额多笔 pending 订单会被同一笔转账重复认领；历史 9 笔 0 元订单共用 `ZERO_YUAN_COUPON` |
| 订单金额展示 | 有误 | 支付页固定显示 `amount_usdt`，优惠券订单应显示 `final_amount` |
| 前端支付页 | 不完整 | 写死 USDT-TRC20 文本，无图标、无网络警告、无 OKX/Binance 购买入口 |

## 二、本次修复内容

### 后端（identity-service）

1. EVM 响应解析容错：`Result` 兼容 JSON 字符串（限流/缺 Key 提示），不再因 `unmarshal string` 刷错误。
2. EVM 多网络支持（Polygon 为主，Arbitrum 可选）：
   - `ERC20Config` 支持 `networks` 列表，网络可后台多选（Polygon / Arbitrum One）
   - 统一使用 Etherscan V2 API（`https://api.etherscan.io/v2/api?chainid=137`），V1 已官方弃用
   - 订单币种按网络显示：`USDT-TRC20` / `USDT-Polygon` / `USDT-Arbitrum One`
   - Ethereum 主网已下线（手续费过高）；BSC/BEP20 已移除（Etherscan V2 免费计划不支持 BSC 链查询，且 BscScan V1 已弃用）
3. 防重复激活：
   - 新增 `MarkPaidIfPending`，轮询匹配仅允许 pending 订单被标记
   - 每笔链上交易先查询 `tx_hash` 占用，防止同金额多订单被同一转账重复激活
   - 新增 migration `000074`：真实交易哈希部分唯一索引（排除 `ZERO%` 内部标记）
   - 0 元订单标记改为 `ZERO:<order_no>`，避免历史共用标记
4. 后台支付配置：`usdt_erc20` 支持网络多选（Polygon / Arbitrum One）与 Etherscan V2 API Key 配置（留空保持不变）。
5. 轮询按需拉取：存在对应网络的待支付订单时才查询链上数据，无订单时不发外部请求，减少噪音与限流风险。
6. 防串单：订单按网络（pay_currency）匹配，唯一约束为 `(pay_currency, tx_hash)`，同一地址多链收款互不干扰。

### 前端（user-web / admin-web）

1. 结算确认卡片：点击“去结算”后弹出确认支付卡片，展示套餐、周期、原价/优惠/总计、支付方式、应付等值 USDT，确认后跳转订单支付页。
2. 支付页真实链路：
   - USDT 品牌徽标（₮ / USDT 绿色标）
   - 按订单网络显示 `USDT-TRC20 收款地址` / `USDT-Polygon 收款地址` / `USDT-Arbitrum One 收款地址`
   - 结算卡片内 EVM 支付方式可切换已启用的网络
   - 警告：请勿使用其它网络或其它币种转账，否则可能无法自动到账
   - OKX 购买 USDT、Binance 购买 USDT 两个真实入口
   - 实付金额改用 `final_amount`

## 三、部署与验证结果

- 已部署至 vps190（identity-service 新二进制 + user-web/admin-web 前端），服务 active
- 数据库：erc20 已启用，networks 当前为 `["polygon"]`；migration 74/75 已执行并注册
- E2E 实测：
  - `GET /api/v1/user/payment-methods` 返回 `USDT-TRC20` 与 `USDT (networks=[polygon])`
  - 创建 TRC20 订单成功：`USDT-TRC20` + `TLAoiTwPNCtFXpJWmPrvgm8tRpC9ggP42H`
  - 创建 Polygon 订单成功：`USDT-Polygon` + `0xeB784Ac722D7bF65A6F5aCa5a47591f54EF323Eb`
  - 创建 Arbitrum/BSC 订单返回 `400 unsupported payment network`
  - 无头浏览器验证支付页含收款地址、警告、OKX/Binance 入口、USDT 徽标；结算确认卡片文案齐全
- 待办：Polygon/Arbitrum 自动到账需在后台填入 Etherscan V2 API Key（免费注册：etherscan.io/apidashboard）。TRC20 无需 Key，已真实可用。

## 五、Aptos 支持评估

结论：可以做，但属于独立的新支付通道，不是“在现有 EVM 卡片上加一个网络”。

- Aptos 是非 EVM（Move 链），不能复用 Etherscan V2；需对接 Aptos Indexer/REST API 查询链上 coin 转账
- Aptos 的 USDT 是链上 coin 资产，地址格式与 EVM 不同，必须使用独立的 Aptos 收款地址，无法与 TRC20/EVM 共用地址
- 需要新增 `usdt_aptos` 支付方式：独立配置、独立轮询（Aptos REST/GraphQL）、独立前端与后台卡片
- 工作量约为再实现一条 TRC20 级通道，且依赖第三方 Indexer 的稳定性和限流

建议：先让 TRC20 + Polygon 稳定跑通（当前已部署），Aptos 作为二期需求，在确认 Aptos 收款地址和 Indexer 可用性后再实施，避免同时引入多个新外部依赖影响系统稳定性。

## 四、支付中间件选型分析

目标：维护少、可后台更换收款地址、支持 TRC20 与 Polygon。

| 项目 | 维护状态 | TRC20 | Polygon/多链 | 后台换地址 | 结论 |
|---|---|---|---|---|---|
| assimon/epusdt | 原项目已转手、长期停更，存在大量分叉 | 支持 | 不支持（TRC20 为主） | 有 | 不推荐，风险是无人维护 |
| v03413/BEpusdt | 活跃，2026-07 仍有 v1.24.1 发布 | 支持 | 支持 TRON/Ethereum/BSC/Polygon 等 | 支持静态+动态地址 | 推荐 |
| TokenPay | 分叉多（mo-gong / LightCountry 等），原版维护偏弱 | 支持 | 支持 ETH 系 | 有 | 次选，分叉选择成本高 |
| itboy666/v2board-epusdt | 未检索到可靠仓库 | 不确定 | 不确定 | 不确定 | 不推荐 |

### 推荐：v03413/BEpusdt

理由：
- 活跃维护，2026 年 7 月仍在发版，长期风险最低
- 单体二进制 + Docker 部署，依赖少（相比 Epusdt 的 MySQL+Redis 更轻）
- 完全兼容 Epusdt API，未来若从自研轮询迁移可无缝替换
- 支持 TRON/Ethereum/BSC/Polygon，静态地址 + 动态地址均可后台管理，满足“后台更换收款地址”

### 落地路径建议（保留自研 + 预留迁移）

1. 近期：继续使用自研轮询。TRC20 已可用；补 Etherscan V2 API Key 后 Polygon 自动到账。
2. 中期：在 identity-service 增加支付网关抽象层（`PaymentGateway` 接口），把 BEpusdt 作为可切换后端，自研轮询保留为 fallback。
3. 远期：若业务量增长、需要多币种/代币收款，再切 BEpusdt，前端订单/回调接口无需重写。
