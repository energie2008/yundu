# 支付宝/微信支付基础搭建与易支付接入分析

日期：2026-08-02
范围：YunDu 法币支付（支付宝/微信）基础能力建设 + 第三方易支付接入策略

## 一、现状盘点（基于代码事实）

当前项目对支付宝/微信只有“框架预留”，没有真实支付能力：

| 层面 | 现状 | 问题 |
|---|---|---|
| 配置模型 | `WechatConfig/AlipayConfig` 有 `enabled/app_id/mch_id/api_key/notify_url/private_key` 字段 | 仅占位，未对接任何真实通道 |
| 订单创建 | `CreateOrder` 对 wechat/alipay 生成 `pay_currency=CNY`，`pay_address=""`，`payment_uri="pending:..."` | 订单可创建，但没有真实支付链接/二维码 |
| 支付入口 | 订单详情页仅当 `pay_address` 非空时显示支付信息 | 法币订单无地址，用户下单后看不到支付入口，等于“死单” |
| 回调 | 无任何异步通知/验签/查单接口 | 支付成功无法自动激活订阅 |
| 后台配置 | 支付页对 wechat/alipay 只显示启用开关，编辑表单仍是 USDT 的地址/容差字段 | 无法配置易支付商户号、密钥 |
| 轮询 | 仅链上轮询（TRC20/Polygon） | 法币没有主动查单兜底 |

结论：要接第三方易支付，先补四块基础：可插拔网关抽象、订单网关字段、回调验签+幂等激活、主动查单兜底；同时修掉前端支付入口缺失的问题。

## 二、同行策略

### 1. 渠道分层：USDT 做利润主力，支付宝/微信做转化主力

- v2board/Xboard 生态主流做法：USDT（Epusdt/BEpusdt 自建）几乎零手续费、无风控，作为“低价通道”；支付宝/微信走易支付/四方代收，手续费 2%-5%，但转化率最高。
- 运营上普遍给 USDT 额外折扣（5%-10%），引导大额用户走链上，把利润留在手续费侧。
- 对机场这种“小额高频+续费”业务，支付宝/微信是留存关键，不能只有 USDT。

### 2. 个人收款风控现实

- 个人无法直接申请支付宝/微信官方商户；同行普遍用第四方易支付（服务商用自己的通道收款、扣手续费后 D0/D1 结算）。
- 原版 V2Board 的 `Payments/epay.php` 就是为易支付协议写的：`submit.php` 下单 + `notify_url` 异步回调 + MD5 验签。
- 3X-UI 不卖货，没有支付；Xboard 通过插件接 Epusdt/Epay，生态已验证该协议。

### 3. 稳定性策略

- 至少接 2 家易支付/四方，后台可一键切换主备通道，避免单一平台掉线或跑路。
- 回调必须幂等：重复通知不重复激活；金额以订单锁定金额校验；IP/签名双重校验。
- 定时对账：主动查单兜底，回调丢失也能自动激活。
- 前端展示支付二维码/跳转链接 + 倒计时 + “我已支付”手动刷新。

## 三、推荐方案：易支付网关抽象层

### 1. 支付网关接口（核心基础）

```go
type PayGateway interface {
    Name() string
    CreatePayment(ctx context.Context, order *model.PaymentOrder) (*GatewayPayment, error)
    VerifyNotify(ctx context.Context, params map[string]string) (*GatewayNotify, error)
    QueryOrder(ctx context.Context, order *model.PaymentOrder) (*GatewayQueryResult, error)
}
```

- `CreatePayment`：调用易支付 `submit.php`，返回支付跳转 URL / 二维码内容
- `VerifyNotify`：MD5 验签（参数 ASCII 排序，`sign/sign_type/空值`不参与），校验金额与订单
- `QueryOrder`：主动查单，用于轮询兜底
- 现有 `PaymentService` 是集中入口，插入 gateway registry 即可，USDT 轮询和法币回调互不干扰

### 2. 订单模型补充

- 复用 `tx_hash` 存易支付 `trade_no`（内部流水，不对外展示），或新增 `gateway_trade_no` 列
- 建议新增轻量 migration：
  - `payment_gateway VARCHAR(32) DEFAULT ''`（epay_xxx 通道标识）
  - `gateway_trade_no VARCHAR(128)`
  - `gateway_status VARCHAR(32)`
- 法币订单 `pay_address` 可存易支付二维码内容，前端现有支付卡片无需大改

### 3. 回调与激活

- 新增公开路由：`POST /api/v1/payment/notify/:gateway`（网关放行，签名校验）
- 复用现有 `MarkPaidIfPending` + `activateOrder`，天然幂等
- 回调日志记录完整参数与验签结果，便于对账排障

### 4. 后台配置（复用现有 Payments 页面）

- wechat/alipay 卡片增加易支付配置：`商户ID(pid)`、`商户密钥(key)`、`网关地址`、`下单类型(type=alipay/wxpay)`、`启用/停用`
- 支持主备双通道（通道 A/B），切换只改配置不重发版

### 5. 前端支付页

- `Checkout` 已支持 fiat 支付方式展示，无需大改
- `OrderDetail` 修复：法币订单有 `payment_uri` 时显示“跳转支付/扫码支付”卡片；有 `pay_address`（二维码）时显示二维码
- 保留“我已支付，刷新状态”，配合主动查单

## 四、易支付协议要点（彩虹易支付兼容）

下单（服务端发起，推荐 POST）：

```text
POST https://<epay-domain>/submit.php
pid=商户ID&type=alipay|wxpay&out_trade_no=订单号
&notify_url=回调地址&return_url=跳转地址&name=商品名&money=金额
&sign=MD5(参数ASCII排序拼接&key)&sign_type=MD5
```

回调：

```text
POST notify_url
pid&trade_no&out_trade_no&type&name&money&trade_status&sign
```

- 验签后检查 `money` 与订单 `FinalAmount` 一致、`trade_status` 为已支付
- 返回 `success`（小写）即停止重试；返回其它内容易支付会多次回调

## 五、运营与风控建议

1. 双平台冗余：A 平台主用、B 平台备用，后台一键切换；上线前用 0.01 元测试单验证回调。
2. 手续费定价：USDT 标价按 7.2 汇率；支付宝/微信按易支付实际成本上调约 3%，或统一含在套餐价内。
3. 订单有效期：法币订单 10-30 分钟，超时自动过期；回调晚到但订单已过期时由管理员补单。
4. 金额防串：回调只认订单号+金额+签名，金额用数据库锁定值，不接受回调携带金额覆盖。
5. 日志与对账：每天跑一次“已支付未激活”清单，回调丢失由主动查单兜底。

## 六、实施顺序建议

1. 第一批（基础，无需商户号即可交付）：网关抽象 + 订单 gateway 字段 + 回调验签框架 + 前端支付入口修复 + 后台易支付配置表单。
2. 第二批（真实接通）：接入具体易支付平台（彩虹易支付兼容协议），0.01 元测试通过后上线支付宝/微信。
3. 第三批（稳定）：双通道切换 + 主动查单 + 对账任务 + 监控告警。

当前最优先补的是第一批：它决定后续接任何易支付平台都不需要改架构，只改配置。

## 七、第一批实施记录（已完成，2026-08-02，v0.7.30）

- `PayGateway` 抽象 + 彩虹易支付兼容实现 `epay_gateway.go`：`submit.php` 下单、MD5 验签、`api.php` 主动查单
- 订单表 migration 76：新增 `gateway` / `gateway_trade_no` / `payment_uri`
- 法币订单创建时自动调用易支付生成支付链接/二维码，网关失败自动取消订单
- 回调路由 `POST /api/v1/payment/notify/:method`（api-gateway 已放行，绕过登录限流），验签+金额校验+`MarkPaidIfPending` 幂等激活
- 轮询新增法币订单主动查单兜底（订单创建 1 分钟后每 60 秒查一次）
- 后台支付配置：支付宝/微信可配置易支付 `pid / key / 网关地址 / 下单类型 / notify_url / return_url`
- 前端订单详情：法币订单显示“去支付”按钮、二维码、倒计时与刷新状态
- 已用 mock 易支付全链路验证：下单、错误签名拒绝、正确签名回调、订阅自动激活、公网回调通路
- 单测覆盖：签名、错误签名拒绝、下单解析、查单已支付/未支付

## 八、机场常用彩虹易支付兼容平台推荐

### A. 自建彩虹易支付（推荐长期方案）

机场圈最主流的是“自己搭一套彩虹易支付”，再对接收款通道：

- 彩虹易支付（PHP 开源，`assimon`/多个维护 fork）— 协议兼容、社区资料最多
- 独角数卡 / 异次元（同生态商城+支付网关）
- 收款通道：V免签（个人微信/支付宝监听）、支付宝当面付、微信 Native（需要营业执照或渠道）

优点：完全掌控、费率由自己选择、不怕平台跑路；缺点：需要维护一台 PHP 服务，且收款通道资质仍是核心。

### B. 现成免签约四方易支付（快速上线推荐）

这类平台提供服务商自有通道，按彩虹协议开放 `pid/key`，适合快速开通：

- v8易支付（`pay.v8jisu.cn` 等）— 彩虹协议兼容、机场圈使用较多
- 码支付（个人收款聚合）— 老牌，但近年风控严格
- PayJS / 虎皮椒 — 微信/支付宝聚合，个人开发者常用
- 各家“XX易支付”聚合站 — 需自行甄别

选择标准（重要）：
1. 明确支持彩虹易支付 `submit.php` / MD5 协议（本系统已按此实现）
2. 经营时长 ≥ 1 年、有公开客服/工单、支持 D0 结算
3. 费率透明（支付宝/微信通常 2%-5%），先小额试单再放量
4. 避开“价格异常低、无备案、无法联系”的平台，防跑路

### C. 落地建议

- 先用一家现成免签约平台快速开通支付宝/微信（本周可完成）
- 同时准备自建彩虹易支付作为第二通道，降低单一平台风险
- 后台已支持“只改配置不发版”切换，接第二家平台时只需在支付配置页换 `pid/key/网关地址`
