# YunDu 三文档对照 · 801 方案回退 · 标准化 · 零 SSH 终局方案

> 精读文档：
> 1. `D:\机场搭建\技术指导\YunDu-项目深度体检报告-yundu-src版-20260730.md`（1066 行）
> 2. `D:\机场搭建\技术指导\编译推送与安装升级维护教程.md`（1054 行）
> 3. `D:\机场搭建\技术指导\YunDu-面板节点下发失败诊断与维护指南-20260801.md`（1256 行）
> 对照代码：`D:\机场搭建\yundu-src`（基线 `9c47780` + 工作区未提交改动）
> 日期：2026-08-01

---

## 一、三份文档精读摘要

### 1.1 730 体检报告（架构全貌型）

定位是“单一权威体检基准”，结构为**上篇架构体检（§1–§13）+ 下篇运维体检手册（§14–§16）**：

- 架构体检覆盖：系统架构（三平面 + 八大设计原则）、渲染器（注册表 + 双核校验入门禁 + chain 收敛）、双内核（P2 翻转 + 蓝绿热转 + 四级 HotDiff + `_xray_config`）、Agent 骨架（runAgent 2163 行、三通道、加密 Payload、八阶段事务、自升级）、证书（certmagic + 四级回退）、流量限制、WARP（outbound 三件套、账户池、按需分配、流媒体解锁、urltest 负载均衡）、链式代理、健康度评分、四系统对比、超越方案。
- 运维体检手册：验收 SQL 脚本集、绿/黄/红阈值、日志关键字、编译命令与 6 个功能标签、goose 迁移流程、文档导航。
- 评分：8.8/10（架构 9、渲染器 9、双核 9、Agent 8.5、证书 8.5、流量 8、WARP 9、链式 8.5）。
- 特点：**file:line 级证据**，727→730→731/801 的增量演进记录，是覆盖面最广的文档。

### 1.2 编译推送教程（运维操作型）

定位是**双路径部署手册**：

- 路径 A：CI 发版（git push main + tag `v*` → GitHub Actions 编译 7 二进制 × 2 架构 → Release → `install.sh upgrade`）。
- 路径 B：本地交叉编译热修复（PowerShell + `CGO_ENABLED=0` + SCP + systemd 替换，5 分钟完成），731/801 全部走此路径。
- 关键避坑：SCP 后权限 644 导致 `status=203/EXEC`、运行中二进制 `Text file busy`、PowerShell 吞引号、goose 迁移表名 `goose_db_version`、DB 用户 `app` 库 `airport`、前端不在 CI、node-agent 必须携带 6 个功能标签。
- 明确记录：**前端需要单独构建并手动部署到宝塔 nginx 真实根目录**（`/www/wwwroot/6.tiktokplay.na.am` 等），CI 不产出前端。

### 1.3 801 维护指南（故障诊断型）

定位是**下发失败诊断与维护**：

- 机制全景：配置渲染链路、版本协商（`!=` 而非 `<`）、GetRuntimeConfig 三条路径、`_xray_config` 内嵌、dispatch_status 状态机。
- 7 类根因 + 修复记录：版本号漂移、重复 runtime、Agent 版本过低、path3 不更新、agent_version 竞态、版本号死循环、隧道 hostname。
- 生产验证：801 09:56 CST 部署简化版方案，24/24 节点 pushed、0 failed、每台 VPS 恰好 2 条 runtime、migration 70 已执行。
- 零 SSH 原则：面板是唯一真相源，SSH 只读诊断，Agent 升级是唯一人工 SSH 写操作。
- 下一步：监控告警（P1）、Agent 自动升级（P2）、注册门禁转强制（P2）、文档同步（P3）。

---

## 二、哪份分析更像项目的全貌：三方对比

### 2.1 对比表

| 维度 | 730 体检报告 | 我的 360 度调查分析 | 801 维护指南 | 编译推送教程 |
|---|---|---|---|---|
| 定位 | 架构全貌 + 运维体检 | 独立代码调查 + 工程卫生 | 下发失败诊断 | 部署操作 |
| 覆盖面 | 13 章架构 + 3 章运维 | 13 章架构 + 对比 + 维护 | 机制 + 根因 + 修复 | CI + 热修复 + 前端 |
| 代码事实深度 | 最深（file:line 全覆盖） | 中深（关键链路 + 定向测试） | 深（下发链路） | 无（操作流程） |
| 独立验证 | 有（2 个 Search 子代理 + 官方仓库抓取） | 有（go test 实测 + git 历史） | 有（生产 SQL/日志验证） | 有（部署实践） |
| 新发现 | 少（文档倾向“已根治”） | 多（红测试、空文档、ack 模糊匹配、自愈分叉） | 中（生产坑） | 中（部署坑） |
| 可操作性 | 强（验收 SQL） | 中 | 最强（Checklist） | 强 |
| 时效性 | 自称 801 版，但代码未提交 | 基线 9c47780 + 未提交 | 生产 09:56 部署时点 | 2026-08-01 更新 |

### 2.2 结论：730 更像“全貌”，但它不等于“全真”

**如果“全貌”指覆盖面和结构化程度，730 体检报告最接近项目全貌**：它把三平面、双内核、WARP、链式代理、证书、运维体检全部按同一目录结构串起来，且 file:line 级证据密度远超我的调查分析。

**但“更像全貌”不等于“更准确”**。三方对照后发现 730/801 存在系统性偏差：

1. **“已根治”结论下得过早**：
   - 730 自己承认“其他未排序的渲染数据源（outbound/chain）可能复发”（§4.5 #6 残留风险），却又在 §1.4 宣称“7 类根因全部根治”；
   - 801 §7.2 也标注“全局覆盖待扩展”，与“全部根治”矛盾。
2. **遗漏了双内核 + 三通道下新引入的冲突**（我的冲突精读已定位）：
   - ack 回写走 nil-ref 模糊匹配，可能把 sing-box 的 ack 挂到 xray runtime；
   - HTTP 自愈刷 pushed、WS/gRPC 自愈刷 applied，三通道语义分叉；
   - `ReportConfigResult` 兜底标记全部制造 `lpv != _dispatch_version` 矛盾态；
   - `SelfHealFailedDispatch` 用 `updated_at` 而非 `_dispatch_time`。
3. **文档与代码不同步**：
   - 801 声称“注册门禁已以观察模式运行”，代码中不存在（`MinAgentVersionForP2` 只在推送时检查）；
   - 801 §7.4 声称“ack 回写 node 级 applied 待优化”，工作区代码已实现；
   - 730 声称“代码基准 main 含 801 简化版”，实际全部改动在工作区未提交；
   - 教程声称“CI 标签待更新”，工作区 release.yml 已改（未提交）。

**所以三方互补的准确结论是**：

> 全貌看 730，故障处置看 801，部署操作看教程；**准确性以我独立核对的代码事实为基准**。730 的全貌 + 801 的生产验证 + 我的冲突精读，三者合并才构成当前最可信的项目图景。

---

## 三、“方案已经执行”的现状核实

综合 801 文档与 Git 状态，方案执行状态是**“生产已部署、源码未合入”**：

| 层 | 状态 | 证据 |
|---|---|---|
| 生产 node-service | 801 09:56 CST 已替换为新二进制 | 801 文档 §3.9 部署验证（24/24 pushed） |
| 生产数据库 | migration 70 已执行并注册 goose | 801 文档 §8.2（version_id=70） |
| 本地源码 | 方案改动在 7 个文件的工作区未提交状态 | `git status`（release.yml、agent_handler.go、node_repo.go、deployment_service.go、health_service.go、kernel_render_adapter.go、runtime_service.go） |
| GitHub main | 不含方案改动 | git log 最新 `9c47780` |

这意味着三个风险：

1. **基线不可复现**：现在从 GitHub 拉取任何 tag 都编译不出生产正在运行的修复，出故障无法用标准流程重建；
2. **回退困难**：生产二进制没有对应源码版本，回退只能靠“当时上传的二进制”或手工还原；
3. **文档漂移**：730/801 文档描述的是“生产 + 计划”的混合状态，不是仓库状态。

---

## 四、回退策略（分场景，先判断要不要回退）

### 4.1 总体判断：方案整体不要回退，只回退“有害子行为”

方案解决的 7 类根因是生产实测有效的（24/24 pushed、版本稳定、无重复 runtime）。回退整个方案会重新引入版本号死循环、重复 runtime、failed 卡死等问题。**需要回退/修正的是我冲突精读中定位的 4 个实施缺陷**，而不是方案本身。

### 4.2 场景 A：新 node-service 二进制出问题，需回退二进制

```bash
# 标准回退（GitHub Release 产物，需先保证 tag 存在）
curl -fsSL https://github.com/energie2008/yundu/raw/main/install.sh | bash -s -- upgrade panel --version=v0.3.1

# 或保留旧二进制备份回退（推荐立即补做备份）
ssh -i key/190key.pem root@43.135.147.190
cp /opt/yundu/bin/node-service /opt/yundu/bin/node-service.bak.20260801
# 出问题时：
systemctl stop yundu-node-service
cp /opt/yundu/bin/node-service.bak.20260801 /opt/yundu/bin/node-service
systemctl start yundu-node-service
```

### 4.3 回退陷阱：二进制回退必须与 migration 70 配套评估

801 方案的核心之一是把 `provider_ref` 回退为 `sing-box`/`xray` 并回填 NULL。**旧二进制（无 provider_ref 回退的提交版）只按 `provider_ref IS NULL` 查询复用 runtime**：

- migration 70 已把 NULL 回填为 `sing-box`/`xray`，旧二进制 `GetByServerAndProvider(..., nil)` 查 NULL 会 miss；
- miss 后旧逻辑创建新 runtime（provider_ref=NULL），而 `uq_runtimes_server_type_null_ref` 只允许每 server+runtime_type 一条 NULL；
- 同一台 VPS 再次注册时会唯一约束冲突，注册失败。

因此，**如果回退 node-service 到 801 之前的旧版本，必须同时处理 runtime 数据**（把该 server 主 runtime 的 provider_ref 改回 NULL，或删除新 runtime 重建 NULL 记录），否则会出现“升级成功、回退后无法注册”的假性故障。

**更安全的路径**：不回退到“旧版语义”，而是先提交当前工作区形成基线，再发布“修正版”（见第五章），修正版兼容 migration 70 与三通道。

### 4.4 场景 B：migration 70 回退

**不建议回退**。000070 是幂等数据迁移：回填 NULL + 部分唯一索引，没有破坏性删除。保留它不会影响任何版本代码（旧代码可正常读写非 NULL 记录，只是复用语义不同）。

若确实要回退：

```sql
DROP INDEX IF EXISTS uq_runtimes_server_type_null_ref;
-- 注意：不回填 provider_ref，否则旧代码仍 miss；回退后需人工保证每 server+runtime_type 一条 NULL runtime
```

回退后重复 runtime 的洞会重新打开，需要重新依赖人工 SQL 巡检。

### 4.5 场景 C：源码回退

**不要丢弃工作区改动**。当前 7 个文件的未提交改动就是生产正在运行的修复，丢弃后生产与源码彻底失联。正确动作：

1. `git add` + 提交为一次提交（建议 message 标注“801 简化版方案 + 生产验证基线”）；
2. 在该提交之上打修正补丁（第五章 P0 清单）；
3. 打 tag `v0.7.14`（或合适版本）触发 CI，让生产二进制与 Release 产物一一对应。

---

---

## 五、解决方案：修正版根治（在 801 方案之上）

依据《YunDu-简化版根治方案-架构冲突精读分析-20260801.md》，把方案已部署版本升级为“三通道安全版”：

### P0（1–2 天，随下一次热修复一起部署）

| # | 修正 | 目的 |
|---|---|---|
| 1 | `SelfHealFailedDispatch` 改读 `metadata._dispatch_time` 判 5 分钟陈旧 | 消除 `updated_at` 被其他更新刷新导致的自愈失灵 |
| 2 | `runtime_service.go` Create 失败仅对 PostgreSQL 23505 走重查，其他错误原样返回 | 避免把 DB 故障伪装成注册成功 |
| 3 | 删除 `ReportConfigResult` 的“全部不匹配则标记全部”兜底；不匹配时只记 runtime 级日志 | 杜绝 `lpv != _dispatch_version` 矛盾态 |
| 4 | 三通道 ConfigResult 全部复用 kernel 类型精确解析 runtime（禁止 nil 回退） | 解决 ack 挂错 runtime |
| 5 | 提交工作区并打 tag，建立可复现基线 | 解决生产/源码失联 |

### P1（1 周）

| # | 修正 | 目的 |
|---|---|---|
| 6 | 三通道自愈收敛为单一 `DispatchSelfHealService`：版本一致+运行中→applied；版本不一致→failed+触发 reload | 消除 pushed/applied 双向震荡 |
| 7 | `ReportConfigResult` 的 config_versions / nodes.metadata / deployment_targets 三写放入同一事务（或 outbox） | 消除跨表不一致 |
| 8 | `UpdateDispatchStatus` 改批量 SQL（`id = ANY($1)`） | 消除逐节点半更新 |
| 9 | 注册路径加版本门禁（先观察 warning，7 天转强制）；同时放开 Agent 注册 `agent_version` 的 JSON tag | 落实“低版本注册即暴露” |

### P2（1 周）

| # | 修正 | 目的 |
|---|---|---|
| 10 | 排序契约统一：node/chain_bindings/runtimes/outbound 等渲染相关查询全部 `ORDER BY ... id`，渲染入口使用与 DB 相同键 | 版本号死循环全局根治 |
| 11 | 补 `config_render_hash_churn` 指标与 failed TTL 指标 | 复发可观测，替代人工 SQL 巡检 |

---

## 六、标准化路线

### 6.1 代码与发布标准化

1. **基线纪律**：所有生产部署必须对应一个 Git commit/tag；热修复也先 commit 再编译，杜绝“生产二进制无源码”状态。
2. **CI 标准化**（当前 release.yml 的待办）：
   - node-agent 编译标签固定为 6 个（工作区已改，合入即生效）；
   - 增加 `go test` job（先把 `packages/subscription/validator` 红测试修绿，再纳入门禁）；
   - 增加前端构建 job：admin-web / user-web 打包为 `*.tar.gz` 进 Release 产物；
   - 增加 `migrate` 二进制与迁移一致性校验（goose 版本表与 migrations 目录 diff）。
3. **部署标准化**：统一 systemd 单元参数、日志路径、二进制权限（`chmod +x` 写入 install.sh）、`MemoryMax`；SCP 热修复只作为临时手段，正式变更一律走 CI。
4. **环境标准化**：`.env.example` 补全 `YUNDU_PAYLOAD_KEY`、`XRAY_BINARY`、`SINGBOX_BINARY`、WARP 环境变量，生产禁止 `change-me` fallback。

### 6.2 文档标准化

1. 730 / 801 / 简化版方案 / 教程四份文档建立“状态头”：每条结论标注 `[代码基线 commit]` + `[验证状态: 已合入/生产验证/待部署]`；
2. 增加行号核验脚本（`Select-String` 或 grep）到 CI 或 pre-commit，文档 file:line 引用失效即告警；
3. 修复已知漂移：730 “全部根治”措辞、801 “注册门禁已落地”、两份 0 字节文档、README 端口/基线。

### 6.3 数据标准化

1. migration 全量走 `cmd/migrate` 或启动自动迁移（`MIGRATE_ON_START`），不再手工 `psql -f` + 手工注册 goose；
2. `config_versions` 保留策略（每 runtime 最近 N 条 + 定期清理）由服务实现，替代文档 SQL 手工清理；
3. runtime 唯一性、dispatch 矛盾态、hash churn 三类检查全部落为 Prometheus 指标 + Grafana 告警。

---

## 七、零 SSH 终局路线图

### 7.1 现状盘点：还剩哪些人工 SSH 写操作

| 操作 | 当前方式 | 801 后状态 |
|---|---|---|
| 节点配置变更 | admin-web → 面板自动下发 | ✅ 已零 SSH |
| 证书签发/续期 | Agent certmagic 自动 | ✅ 已零 SSH |
| WARP 注册/出口 | warpreg + Agent 自动 | ✅ 已零 SSH |
| nginx/cloudflared | Reconciler 自动 | ✅ 已零 SSH |
| node-agent 升级 | SCP + systemctl（801 §6.4） | ❌ 唯一节点端人工 SSH 写操作 |
| 面板二进制升级 | install.sh upgrade panel（SSH 执行） | ❌ 面板端人工操作 |
| DB migration | SSH + docker exec psql + 手工注册 goose | ❌ 人工操作 |
| 前端发布 | 本地 build + SCP + 宝塔目录替换 | ❌ 人工操作 |
| 诊断 | SSH 只读日志/SQL | 🔶 已部分可替代（logStore + admin 日志 API） |

### 7.2 Agent 升级闭环（优先级最高，代码骨架已存在）

现状（已核实）：
- Agent `selfUpgrader` 每 5 分钟轮询面板 `/api/v1/agent/upgrade/check`，心跳也能即时下发 `HEARTBEAT_ACTION_UPGRADE`；
- 面板已有 `UpgradeCheck`、`DownloadAgentBinary`、`BinarySpecService`；
- 唯一缺口：面板上的版本源是 `/opt/yundu/agent-upgrade/info.json` + 二进制文件，**如何被更新**。

补齐方案（三选一，推荐 1+3 组合）：

1. **CI 自动同步**：release.yml 增加 job，发布后把 `node-agent-{amd64,arm64}` 与 `info.json` 推送到面板服务器，或上传到 GitHub Release 后由面板 admin API 拉取；
2. **面板管理 API**：admin-web 增加“Agent 版本库”页，上传二进制 + 填版本/SHA256/download_url，服务端原子写 info.json（校验 SHA256 后替换），全程无需登录服务器；
3. **download_url 直连 GitHub Release**：info.json 的 download_url 直接指向 Release 资产，面板只维护一个小 JSON，节点从 GitHub 下载。

闭环效果：面板发版 → 心跳通知 → Agent selfUpgrader 下载 + SHA256 校验 + 原子替换 + systemd 拉起 → 新版本注册。**节点端实现完全零 SSH**。

### 7.3 面板升级闭环

面板侧 5 个 Go 服务可复用 Agent 的 selfUpgrader 模式：

- 增加面板自升级组件：`install.sh upgrade panel` 的逻辑内建到 yunductl 或新增 `panel-upgrade` 服务；管理员在 admin-web 触发“升级面板”，服务从 GitHub Release 下载新二进制、校验、原子替换并 `os.Exit(0)` 由 systemd 拉起；
- 先做 node-service 自升级（改动最小、收益最大），再推广到其余服务；
- migration 随启动执行（`MIGRATE_ON_START` 或升级流程内先跑 migrate）。

### 7.4 migration 零 SSH

1. 生产环境用 `cmd/migrate`（已存在）替代手工 `psql -f`；`goose_db_version` 由工具维护；
2. 服务启动前自动检查 pending migrations（node-service 提供 `MIGRATE_ON_START=true` 选项，生产 systemd 单元默认开启）；
3. 面板 admin API 暴露“迁移状态/执行迁移”只读+受控接口，作为人工 SSH 的最终替代。

### 7.5 前端零 SSH

1. CI 前端 job 产出 `admin-web-dist.tar.gz` / `user-web-dist.tar.gz` 进 Release；
2. `install.sh upgrade panel` 自动检测 nginx 真实 root（读 `nginx -T` 或面板配置），备份 → 替换 → 改属主；
3. 回滚用时间戳备份目录，与教程 §11.6 一致。

### 7.6 诊断零 SSH

已有基础：node-service `LogStore` + `GET /admin/servers/:id/logs`、健康仪表盘、AI 诊断。
补齐：

1. 把 801 §2.3 的 SQL 脚本 A/B/C/D 封装为只读 admin API（`/admin/health/checks`）；
2. admin-web“健康仪表盘”页面展示：dispatch 总览、runtime 唯一性、10 分钟 CV 数、failed TTL 状态，一键刷新；
3. 监控告警（P1 指标）直接推送到面板通知中心，替代“等用户报障”。

### 7.7 零 SSH 验收标准

```text
1. 新节点接入：仅执行一次 install.sh agent（bootstrap），此后所有操作走面板；
2. node-agent 升级：面板发版 → 自动升级，无人登录节点；
3. 面板升级：admin-web 一键触发，无人登录面板服务器；
4. migration：随启动自动执行，无人手工 psql；
5. 前端发布：随 Release 自动部署到正确站点目录；
6. 诊断：面板健康仪表盘覆盖 801 Checklist 全部检查项；
7. SSH 仅保留“只读诊断 + 首次 bootstrap”两个边界，无任何日常写操作。
```

---

## 八、结论

1. **全貌判定**：730 体检报告覆盖面最广、最接近项目全貌；801 指南是运维权威；教程是操作权威；**我的独立分析负责纠正它们的“已根治”过度乐观与文档漂移**。
2. **方案回退**：不要回退 801 简化版整体；先提交工作区形成基线，再发布修正版。若必须回退二进制，务必先处理 migration 70 与 provider_ref 语义，否则旧二进制会注册失败。
3. **解决路径**：按第五章 P0–P2 修正三通道自愈分叉、ack 模糊匹配、兜底矛盾态与排序契约；把“7 类根因全根治”的结论从文档层提升为代码层 + 指标层可验证。
4. **标准化**：CI 纳入测试/前端/migration 校验，文档带状态头与行号校验，生产部署全部可回溯到 Git 基线。
5. **零 SSH**：Agent 升级闭环的代码骨架已存在，补“版本库更新入口”即可先实现节点端零 SSH；随后做面板自升级、migration 自动执行、前端 CI 部署与诊断仪表盘，最终把 SSH 收敛为“首次 bootstrap + 只读诊断”两个边界。

---

*本文档基于 2026-08-01 `yundu-src` 工作区与三份技术指导文档交叉核对；引用行号以当前工作区为准，合入后需按新基线复核。*
