# YunDu 支付配置与 BEP20 可用性修复报告

日期：2026-08-05
范围：identity-service（支付后端）+ user-web / admin-web（前端）
依据文档：《USDT支付链路检查与中间件选型分析-20260802.md》

---

## 一、USDT-BEP20 是否真实可用（结论）

### 结论：**可以做到自动到账，但必须换 BSC RPC 节点**

- **下单**：配置收款地址后即可下单（已配置 `0x92c08f0e24558858778b1E34910e4CE948A6CDFF`，实测成功）。
- **自动到账（原不可用）**：默认三个 BSC 公共 RPC（bsc-dataseed.binance.org 等）对 `eth_getLogs` 全部返回
  `-32005 limit exceeded`；BscScan V1 已弃用、Etherscan V2 免费计划不支持 BSC。
- **突破点**：BEP20 生产查询本来就按“收款地址”做 topics 过滤（Transfer + to=地址），
  实测 `https://bsc-mainnet.nodereal.io/v1/64a9df0874fb4a93b9d0a3849de012d3` **可在大范围（13000+ 区块）正常查询**。
  - 之前误判是因为测试时未带 topics 过滤，USDT 合约全量日志必然超 5 万条上限。
- **修复**：BEP20 默认 RPC 列表加入 NodeReal 节点（放首位），并在后台支持编辑 `rpc_nodes`；
  实测重启后 BSC 轮询不再报错（NodeReal 成功，仅出错才打日志）。
- **建议**：NodeReal 公共 key 长期可能限流，若发现 BEP20 又开始报错，请在后台支付配置里换成
  自建/付费 BSC RPC（如 Ankr/GetBlock 专属端点）。

## 二、EVM 双通道设计（Polygon + Arbitrum One，无 ERC）

按 08-02 文档与要求，EVM 统一为 **Polygon / Arbitrum One 双通道，共用同一收款地址**
`0x92c08f0e24558858778b1E34910e4CE948A6CDFF`，不再使用 “USDT-ERC20” 命名：

| 通道 | 内部 method | 网络 | 状态 |
|---|---|---|---|
| USDT（Polygon） | usdt_erc20 + network=polygon | Polygon (chainid 137) | ✅ 可用（Etherscan V2 key 已配） |
| USDT（Arbitrum One） | usdt_erc20 + network=arbitrum | Arbitrum One (42161) | ✅ 可用 |
| USDT-TRC20 | usdt_trc20 | Tron | 可选（当前 disabled） |
| USDT-BEP20 | usdt_bep20 | BSC (56) | ✅ 已修复自动到账（NodeReal RPC） |

- 后端/前端名称统一为 “USDT-EVM（Polygon / Arbitrum One 双通道）”；用户端显示 “USDT” + 网络二选一。
- 修复 ERC20/EVM 地址**前导空格**问题（` 0x92c08...` → `0x92c08...`），否则 Etherscan 返回
  “Invalid address format” 且链上 `to` 匹配永远失败。地址加载统一 TrimSpace。

## 三、支付配置“全部启用但 user-web 支付页面不完整”修复

根因与修复：
1. **支付宝/微信**：原先只要 `enabled=true` 就显示给用户，但易支付未配置（缺 key）时下单直接 500，
   前端又无错误提示 → “点确认支付没有响应”。
   - 用户端 `payment-methods` 仅返回**真实可用**方式：支付宝/微信需 `enabled && epay.Configured()`
     （pid+key+网关三者齐全）。
   - 易支付网关由 `submit.php`（返回 HTML/文本）改为 `mapi.php`（JSON API）；
     响应兼容 `code/msg/message/data/redirect` 多种格式；非 JSON 文本错误透传。
   - `notify_url/return_url` 未配置时按 `AGENT_PANEL_ENDPOINT` 自动补齐
     （`https://6.tiktokplay.na.am/api/v1/payment/notify/alipay|wechat`），修复支付成功无法回调激活。
   - 网关失败返回 **503 + 友好提示**“支付通道暂不可用…”，前端结算弹窗 toast 显示“下单失败 + 原因”。
2. **USDT 地址为空**：启用但无收款地址时用户端不显示；admin 卡片提示“已启用但未配置收款地址”。
3. **admin 卡片**：支付宝/微信显示“已启用但易支付未配置”；BEP20 显示 RPC 依赖提示并支持编辑 RPC。

## 四、结算界面提示（已加到所有 USDT 通道）

user-web 结算页（Checkout）选择任意 USDT 通道（TRC20 / BEP20 / EVM）时显示：
> ⚠️ 请勿选错网络，USDT手续费极低真实可用，优惠码见系统公告

订单详情页同样对所有 USDT 订单显示“请勿选错网络”提示（按订单网络：TRC20 / BEP20 / Polygon / Arbitrum One）。

## 五、部署与验证记录

| 项 | 结果 |
|---|---|
| 代码提交 | `b7c5ece`（支付可用性/易支付/BEP20 RPC/提示）、`2418792`（epay 错误透传）、`31f0388`（user-web 下单错误提示） |
| 二进制 | identity-service 重建部署 vps190（备份 `.bak.payfix*`），SHA 校验一致 |
| 前端 | user-web（7.tiktokplay.na.am）、admin-web（6.tiktokplay.na.am）重建部署 |
| 生产配置 | bep20 地址=RPC 更新；erc20 地址 trim；易支付 key 已由运营配置 |
| 实测 | polygon/arbitrum/bep20 下单全部 OK；BSC 轮询（NodeReal）无报错；EVM 无 Invalid address |
| 支付宝/微信 | pid/key/网关校验通过，但**平台无可用收款账号**（`没有找到可用支付账号`）→ 需在易支付平台配置收款通道 |

## 六、待办（运营侧）

1. **易支付平台**：在 m.jiuwufu.com 后台为商户 10758 配置“支付宝/微信收款账号（通道）”，
   配置后支付宝/微信下单即可成功；否则用户端会显示“支付通道暂不可用”。
2. **BEP20 RPC 长期可用性**：观察 NodeReal 公共端点限流情况；必要时在后台换成自建/付费 BSC RPC。
3. **TRC20**：如需开放 TRC20，后台启用并确认收款地址（TLAoiTwPNCtFXpJWmPrvgm8tRpC9ggP42H）。

---

## 七、易支付第三方可配置化（换第三方无需 SSH/改代码）

现状与改进（提交 `531116c`）：

| 配置项 | 存储 | 是否后台可改 |
|---|---|---|
| 商户ID pid / 商户密钥 key | system_settings | ✅ |
| 网关地址 gateway_url | system_settings | ✅ |
| 下单类型 pay_type | system_settings | ✅ |
| 通知地址 notify_url / 回跳 return_url | system_settings | ✅（未填自动按 AGENT_PANEL_ENDPOINT 补齐） |
| JSON下单接口 mapi_path（默认 mapi.php） | system_settings | ✅ 新增 |
| 表单下单接口 submit_path（默认 submit.php） | system_settings | ✅ 新增 |
| 查单接口 query_path（默认 api.php） | system_settings | ✅ 新增 |

**自动兼容**：下单先调 mapi（JSON）；若平台无 mapi 或返回 HTML/纯文本，自动回退 submit 并解析
`location.href` / `<form action>` / 首个 http(s) 链接作为支付跳转地址——即使非标准彩虹协议平台也能用。

**换第三方运维方式**：管理后台 → 支付配置 → 支付宝/微信 → 编辑，只改 `pid / 密钥 / 网关地址`
（非标准平台再改接口路径）即可，保存即生效，无需 SSH、无需发版。

---

## 八、微信支付确认（与支付宝同等处理）

- 微信与支付宝走同一套易支付网关逻辑：enabled && epay.Configured() 才展示给用户；
  notify_url/return_url 未填自动按面板补齐（`/api/v1/payment/notify/wechat`）；
  下单失败返回 503 友好提示；前端结算弹窗显示原因。
- pay_type 已显式写入：alipay=`alipay`、wechat=`wxpay`（平台实测只认这两种类型）。
- 平台侧：m.jiuwufu.com 商户 10758 对 `alipay`/`wxpay` 均返回“没有找到可用支付账号”，
  即平台未给该商户配置支付宝/微信收款通道 —— 需在平台商户后台添加收款账号后即可支付，系统零改动。
