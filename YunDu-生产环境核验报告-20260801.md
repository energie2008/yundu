# YunDu 生产环境核验报告（只读巡检）

> 核验时间：2026-08-01 11:57–12:05（Asia/Shanghai）
> 核验方式：SSH 只读命令 + PostgreSQL 只读 SQL（无任何修改）
> 环境：vps190（面板）/ vps206 / vps81（节点）
> 对照基线：`D:\机场搭建\yundu-src`（`9c47780` + 工作区未提交改动）

**域名更正（用户确认）**：

- admin-web：`https://6.tiktokplay.na.am/`
- user-web：`https://7.tiktokplay.na.am/`
- `6.tiktokplay.na.am` 开启小黄云（Cloudflare 代理），因此只暴露 80/443，控制面端口（9082）无法穿透。

---

## 一、连接信息与密钥核对

| 主机 | IP | 用户 | 实际密钥文件 | 连通 |
|---|---|---|---|---|
| vps190（面板） | 43.135.147.190 | root | `D:\机场搭建\key\190key.pem` | ✅ |
| vps206（节点） | 158.101.13.206 | ubuntu | `D:\机场搭建\key\206.key` | ✅ |
| vps81（节点） | 132.226.156.81 | ubuntu | `D:\机场搭建\key\81.key` | ✅ |

注意：实际密钥名是 `206.key` / `81.key`（不是 `206key` / `81key`）。

### 1.1 域名与前端在线核验

| 域名 | DNS | 边缘 | HTTP 状态 | 说明 |
|---|---|---|---|---|
| 6.tiktokplay.na.am | 172.67.223.185 / 104.21.86.186（CF） | `server: cloudflare` | 200 | admin-web 在线，cf-cache-status DYNAMIC |
| 7.tiktokplay.na.am | 172.67.223.185 / 104.21.86.186（CF） | `server: cloudflare` | 200 | user-web 在线，cf-cache-status DYNAMIC |

实测两个域名都解析到 Cloudflare 并被代理，不仅 6 走 CDN，7 同样走 CDN。因此**任何 Agent 指向 `*.tiktokplay.na.am:<非443端口>` 的控制面通道都无法穿透 CF**，这是 gRPC 9082 不可达的直接原因。

---

## 二、核验结果

### 2.1 面板 vps190

| 检查项 | 结果 |
|---|---|
| node-service 运行状态 | `active (running)`，二进制 62,376,000 字节，构建时间 08-01 09:50 |
| 801 修复符号 | ✅ `SelfHealFailedDispatch`、`dispatchAndPush`、`ReconcileDispatchStatusOnHeartbeat` 均在二进制中 |
| 二进制备份 | ✅ `node-service.bak.801`（08-01 09:50，62,346,410 字节）等多份备份存在 |
| gRPC server | `:9082` 监听，`tls=false`（明文） |
| HTTP API | `:8082` 监听 |
| goose 迁移 | ✅ `version_id=70, is_applied=t` |
| runtimes 唯一性 | ✅ 每台 VPS 恰好 2 条（sing-box + xray），`provider_ref` 全非 NULL |
| agent_version | vps081=0.7.13 / vps206=0.7.13 / vps190=0.7.9 |
| 10 分钟 config_versions 增长 | 0（无版本飙升） |
| failed 节点 | 0 |
| 自愈日志 | `ReconcileDispatchStatusOnHeartbeat` 实际触发过：vps081 在 02:47/04:10/04:25/06:27 多次清除 stale failed（affected 3–4 节点，版本 42→45） |
| Agent 升级版本源 | `/opt/yundu/agent-upgrade/` 有二进制与 info.json 历史备份，但**当前无生效 info.json**（升级通道处于关闭状态） |

### 2.2 节点 vps206

| 检查项 | 结果 |
|---|---|
| node-agent | `active`，版本 `0.7.13`，version.txt=`363` |
| 主内核 | ✅ `sing-box started successfully (primary)`（08-01 09:56 应用 v363） |
| 辅内核 | ✅ `native xray started successfully`，xray gRPC `127.0.0.1:10085` 监听 |
| 通信通道 | `channel=ws, healthy`，心跳 10s 正常；日志同时存在 HTTP `POST /api/v1/agent/heartbeat` |
| 流量上报 | ✅ `traffic reported` 正常 |
| WARP | ✅ wireguard keepalive 正常（peer 为 CF WARP 公钥） |
| 自升级检查 | 每 5 分钟 `self-upgrader: no update`（面板无 info.json 所致） |

### 2.3 节点 vps81

| 检查项 | 结果 |
|---|---|
| node-agent | `active`，版本 `0.7.13`，version.txt=`45` |
| 主内核 | ✅ `sing-box started successfully (primary)`（07-31 23:47 应用 v45） |
| 辅内核 | ✅ `native xray started successfully`，xray gRPC `127.0.0.1:10085`、tunnel `127.0.0.1:20530` 监听 |
| 通信通道 | ✅ `channel=ws, healthy` |
| gRPC 通道 | ❌ 持续重连失败：`dial tcp [2606:4700:3036::6815:56ba]:9082: i/o timeout`，约每 50 秒重试一次 |
| 节点配置 | `PANEL_URL=https://6.tiktokplay.na.am`、`RUNTIME_TYPE=sing-box`、`YUNDU_NATIVE_RUNTIME=true` |

---

## 三、与文档/代码基线的一致性判定

### 3.1 与 801 维护指南一致 ✅

- 24/24 节点 `pushed`、0 failed、10 分钟 CV=0；
- 每台 VPS 恰好 2 条 runtime，provider_ref 全非 NULL；
- migration 70 已注册；
- agent_version v0.7.13 正确；
- node-service 二进制含 801 修复符号。

生产现状与 801 文档 §3.9 部署验证记录完全吻合。

### 3.2 与本地代码基线不一致 ⚠️

- 生产二进制包含的 801 修复（`dispatchAndPush` / `SelfHealFailedDispatch` / `ORDER BY priority, id` / provider_ref 回退等）只存在于**工作区未提交改动**；
- GitHub main（`9c47780`）编译不出生产当前行为；
- 生产二进制是否包含“B-精确 ack 回写”与“A-时序”需进一步比对（当前 strings 已确认核心符号，行级差异无法从二进制直接判定）。

---

## 四、现场新发现（此前文档未记录）

### 4.1 gRPC 通道在生产实际不可用（vps81）

面板 `9082` 监听公网，但 `6.tiktokplay.na.am` 开启小黄云（Cloudflare 代理），CF 只开放 80/443，不转发 9082。Agent 的默认 gRPC 地址为 `PANEL_URL:9082`，因此：

- vps81 每 ~50s 尝试 gRPC 重连并超时，产生持续日志噪声与资源浪费；
- 三通道实际形态是 **WS 主通道 + HTTP 兜底**，gRPC 名义优先但从未建立；
- 影响：gRPC onMessage 的 ack/心跳路径不生效，**WS `handleWSConfigResult`（nil-ref 解析 runtime）才是生产实际 ack 路径**，冲突精读中的“ack 可能挂错 runtime”在生产是现实风险而非理论问题。
- 后续可选方案：控制面通道改走 CF 可穿透的 443（`wss://` / HTTP 兜底），或为控制面使用独立非 CDN 域名/直连 IP；`7.tiktokplay.na.am` 同样在 CF 后，不能作为 gRPC 替代入口。

### 4.2 生产自愈实际由 WS/gRPC 的 Reconcile 语义主导

面板日志显示只有 `ReconcileDispatchStatusOnHeartbeat`（版本一致+运行中 → failed 刷 applied）触发，未发现 HTTP `SelfHealFailedDispatch`（failed 超 5 分钟 → pushed）触发记录：

- 实证三通道自愈语义分叉：WS 每 10s 就把 failed 刷成 applied，HTTP 的 5 分钟 TTL 自愈永远轮不到；
- 若 Agent 降级到 HTTP 通道，同一 failed 会走“刷 pushed 重新拉取”的语义，状态会横跳。

### 4.3 面板 gRPC 为明文（tls=false）

`:9082` 监听公网且 `tls=false`。Agent 与面板跨公网通信无传输加密（认证仍有 HMAC/Bearer，但存在中间人风险）。这是“通道 TLS 默认 insecure”风险的生产实锤。

### 4.4 Agent 自升级版本源关闭

`/opt/yundu/agent-upgrade/info.json` 当前不存在（只有 `.bak` / `.disabled`），导致两个节点 self-upgrader 每 5 分钟得到 “no update”：

- 零 SSH 闭环缺少的“版本库更新入口”现场确认；
- 当前 Agent 升级仍只能走 SCP 人工替换。

### 4.5 vps190 面板机上存在 node-agent 0.7.9

面板服务器也有 node-agent（0.7.9），低于节点端 0.7.13。若面板机不需要运行 Agent，建议卸载或说明用途，避免版本巡检误判。

---

## 五、结论与建议

1. **生产健康度：优**。核心链路（双内核、下发、自愈、WARP、流量）全部在跑，24/24 pushed、0 failed、版本稳定。
2. **第一优先**：提交工作区 801 修复并打 tag，让生产二进制可复现；同时把修正版（三通道统一自愈、ack 精确 runtime、`_dispatch_time`、去兜底标记）合入。
3. **gRPC 通道二选一**：由于 6/7 域名都在 CF 后且只开放 80/443，要么将 Agent 控制面通道改为 CF 可穿透的 443（`wss://` + TLS），要么为控制面配置独立非 CDN 域名或直连 IP；当前持续重连失败至少应加“不可达即停”退避。
4. **零 SSH 推进**：恢复/重建 `/opt/yundu/agent-upgrade/info.json` 管理入口（面板 API 或 CI 同步），两个节点即可走 selfUpgrader 自动升级。
5. **安全**：为 9082 gRPC 配 TLS（现有代码已支持证书路径），或限制仅内网/IP 白名单访问。

---

*本报告全部为只读操作，未修改任何生产文件、配置或数据。*
