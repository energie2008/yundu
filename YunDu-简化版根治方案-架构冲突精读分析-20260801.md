# YunDu 简化版根治方案 × 当前架构：冲突精读分析

> 精读文档：`D:\机场搭建\技术指导\YunDu-架构缺陷根治方案-简化版-最小改动零SSH-20260801.md`（550 行）
> 对照代码：`D:\机场搭建\yundu-src`，Git 基线 `9c47780` + 工作区未提交改动
> 日期：2026-08-01

---

## 一、先确认一个关键前提：方案已经“在代码里”，但还没有“在仓库里”

精读前先核对 Git 状态：**这份方案的 A/B/C/D 大部分改动只存在于工作区未提交状态**，并未合入 `main`：

```text
M .github/workflows/release.yml
M apps/node-service/internal/handler/agent_handler.go
M apps/node-service/internal/repo/node_repo.go
M apps/node-service/internal/service/deployment_service.go
M apps/node-service/internal/service/health_service.go
M apps/node-service/internal/service/kernel_render_adapter.go
M apps/node-service/internal/service/runtime_service.go
```

文档声称“代码基准 main 含 731/801 四项稳定性修复”，但实际：

- `health_service.go` 的 A-时序修复（agent_version 无条件更新）在未提交差异中；
- `node_repo.go` 的 `ORDER BY priority ASC, id ASC` 在未提交差异中；
- `kernel_render_adapter.go` 的入口 `sort.Slice` 在未提交差异中（**文档说“保留现有”是误述，它其实是本方案新加的**）；
- `deployment_service.go` 的 `dispatchAndPush`、`SelfHealFailedDispatch`、`injectXrayConfig` 返回节点列表、`ReportConfigResult` B-精确，都在未提交差异中；
- `runtime_service.go` 的 provider_ref 回退 + Create 失败重查，在未提交差异中。

结论：方案本身与“当前架构”不是完全冲突，但**方案的实现尚未成为可发布基线**，任何基于该方案做验收的结论目前都不可复现。

---

## 二、与“现在架构”冲突的 10 个点（按严重度排序）

### 冲突 1（严重）：ack 回写用 nil-ref 模糊匹配 runtime，双内核下会更新错节点

现状代码事实：

- `apps/node-service/internal/handler/ws_handler.go` `handleWSConfigResult`：
  `runtimeService.GetRuntimeByServerAndProvider(ctx, serverSrv.ID, providerType, nil)`。
- `apps/node-service/internal/app/app.go` gRPC `onMessage` 的 ConfigResult 分支：同样传 `nil`。
- 仅 HTTP 路径 `agent_handler.go` `ReportConfigResult` 支持 `X-Runtime-Ref` 头。
- `runtime_service.go` `GetRuntimeByServerAndProvider` 的 nil 回退逻辑：**“未指定 ref 时优先返回 xray runtime，其次首个 node-agent runtime”**。

当前架构是双内核：一台 server 正常有 sing-box（主）与 xray（辅）两个 runtime。sing-box 节点走 WS 或 gRPC 上报 ConfigResult 时，nil 回退会解析到 **xray runtime**，随后 `ReportConfigResult` 用 `ListByRuntimeID(xrayRT.ID)` 查节点：

1. sing-box 节点不在 xray runtime 的节点列表里；
2. B-精确过滤 `lpv == versionNo` 后 `targetNodes` 为空；
3. 代码兜底 `targetNodes = nodes`，把 **xray runtime 的全部节点**标记为 applied / 推进 deployment target。

也就是说：方案 B 的“ack 闭环”在双内核 + 三通道架构下，可能把 sing-box 的 ack 记到 xray 节点头上。心跳三通道已经用 `kernelRuntimeRef`（kernel 类型推导 sing-box/xray）做精确匹配（代码注释明确“避免 nil-ref 回退误选到 xray runtime”），但 **ConfigResult 三通道没有对齐这个契约**。这是方案 B 与双内核架构最直接的冲突。

### 冲突 2（严重）：自愈语义在三通道分叉，状态会双向震荡

同一“failed 下发状态”，三条通道的收敛方向不同：

| 通道 | 函数 | 触发条件 | 收敛方向 |
|---|---|---|---|
| HTTP | `SelfHealFailedDispatch`（未提交） | failed 且 `nodes.updated_at > 5min` | 刷成 **pushed**（重新拉取配置） |
| WS | `ReconcileDispatchStatusOnHeartbeat`（已提交） | action==NONE 且 kernel running | 刷成 **applied**（不重新拉取） |
| gRPC | `ReconcileDispatchStatusOnHeartbeat`（已提交） | 同上 | 刷成 **applied** |

冲突后果：

- Agent 在 gRPC/WS 主通道与 HTTP 兜底通道间切换时，failed 可能先被刷 applied、又被刷 pushed，或反过来，面板状态反复横跳；
- HTTP 的 pushed 会诱导 Agent 重新拉取一次配置，制造不必要的重部署；
- 两个自愈函数判断标准不一致：`Reconcile` 要求“版本一致 + 内核运行中”，`SelfHeal` 只要求“failed 超 5 分钟”，不看版本是否一致。

方案 D 说“复用心跳周期、零额外 goroutine”，这在单通道时代成立；三通道时代变成**三份各自维护的自愈逻辑**，违反方案自己提倡的“收口”。

### 冲突 3（高）：SelfHealFailedDispatch 用 `updated_at` 判陈旧，而状态时间戳明明存在 `_dispatch_time`

- `node_repo.go` `UpdateDispatchStatus` 每次都会写 `metadata._dispatch_time`（timestamptz）；
- 但 `SelfHealFailedDispatch` 判断“failed 超 5 分钟”用的是 `n.UpdatedAt`。

`nodes.updated_at` 会被**任意节点编辑/其他更新**刷新。一个 failed 状态可能因为用户改了节点名、改了限速，导致 updated_at 不断刷新，5 分钟自愈永远不触发；反之一个刚 failed 的节点若 updated_at 恰好在 5 分钟前（例如导入数据），会被误判为陈旧。既然方案 B 已经把状态收口到 `_dispatch_*` 字段，时间判定也应使用 `_dispatch_time`。

### 冲突 4（高）：ReportConfigResult 的“兜底标记全部”会制造矛盾态

未提交的 B-精确实现：

```go
if len(targetNodes) == 0 {
    targetNodes = nodes   // 兜底：全部标记
}
```

当 ack 版本与所有节点 `lpv` 都不匹配（延迟 ack、版本跳变、冲突 1 解析错 runtime）时，兜底会把一个旧版本号 applied 写到所有节点，产生：

```text
last_published_version = V2（或更高）
_dispatch_status      = applied
_dispatch_version     = V1（旧版本）
```

这恰恰制造了方案自己定义的“矛盾态”（lpv 与 dispatch_version 不一致），而且比“failed 卡住”更难排查。方案 B 的本意是“只标记当前版本”，兜底逻辑与其目标自相矛盾。

### 冲突 5（高）：三写非原子，节点级状态与部署批次状态仍然分裂

`ReportConfigResult` 一次回执同时写三处：

1. `config_versions` 的 applied 标记；
2. `nodes.metadata._dispatch_*`（逐节点单条 UPDATE，非批量事务）；
3. `deployment_targets` 状态推进（每节点循环查询 + 更新）。

方案 B 的“不建新表”只收口了 nodes.metadata 一条线，**没有把 deployment_targets 纳入同一状态机**。节点显示 applied 而批次 target 仍 pending/applying 的跨表不一致，方案没有覆盖，也解释了此前“部署卡在部署中”的同类问题反复出现。

### 冲突 6（中高）：A 方案的 SQL 与真实表结构不符

文档建议：

```sql
CREATE UNIQUE INDEX ... ON runtimes (server_id, provider_type, provider_ref)
WHERE deleted_at IS NULL AND provider_ref IS NOT NULL;
```

真实表结构（`migrations/000004_node_domain.sql`）：

- `runtimes` **没有 `deleted_at` 列**（硬删除，ON DELETE CASCADE），文档 SQL 直接执行会报列不存在；
- 表已有 `UNIQUE (server_id, runtime_type, provider_ref)`；
- 实际落地的是 `migrations/000070`：`uq_runtimes_server_type_null_ref (server_id, runtime_type) WHERE provider_ref IS NULL`，配合回填 NULL。

结论：文档对表结构的假设过时，真实实施（000070）比文档方案更准确——它只补了 NULL 的洞，而文档却建议再建一个语义不同、会误挡“同 provider_type 多 runtime”的约束。真正要补的是“非 NULL 唯一已有原约束兜底 + 并发注册时的错误处理”，而不是照抄文档 SQL。

### 冲突 7（中高）：Create 失败被无条件吞掉，可能返回错误 runtime

未提交 `runtime_service.go`：

```go
if err := s.runtimeRepo.Create(ctx, runtime); err != nil {
    existing, qerr := s.runtimeRepo.GetByServerAndProvider(ctx, server.ID, providerType, providerRef)
    if qerr == nil && existing != nil {
        return existing, nil
    }
    return nil, err
}
```

任何 Create 失败（不仅是 23505 唯一约束冲突，还包括 DB 断连、约束外错误）都会先重查一次。若此时恰好存在一条同 server/provider 的旧 runtime，就会把“注册失败”伪装成“复用成功”，返回一个可能与本次声明的 runtime_type 不同的 runtime。正确做法是仅对 PostgreSQL `unique_violation`（SQLSTATE 23505）走重查分支。

### 冲突 8（中）：C 方案是两套不一致的排序契约，priority 语义被渲染层覆盖

当前实际：

- DB 层（未提交）：`ORDER BY priority ASC, id ASC`；
- 渲染层（未提交新增）：`buildConfigViaKernelRender` 入口 `sort.Slice(nodes, by ID)`；
- 更早的已提交代码：`buildRuntimeConfig` 也按 ID 排序。

问题：

1. **两个排序键不一致**：DB 先 priority 后 id，渲染层直接按 id 重排，priority 不再决定 inbound/outbound 顺序。文档说“DB tiebreaker + 渲染层 sort 双保险”，实际是两套顺序契约，谈不上“双保险”；
2. **文档误称 sort “已存在”**：`kernel_render_adapter.go` 的 sort 是本次未提交新增；
3. **“扩展 sort 到其他渲染入口”未实施**：
   - `node_repo.go` `ListByServerID` 仍 `ORDER BY n.priority ASC`（无 tiebreaker，用于 BuildNginxVhostsForServer）；
   - `node_repo.go` 另一处 `ORDER BY n.is_enabled DESC, n.priority ASC`（无 tiebreaker）；
   - `chain_repo.go` `node_chain_bindings` 仍 `ORDER BY priority ASC`（无 tiebreaker）；
   - `runtime_repo.go` `ListByServer` 仍 `ORDER BY created_at DESC`（无 tiebreaker，`injectXrayConfig` 依赖该顺序挑选 xray runtime）。

因此“版本号死循环全局根治”只覆盖了 `ListByRuntimeID` 一条链，其他会进入渲染/绑定/hash 路径的查询仍是潜在漂移源。

### 冲突 9（中）：D 的注册门禁没有落地，Agent 版本字段来源也未对齐

- `MinAgentVersionForP2`（deployment_service.go）只在 `pushConfigToRuntime` 推送时检查；
- 注册路径（`agent_handler.Register` → `runtime_service.RegisterRuntime`）既无强制拒绝，也无观察 warning——方案 D 改动 2 未实施；
- Agent 侧 `client.go RegisterRequest.AgentVersion` 的 JSON tag 是 `json:"-"`，真实版本号塞在 `Metadata["agent_version"]` 里；服务端 `RegisterRuntimeRequest.AgentVersion` 顶层字段在 HTTP 注册时实际拿不到。

所以“低版本 Agent 注册即暴露”目前仍是空话，版本过低依然要等到心跳+推送才失败，属半治标。

### 冲突 10（低）：文档边界声明已过时

文档第十节声明“selfUpgrader 对接面板版本源是独立 P0 项”，但当前代码已经存在：

- Agent：`main.go` 与 `machine.go` 的 `UpdateURL` 均指向面板 `/api/v1/agent/upgrade/check`；
- 面板：`agent_handler.go` `UpgradeCheck` + `BinarySpecService`，`app.go` 已注册该路由。

另外文档说“零 SSH、Agent 零改动”，但未提交的 `release.yml` 正在改 Agent 构建标签（新增 `with_grpc`、`with_clash_api`），说明 Agent 的发布形态也在变，文档与发布状态不同步。

---

## 三、冲突的本质：方案假设停在“单通道 + 单 runtime + 节点级状态”，架构已演进

简化版方案的三条定律（不改协议、不建表、不建组件）在 7-31 的代码形态下成立；到 8-01 的代码形态，架构已引入：

| 架构演进 | 方案的隐含假设 | 冲突 |
|---|---|---|
| 双内核 runtime（sing-box 主 + xray 辅） | 一台 server 一个 runtime，nil 匹配安全 | ack/注册/回退解析可能选错 runtime |
| 三通道（gRPC/WS/HTTP）心跳各自实现 | 一个心跳入口，内联自愈天然唯一 | 自愈逻辑复制三份且语义分叉 |
| deployment_batches/deployment_targets | 节点级 metadata 就是完整状态 | 节点状态与批次状态跨表分裂 |
| config payload 加密 + Delta Sync | 状态版本号即 config_version | ack/版本判定只覆盖全量路径，Delta ack 未纳入 |
| 3X-UI provider 抽象（ADR-0001） | provider_ref 永远可从 runtime_type 推导 | 未来多 provider 共存时身份契约缺失 |

**结论：方案没有错，错在把它当成“可以在旧位置上打补丁”的收口。** 真正与现在架构冲突的是实现点，而不是目标：

- 状态收口目标（单一写入入口、ack 闭环、确定性排序、TTL 自愈）完全正确；
- 但实现点应挂在架构的抽象层（runtime 身份、通道无关的自愈服务、批次状态机），而不是挂在 HTTP handler 与 metadata JSON 上。

---

## 四、如何才是彻底根治（按依赖顺序）

### P0：把当前工作区修复合入并修正明显缺陷（1–2 天）

1. 提交并发布未合入的 A/B/C/D 实施，使“根治”成为可复现基线；
2. `SelfHealFailedDispatch` 改用 `metadata._dispatch_time` 判陈旧，去掉 `updated_at` 依赖；
3. `runtime_service` Create 失败仅对 `23505 unique_violation` 走重查，其他错误原样返回；
4. `ReportConfigResult` 删除“兜底标记全部”：ack 版本与节点 lpv 不匹配时只更新 runtime 级记录并告警，不写节点级 applied；
5. 三通道 ConfigResult 统一携带/推导 runtime ref（复用 `kernelRuntimeRef`），禁止 nil 回退解析 runtime；
6. 注册路径补观察门禁：从 `Metadata["agent_version"]` 取版本，低于阈值记 warning（观察 1 周后转拒绝）。

### P1：自愈与状态机收口到架构层（1 周）

1. 把 `SelfHealFailedDispatch` 与 `ReconcileDispatchStatusOnHeartbeat` 收敛为**一个** `DispatchSelfHealService`，三个通道只调它；语义统一为：
   - 版本一致 + 内核运行中 → failed 刷 applied（可信收敛）；
   - 版本不一致 + failed → 维持 failed 并触发 reload（真失败路径）；
   - 与 TTL 无关，避免通道间横跳。
2. `ReportConfigResult` 的 nodes / config_versions / deployment_targets 三处写入放入同一数据库事务，或在 nodes 更新失败时回滚批次推进；
3. `UpdateDispatchStatus` 改为批量单条 SQL（`UPDATE ... WHERE id = ANY($1)`），消除逐节点半更新；
4. 节点级状态与批次级状态建立不变式：`node.applied@V 且 target.version=V 时 target 必须 success`，并加指标告警。

### P2：确定性契约全量落地（1 周）

1. 统一排序键：所有渲染/绑定/哈希路径统一 `ORDER BY priority ASC, id ASC`（node、chain_hops、node_chain_bindings、runtimes、users、outbound_policies 等）；
2. 渲染入口保留与 DB **相同键**的 sort 或去掉重复 sort，二选一，避免两套契约；
3. 补 `config_render_hash_churn` 指标（文档删除它是过度简化）：同输入 hash 变化即告警，防止未来新查询漏排序时再次版本风暴；
4. 更新 `deployment_service.go:1570` 与 `kernel_render_adapter.go` 过时注释。

### P3：身份契约与升级闭环（2–4 周，可选但建议）

1. Agent 注册时把 `agent_version`、`runtime_ref`（sing-box/xray）提升为 HTTP RegisterRequest 的正式字段（改 JSON tag 即可，不改 proto），服务端注册即校验；
2. 未来引入 3X-UI/自定义 provider 时再进 proto：`provider_ref` 成为注册/心跳/ack 的必填身份，nil 回退分支退役；
3. Agent 自升级与 `upgrade/check` 版本源打通灰度：面板下发 canary 百分比、期望 SHA256，Agent 按规格升级，替换“人工 SCP”这条零 SSH 例外；
4. Delta Sync ack 纳入状态机：DeltaAck 成功也推进 `_dispatch_version`，避免增量路径与全量路径状态漂移。

### 彻底根治的验收标准（替代文档第八节的单表验收）

```text
1. runtimes：每 server + runtime_type + provider_ref 唯一，无 NULL ref（含并发注册压测）；
2. nodes：lpv / _dispatch_version / _dispatch_status 三字段一致，无矛盾态；
3. deployment_targets：与节点 applied 状态一致，无“节点 applied 批次 pending”；
4. 三通道任意切换：心跳、ack、自愈行为一致，无状态横跳；
5. 同一输入配置连续渲染 7 天 hash 不变（hash_churn 指标为 0）；
6. 低版本 Agent 注册即告警/拒绝，不再等到推送阶段；
7. 7 天零人工 SSH/SQL 修复下发问题。
```

---

## 五、一句话总结

简化版方案的方向正确、代价最小，但它的**实现假设（单 runtime、单通道、metadata 即全部状态）与 8-01 代码的双内核 + 三通道 + 批次部署架构已经脱节**。照当前未提交实现直接发布，会遇到 ack 挂错 runtime、三通道自愈互搏、批次状态分裂三个新问题。彻底根治不是“再修几个 bug”，而是把方案的四个收口从 HTTP handler / metadata JSON 提升到架构的抽象层：runtime 身份精确化、自愈单点化、状态写入事务化、排序契约全局化。

---

*分析基于 2026-08-01 `yundu-src` 工作区实际代码（含未提交改动），可对照 `git diff` 复核。*
