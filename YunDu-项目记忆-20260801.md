# YunDu 项目记忆（20260801 更新版）

> 定位：改造期关键结论与坑位存档，供后续开发/接手直接引用。
> 更新时间：2026-08-01（在原始项目记忆基础上补充 12:31 修正版部署与生产核验结果）
> 原始记忆来源：用户提供的改造记忆 + 三份技术指导文档 + 生产只读核验。

---

## 一、核心机制事实（已验证）

### 1.1 下发链路

- `GetRuntimeConfig` 三条路径：
  - path1：cv==nil，首次部署；
  - path2：hash 变化，生成新版本；
  - path3：hash 不变，复用旧版本。
- 801 修复前 path3 不更新 `dispatch_status`，agent 重启后 version=0 触发 re-fetch 时旧 `failed` 永不消除；修复后 path3 末尾 `markNodesDispatchStatus(allNodes, "pushed")` 兜底。
- 版本协商用 `!=` 而非 `<`：面板是唯一真相源，agent 版本无论高低都收敛到面板 latest（修复 DB 重建后 agent 版本号高于面板时永不 reload 的漂移）。

### 1.2 agent_version 竞态（已根治）

- 原问题：`server.Metadata["agent_version"]` 更新被 metrics 条件块包裹，agent 升级后首次心跳不带 metrics 时不更新，`pushConfigToRuntime` 读到旧版本（如 v0.3.5）误拒推送，但 agent 实际已升级（v0.7.13）。
- 根治：`health_service.go` 将 agent_version 更新移出条件块，无条件更新（A-时序修复）。

### 1.3 双核内嵌（native runtime）

- vps081 agent v0.7.13 已确认成功提取 `_xray_config` 并启动 native xray 内核（Xray 26.3.27）。
- sing-box 主 + xray 辅均在 node-agent 进程内运行，无需独立 xray 二进制。
- `xray.json` 文件在 native 模式下不更新（内存内嵌，不写文件）；验证 xray 是否运行看 agent 日志 `native xray started successfully`，或端口 10085 监听，**不要看文件 mtime**。
- `version.txt` 修改后需重启 agent 才生效（agent 内存缓存版本号）。

### 1.4 7 类下发失败根因与 4 个架构缺陷

| 根因 | 修复 | 彻底性 |
|---|---|---|
| 版本号漂移 | `<` → `!=` | 根治 |
| 重复 runtime | provider_ref 回退 + migration 70 DB 约束 + 23505 重查 | 根治 |
| Agent 版本过低 | 注册观察门禁（本轮新增）+ 推送门禁 | 基本根治（门禁待转强制） |
| path3 不更新 | dispatchAndPush 收口 + path3 兜底 + SelfHeal | 根治 |
| agent_version 竞态 | 无条件更新（移出条件块） | 根治 |
| 版本号死循环 | `ORDER BY priority, id` + 渲染入口 sort | 根治 |
| 隧道 hostname | 一级子域名 + 渲染层校验 | 根治 |

4 个架构缺陷：A 协议设计不完整（RegisterRequest 缺 ProviderRef）、B 状态管理分散（dispatch_status/lpv 多路径）、C 确定性缺失（渲染依赖未排序查询）、D 校验与自愈缺失（无不变式 + failed 无 TTL）。

根治三铁律：协议契约优先于服务端兜底、单一真相源 + 单一更新入口、确定性是渲染层底线。

### 1.5 简化版根治方案（801）

- 核心主张：不改 proto / 不建新表 / 不建新组件 / 不建新 goroutine，约 100 行封堵 4 个缺陷。
- 3 处误判修正：
  1. `ListByRuntimeID` 已有 `ORDER BY priority`，真因是 priority 不唯一，改 `ORDER BY priority, id`；
  2. ack 回写链路已存在（`ReportConfigResult`），只是未回写 node 级 applied；
  3. 竞态根因在 `health_service.go` 条件块包裹 agent_version，移出即根治。
- A=DB 唯一约束 + 时序修正；B=dispatchAndPush 收口 + ack 回写；C=id tiebreaker + 渲染 sort；D=心跳内联 SelfHeal + 注册门禁。

### 1.6 部署避坑（已验证）

- SCP 上传二进制后权限 644，需 `chmod +x`，否则 systemd 报 `status=203/EXEC`。
- `runtimes` 表无 `deleted_at`（ON DELETE CASCADE 硬删除），migration SQL 不可加 `deleted_at IS NULL`。
- `config_versions` 无 `runtime_id`，用 `scope_id` 关联 runtime。
- 迁移表 `goose_db_version`（非 schema_migrations），字段 `version_id` + `is_applied`，手动执行后需注册版本。
- DB：用户 `app`、库 `airport`（非 yundu）；`docker exec yundu-postgres psql -U app -d airport`。
- PowerShell SSH 吞双引号与 SQL 单引号；可靠方式：heredoc + 单引号写 .sql → docker cp → `psql -f`。
- 运行中二进制不可直接覆盖（Text file busy），必须 `systemctl stop` 后再 cp。

---

## 二、2026-08-01 12:31 修正版部署后的最新状态

### 2.1 已上线修正（node-service-only，Agent 零改动）

在 801 方案之上新增：

1. `SelfHealFailedDispatch` 改用 `metadata._dispatch_time` 判 5 分钟陈旧（原用 `updated_at`，节点其他更新会刷新导致自愈失灵）。
2. `ReportConfigResult`：
   - 删除“全部不匹配则标记全部节点”兜底，不匹配只记日志；
   - 新增 `nodesForRuntimeAndServer`，ack 回写覆盖同 server 的 sing-box + xray 全部节点；
   - deployment target 推进遍历全节点，避免 ack 挂错 runtime 时推进错 target。
3. `runtime_service.go` Create 失败仅对 PostgreSQL 23505 唯一约束冲突走重查，其他错误原样返回。
4. WS/gRPC 的 ConfigResult 用 Agent 上报的 `kernel_type`（sing-box/xray）精确解析 runtime，不再走会误选 xray 的 nil 回退。
5. 注册版本观察门禁：低于 `MinAgentVersionForP2`（0.7.5）记 warning，暂不阻断。
6. validator 红测试修复（REALITY SNI 警告按新规范只警告 bing/microsoft，apple.com 不再警告）。

### 2.2 部署验证结果（vps190）

- node-service `active`，新二进制 SHA256 前 16 位 `6667e7e6bc4e9a17`，旧版备份 `/opt/yundu/bin/node-service.bak.20260801fix`。
- vps206：13 sing-box + 6 xray 节点全部 `applied`（xray 内嵌节点随 ack 闭环，新逻辑生产生效）。
- vps081：4 sing-box + 1 xray `pushed`，0 failed。
- 10 分钟 CV=1（部署触发的正常变更），日志无 ERROR/panic。
- 新符号确认：`nodesForRuntimeAndServer`、`no nodes match ack version`、注册门禁日志均在二进制中。

### 2.3 生产核验新发现（现场事实）

- **gRPC 通道不可达**：`6.tiktokplay.na.am` 开启小黄云（Cloudflare 代理，只开放 80/443），Agent 默认 gRPC 地址 `PANEL_URL:9082` 无法穿透 CF；vps81 每约 50s 重连 `[2606:4700:3036::6815:56ba]:9082` 超时。`7.tiktokplay.na.am` 同样在 CF 后。
- **实际通道形态**：WS 主通道（healthy）+ HTTP 兜底；gRPC 名义优先但生产从未建立。
- **自愈语义分叉实证**：生产只有 `ReconcileDispatchStatusOnHeartbeat`（WS/gRPC：failed → applied）触发记录，HTTP 的 `SelfHealFailedDispatch`（failed → pushed）未触发；三通道自愈需收敛为单一语义。
- **面板 gRPC 明文**：`:9082` 公网监听且 `tls=false`。
- **Agent 自升级版本源关闭**：`/opt/yundu/agent-upgrade/` 无生效 `info.json`（仅 .bak/.disabled），selfUpgrader 每 5 分钟 “no update”；节点升级仍只能 SCP。
- **vps190 面板机**存在 node-agent 0.7.9（低于节点端 0.7.13），需确认用途。

---

## 二点五、801 简化版执行结果记录（用户存档，已核对）

### 2.5.1 原始记录

- 部署内容：A-协议收口（agent_version 无条件更新 + migration 000070）、B-状态收口（dispatchAndPush + ack 回写）、C-确定性收口（id tiebreaker + 渲染 sort）、D-自愈收口（心跳内联 SelfHealFailedDispatch）、根因 #2 根治（runtime_service Create 重试 + provider_ref 回退）。
- 10 项验证全部通过：服务 active 无 ERROR、24/24 pushed 0 failed、版本收敛（363==363 / 45==45）、10 分钟 1 条 CV、每台 2 条 runtime 且 provider_ref 非 NULL、agent_version 0.7.13、migration 70 已注册、injectXrayConfig 正常嵌入（vps206:6 / vps081:1）、SelfHeal 编入二进制、重复 runtime 双保险。
- 部署过程修复：SCP 644 权限 → chmod +x；runtimes 无 deleted_at；migration 执行时数据已干净（仅建约束兜底）。
- 改动量：7 个文件约 70 行代码 + 40 行 SQL；二进制 59.49 MB（MiB，即 62,376,837 字节）与生产一致。

### 2.5.2 核对结论与差异标注

| 原始记录 | 现状核对 |
|---|---|
| “注册门禁被跳过，由 pushConfigToRuntime 版本检查覆盖” | ❌ **已过时**：12:31 修正版已在 `runtime_service.go RegisterRuntime` 实现注册观察门禁（低于 0.7.5 记 warning，暂不阻断），不再依赖推送时检查 |
| “ReportConfigResult 只需补版本精确过滤” | ⚠️ 部分过时：修正版在精确过滤基础上**删除兜底标记全部**，并新增 `nodesForRuntimeAndServer` 覆盖 sing-box + xray 双 runtime 节点 |
| “RegisterRuntimeRequest 无 AgentVersion 字段，注册门禁无法实现” | ❌ **已过时**：dto 有 `AgentVersion` 字段但 Agent 客户端 JSON tag 为 `-`（版本藏在 Metadata），修正版门禁已做 Metadata 回退读取 |
| “buildConfigViaKernelRender 是统一渲染入口，sort 已覆盖全部路径” | ✅ 成立；P2 仍需把 chain/runtimes/outbound 等其它渲染相关查询统一排序键 |
| “二进制 59.49 MB 与生产一致” | ✅ 成立（59.49 MiB = 62,376,837 字节，与 12:31 部署二进制一致） |
| 10 项验证全部通过 | ✅ 成立，且 12:31 后 vps206 19 节点已进一步收敛为 `applied`（xray 内嵌节点也闭环） |

---

## 三、当前待办（按优先级）

1. **提交代码并打 tag**：工作区含 801 方案 + 12:31 修正版，全部未提交；生产二进制必须可复现。
2. **自愈三通道统一**：将 `SelfHealFailedDispatch` 与 `ReconcileDispatchStatusOnHeartbeat` 收敛为单一服务，语义统一为“版本一致+运行中 → applied；版本不一致 → failed+reload”。
3. **gRPC 通道**：改走 CF 可穿透的 443（wss+TLS），或独立非 CDN 域名/直连 IP；至少加“不可达即停”退避；9082 配 TLS 或 IP 白名单。
4. **零 SSH 版本库入口**：面板 API 或 CI 同步 `info.json` + 二进制（download_url 可直连 GitHub Release），打通 Agent 自动升级。
5. **P2 排序契约全量**：node/chain_bindings/runtimes/outbound 等渲染相关查询统一 `ORDER BY ... id`；补 `config_render_hash_churn` 指标。
6. **注册门禁转强制**：观察 1 周无副作用后，低于 0.7.5 的注册直接拒绝。

## 2026-08-01 13:40 追加（v0.7.16 后）

- 生产事故复盘：Agent 自动升级后 xray 内存配置丢失且版本一致不触发 reload，xray 永久下线；已由 `xray-cache.json` 落盘缓存 + D3 force 刷新（认证超时也强制）根治，实测重启日志 `xray restored from cache`。
- DNS/证书事故复盘：域名切灰云期间节点解析到源站 IP，源站 443 是 YunDu 自签名证书导致 TLS 全链路失败；已通过节点 `/etc/hosts` 固定 CF 入口恢复（备份 `/etc/hosts.bak.20260801`），并建议源站配置可信证书 + CF 保持小黄云。
- 全自动流程新增：渲染相关查询统一 `ORDER BY ... id`（node/chain/runtime/outbound/credentials）、渲染入口 sort 统一 `priority,id` 键、`nodeservice_config_render_hash_churn_total` 指标、注册门禁转强制、CI 前端构建 job、`install.sh agent --disable-grpc`。

## 2026-08-01 15:10 追加（v0.7.19–v0.7.21）

- **协议注册修复**：`protocol_registry` 新增 `status` 列（migration 000071），补齐 15+ 协议组合真实 schema（对齐 Xray 官方字段）；前端字段 `schema_json`/`config_schema` 对齐；修复“协议全部显示草稿”。
- **部署批次卡死修复**：根因是 Publish 先推送配置后创建批次，ack 早于 targets；修复为批次创建后主动推送 + 列表/详情惰性收口（`ReconcileRunningBatches`）；历史卡死批次已清理。
- **v0.7.20**：`UpdateDispatchStatus` 批量 SQL（`id=ANY`）、ReportConfigResult 三写单事务（nodes/config_versions/deployment_targets）、DeltaSync ack 闭环（Agent 回送 DeltaAck，服务端推进状态）、Agent 注册顶层 `agent_version`、routing 映射单测、DownstreamTagSuffix tag 后缀 fallback 退役、3X-UI provider 退役标注。
- **v0.7.21 / 生产运维**：`MIGRATE_ON_START` 生产启用（.env 覆盖 unit 变量，钩子注入 `MIGRATIONS_DIR=/opt/yundu/migrations` 且失败不 panic）；迁移实测“当前版本 71，待执行 0”；Prometheus 新领域告警规则 `alerts-yundu.yml` 已同步到生产规则目录。
- **迁移执行教训**：`psql -f` 会执行 goose Down 块导致误回滚（000071 曾因此丢失 status 列）；标准流程统一走 `migrate up`（`MIGRATE_ON_START` 或 `/opt/yundu/bin/migrate up`），迁移文件如非必须不写 Down 块。

---

## 四、权威文档导航

- 架构全貌 / 体检基准：`D:\机场搭建\技术指导\YunDu-项目深度体检报告-yundu-src版-20260730.md`
- 下发失败诊断与维护：`D:\机场搭建\技术指导\YunDu-面板节点下发失败诊断与维护指南-20260801.md`（接手人先读 §九 Checklist）
- 根治方案：`D:\机场搭建\技术指导\YunDu-架构缺陷根治方案-简化版-最小改动零SSH-20260801.md`、`...ABCD四项工程-20260801.md`
- 编译部署：`D:\机场搭建\技术指导\编译推送与安装升级维护教程.md`
- 冲突精读 / 生产核验：`D:\机场搭建\yundu-src\YunDu-简化版根治方案-架构冲突精读分析-20260801.md`、`D:\机场搭建\yundu-src\YunDu-生产环境核验报告-20260801.md`

---

*本记忆以 2026-08-01 12:31 修正版部署后的生产状态为准；新代码合入后需同步更新。*
