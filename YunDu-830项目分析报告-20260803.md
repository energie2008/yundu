# YunDu 830 项目分析报告

> 日期：2026-08-03
> 范围：本次生产任务复盘 + 目录文档综合（yundu-src 与根目录/技术指导 MD）
> 基线：yundu-src（node-service / node-agent 均部署 08-03 修复版本）

---

## 一、项目总览

YunDu 是“控制面 + 执行面 + 订阅面”三平面机场系统：

- 控制面：api-gateway + identity / node / subscription / traffic 四服务，PostgreSQL + Redis + NATS + MinIO。
- 执行面：node-agent 内嵌双内核（sing-box 主内核常驻、xray 辅内核处理 XHTTP/REALITY），gRPC/WS/HTTP 三通道下发，加密 Payload Manifest + Delta Sync。
- 订阅面：40+ 客户端渲染、模板注入、负载均衡。

核心差异化能力：双内核原生内嵌、零 SSH 配置闭环、证书/nginx/WARP/端口跳跃全自动、链式代理与计费策略、可观测性全家桶。

参考文档：[README.md](D:/机场搭建/yundu-src/README.md)、[项目360度全方位调查分析.md](D:/机场搭建/yundu-src/项目360度全方位调查分析.md)、`docs/adr/0001-system-architecture.md`。

---

## 二、本次生产任务复盘

### 2.1 版本风暴：根因与根治

- 现象：`config_versions` 每 10 秒 +1，历史累计 75768 条；vps206 一小时 810 次 xray 启停。
- 根因：`mergeRenderedLimiterMeta` 用 Go map 聚合 `_limiter.users`，数组顺序每次渲染随机 → `content_hash` 漂移 → 面板误判“配置变更” → 每心跳建新版本。801 的 `ORDER BY priority,id` 未覆盖 users。
- 次要缺陷：代码写 `config_versions.applied_at`，但无迁移加列，ack 回写永久失败（SQLSTATE 42703）。
- 解决：users 稳定排序 + 单测；迁移 `000077` 补 `applied_at`；历史版本清理（75768 → 953）；部署后版本稳定、xray 重启归零。
- 防复发：渲染确定性契约覆盖 users；`config_render_hash_churn` 指标告警；日常巡检 10 分钟版本增量。

### 2.2 证书策略优化

- 自动下发链路：`injectCertFromBundle` 四级回退（cert_bundle_id → cert_bundles SAN → tls_certificates SNI → 自签兜底）→ PEM 经 TLSMaterials 加密下发 → agent 原子写 `/etc/yundu/certs/{domain}/`。
- 自签模式：持久化到 cert_bundles（provider=self-signed），重启指纹稳定；SNI 用伪装域名；客户端默认 allow_insecure，pinSHA256 仅 URI 高级选项；保存一致性告警只提示不自动改配置。
- 真实证书模式：面板 TLS 证书页 ACME（lego DNS-01/http-01），默认账户入库 `system_settings.acme`（DB 优先、env 兜底），token 只在面板配置一次；签发后同步 cert_bundles，节点 SAN 命中即用真实证书。
- 通配符：支持 `*.xinti.na.am` 单层通配符，精确 SAN 优先、通配符兜底；一次申请覆盖后续子域。
- TLS 分离不受影响：cf_edge（隧道）与 nginx（CDN）节点的 `NeedsCertBundle=false`，证书链路直接跳过，TLS 仍在 CF/nginx 边缘终止。
- 生产验证：`seed4.xinti.na.am`、`*.xinti.na.am` 均签发 Let's Encrypt 证书；P16 精确证书未被通配符覆盖。

### 2.3 tc 限速

- 现象：tc qdisc/class `exit status 2` 刷日志，限速不生效。
- 真相：权限无问题（root + cap_net_admin）；旧 agent 未启用 SpeedEnforcer；ingress filter 每次循环重复添加累积 8681 条。
- 解决：tcshape filter 先删后加幂等；agent 升级 v0.7.22（CI 完整 tags）；vps206 speed enforcer 启动、filter 收敛 1 条，vps81 无直连端口自动跳过。
- 避坑：编译必须带 `with_utls,with_wireguard,with_gvisor,with_quic,with_grpc,with_clash_api`，缺 with_wireguard 会 sing-box 启动失败。

### 2.4 CDN 回源 Host 与优选 IP

- nginx 回源 Host 固定为 `server_name`，消除 CF/隧道探活导致的 `bad host` 噪声。
- 与优选 IP 兼容：优选只改变 TCP 目标地址，TLS SNI 仍是域名，nginx 按 SNI 路由不受影响；约束是节点 transport host 必须与 SNI/server_name 一致。

### 2.5 Agent 升级版本库

- admin-web 页面 + node-service API 已可用：填 version/download_url/sha256 发布一次，全部节点自动升级。
- 二进制可放 vps190 `/opt/yundu/agent-upgrade/`，download_url 用面板自身下载端点，不依赖 GitHub。
- 避坑：sha256 必须与实际二进制一致；当前 info.json 已禁用（原 0.7.23 sha 不匹配），发布前需确认修复已合入正式版。

---

## 三、证书最优策略方案

详细方案见 [YunDu-证书最优策略方案-20260803.md](D:/机场搭建/yundu-src/YunDu-证书最优策略方案-20260803.md)。核心：

| 节点类型 | 默认策略 | 证书来源 | 客户端校验 |
|---|---|---|---|
| REALITY | none | 无证书（dest 伪装） | REALITY 公钥 |
| 直连 TLS/UDP | fixed_self_signed | 持久自签 / 面板 ACME | allow_insecure / pinSHA256 / 严格校验 |
| CDN/Tunnel | 边缘证书 | CF/nginx | 边缘证书 |
| 严格校验 | auto_acme | 面板 ACME（DNS-01） | 严格校验 |

原则：手动优先、显式优于隐式、系统只判定与提示不自动改写；自签适合伪装，真实证书适合可控域名严格校验。

---

## 四、文档与知识库梳理

### 4.1 架构与设计

- `README.md`、`项目360度全方位调查分析.md`：三平面架构、双内核、端口规划、TLS 分离、WARP、链式代理。
- `docs/adr/*`：系统架构 / API 约定 / 节点领域模型 / runtime provider 决策。
- `YunDu项目架构设计文档2.0版本.md`、`YunDu-标准化多VPS架构设计标准模板手册-20260710.md`。

### 4.2 零 SSH 与运维

- `零SSH优化执行计划.md`、`YunDu-最终优化方案-零SSH标准化运维-v1.0.md`、`YunDu-零SSH自动化深度体检报告-20260712.md`。
- `docs/runbooks/*`：727 完整 runbook、vps206 维护、db-bootstrap、node-domain。

### 4.3 生产与方案文档

- `YunDu-生产环境核验报告-20260801.md`、`YunDu-任务分析与总结-20260801.md`、`YunDu-三文档对照-801方案回退与零SSH标准化-20260801.md`。
- `YunDu-简化版根治方案-架构冲突精读分析-20260801.md`：版本风暴 7 类根因与 ABCD 工程。
- `YunDu-证书最优策略方案-20260803.md`：本次证书策略。
- `支付宝微信支付基础搭建与易支付接入分析-20260802.md`、`USDT支付链路检查与中间件选型分析-20260802.md`：支付链路。

### 4.4 避坑与专项

- `文档/YunDu-P0P5架构改造技术总结与避坑指南-20260725.md`、`文档/VPS206-CDN节点修复-20260726.md`。
- `技术指导/`：TLS 分离、WARP、MASQUE、CF 隧道、设备数限制等专项文档。

---

## 五、系统健康度与风险

### 5.1 健康度

- 版本稳定：vps206=10864、vps81=10626 附近，10 分钟窗口无异常增长。
- 节点稳定：vps206/vps81 双核正常、WS 通道健康、端口全监听；xray 重启与 bad host 归零。
- 证书：自签持久化 + 真实 ACME + 通配符链路可用，TLS 分离未受影响。
- 限速：tc 链路就绪（有在线限速用户时生成规则）。

### 5.2 遗留风险

| 风险 | 说明 | 建议 |
|---|---|---|
| obtain/renew 同步超时 | 网关 10s 超时，前端可能 503/504，后台已用 WithoutCancel 保证落库 | 后续改为异步任务 |
| ACME 账户 key 未持久化 | 进程重启会注册新 Let's Encrypt 账户 | 持久化账户 key |
| 多 zone token | 当前 token 仅覆盖 xinti.na.am | 证书级凭证或 All zones token |
| cert_bundles provider 语义 | 真实证书可能覆盖 self-signed 包但 provider 未更新 | 同步时更新 provider |
| 工程卫生 | 测试红、空文档、二进制/bak 残留 | 按 P0 清理 |
| 通道明文 | gRPC/WS 默认 insecure | 控制面强制 TLS |

---

## 六、后续规划

1. 证书阶段二/三：节点保存自动绑定证书包；配置默认 ACME 账户后 auto_acme 全自动申请（新域名/新 VPS 零操作）。
2. obtain/renew 异步化：签发结果通过任务状态回显，避免 10s 超时误导。
3. ACME 账户 key 持久化 + cert_bundles provider 语义修正。
4. 版本风暴监控：config_versions 增量、hash churn 指标接入 Grafana 告警。
5. 正式发版：把 08-03 修复合入 release，恢复 Agent 升级版本库。
6. 工程卫生：红测试修复、空文档补齐、README 基线更新、残留清理。

---

## 七、结论

本次生产任务完成了三件核心事：根治版本风暴（排序契约覆盖 users + applied_at 迁移 + 历史清理）、落地证书最优策略（自签/真实/通配符三态 + 面板化 ACME + 手动优先）、修复直连节点 tc 限速（SpeedEnforcer 启用 + filter 幂等），并顺带修复 CDN 回源 Host、Agent 升级版本库流程。系统回到稳定状态，零 SSH 方向持续推进，剩余风险均已记录并给出下一步。

---

*报告生成日期 2026-08-03（830）。基于本次对话与目录全部 MD 文档综合整理。*
