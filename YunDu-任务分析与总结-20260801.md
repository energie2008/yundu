# YunDu 任务分析与总结（2026-08-01）

> 范围：从“801 简化版根治方案已执行”到 v0.7.21 生产稳定化的完整工作闭环
> 代码基线：main @ `c7e84cf`（v0.7.21，生产 v9 运行中）
> 生产环境：vps190（面板）/ vps206、vps81（节点），Agent 0.7.21

---

## 一、目标

本次任务的总体目标：

1. **验证 801 简化版根治方案**：对照代码事实确认方案是否已执行、是否与当前架构冲突，确定是否需要回退。
2. **消除“人肉运维”**：把节点下发失败、诊断、升级、迁移、部署从 SSH + SQL 收敛为面板可管理、可自动化的标准流程，最终实现“零 SSH、小白可运营”。
3. **修复生产可见问题**：部署管理一直“部署中”、协议注册全是“草稿”、WARP 路由动作缺失、面板误显示 vps190 离线等。
4. **稳定并发布**：让生产二进制可复现（每个发布对应 Git tag），CI 覆盖测试与前端构建。

---

## 二、做了什么（按阶段）

### 2.1 调查与文档精读

- 生成《项目 360 度全方位调查分析》：双内核、渲染器、Agent 推送、证书、WARP、链式代理、健康度评分、四系统对比。
- 精读 730 体检报告、编译推送教程、801 维护指南、简化版根治方案、ABCD 四项工程、WARP 出站动作改造等文档，并与代码逐条核对。
- 结论：730 最接近“项目全貌”，但“已根治”结论下得过早；准确状态以独立代码核对为准。

### 2.2 架构冲突精读与修正版

- 定位 10 个冲突点，最严重 3 个：
  - WS/gRPC 的 ConfigResult 用 nil-ref 解析 runtime，双内核下会把 sing-box 的 ack 挂到 xray runtime；
  - HTTP/WS/gRPC 三通道自愈语义分叉（failed → pushed vs failed → applied）；
  - ReportConfigResult “全部不匹配则标记全部”兜底制造 `lpv != _dispatch_version` 矛盾态。
- 实施修正版并部署（v0.7.14）：`_dispatch_time` 自愈、23505 限定重查、去兜底、`kernel_type` 精确解析、注册观察门禁。

### 2.3 生产核验与事故处理

- 三台生产机只读巡检：24/24 pushed、迁移 70 已执行、双核正常。
- 发现并处理：
  - vps81 gRPC 通道连 `9082` 持续超时（域名走 CF 不转发非 443 端口）→ `CHANNEL_DISABLE_GRPC=1`；
  - 面板误注册 vps190 为节点（0 节点、心跳超时）→ 删除误注册记录并停用面板机 node-agent；
  - 域名灰云期间节点 DNS 解析到源站，源站 443 自签名证书导致全链路 TLS 失败 → 节点 `/etc/hosts` 固定 CF 入口恢复。

### 2.4 发布与零 SSH 闭环

- v0.7.15：Agent 升级版本库 API、健康体检 API、自愈三通道统一、CI 测试 job、`install.sh --disable-grpc`。
- v0.7.16：Agent 重启后 xray 缓存恢复、force 刷新绕过 304、认证超时兜底。
- v0.7.17：排序契约全量、hash 漂移指标、注册门禁强制、CI 前端 job。
- v0.7.18：WARP 池出站动作、阻断修复、零 SSH 收尾。
- v0.7.19：协议注册 status + 真实 schema、部署批次卡死修复。
- v0.7.20/v0.7.21：批量 SQL、三写事务、Delta ack 闭环、Agent 顶层版本字段、MIGRATE_ON_START、前端/面板升级脚本、Prometheus 领域告警。

### 2.5 关键生产能力验证

- Agent 自动升级实战：0.7.13 → 0.7.15 → 0.7.16 → 0.7.21，全部自动完成。
- MIGRATE_ON_START 生产启用：启动即 `migrate up`，版本 71、0 pending。
- 前端自动部署演练：`deploy-frontend.sh` 部署 admin-web + user-web 到宝塔真实站点。
- Prometheus 告警：`alerts-yundu.yml` 四条领域告警已加载。

---

## 三、版本号死循环根因

### 3.1 现象

节点每次心跳都触发 `needs_reload`，面板每 10 秒创建新 `config_versions`，版本号飙升（如 v357→v358→…），日志出现大量 `config pushed to agent`。

### 3.2 根因链

```text
ListByRuntimeID 查询无稳定排序（或 ORDER BY priority 但 priority 不唯一）
  → inbounds 数组顺序每次不同
  → JSON 序列化 hash 不同
  → 面板误判“配置变更”
  → 创建新 config_version
  → agent 拉取应用 → 心跳版本又“不一致” → 死循环
```

具体代码事实：
- `node_repo.go` 的 `ListByRuntimeID` 只有 `ORDER BY priority ASC`，priority 可重复，PostgreSQL 不保证相同 priority 的行顺序稳定；
- xray 配置经 `_xray_config` 内嵌在 sing-box 配置中渲染，xray 节点顺序漂移同样影响 hash；
- 渲染入口与 DB 排序键曾不一致（渲染层按 ID、DB 按 priority），形成两套顺序契约。

---

## 四、根治方案与架构冲突

### 4.1 根治方案（801 简化版 + 修正版）

| 工程 | 内容 | 当前状态 |
|---|---|---|
| A 协议收口 | DB 唯一约束（migration 70）+ provider_ref 回退 + agent_version 无条件更新 | ✅ |
| B 状态收口 | dispatchAndPush 单一入口 + path3 兜底 + ack 回写 applied | ✅ |
| C 确定性收口 | `ORDER BY priority, id` + 渲染入口同键 sort | ✅ |
| D 自愈收口 | SelfHealDispatchStatus 三通道统一 + 注册门禁强制 | ✅ |

### 4.2 与当前架构的冲突（精读结论）

方案方向正确，但实现假设停留在“单 runtime、单通道、nodes.metadata 即全部状态”，与 8-01 代码的双内核 + 三通道 + 部署批次架构脱节：

1. **ack 挂错 runtime**：WS/gRPC ConfigResult nil-ref 回退优先 xray；已改为按 `kernel_type`（sing-box/xray）精确解析，且 `ReportConfigResult` 覆盖同 server 双 runtime 全部节点。
2. **三通道自愈分叉**：HTTP 刷 pushed、WS/gRPC 刷 applied；已统一为 `SelfHealDispatchStatus`（版本一致+运行中→applied；其余超 5 分钟→pushed）。
3. **兜底标记全部**：制造 `lpv != _dispatch_version` 矛盾态；已删除兜底，仅记日志。
4. **`updated_at` 判陈旧**：节点任意更新会刷新；已改用 `_dispatch_time`。
5. **三写非原子**：nodes / config_versions / deployment_targets 跨表；已实现单事务 `ApplyDispatchAndTargetsTx`。
6. **迁移 SQL 与表结构不符**：方案引用了不存在的 `deleted_at`；实际 migration 70 只补 NULL 唯一约束。
7. **Create 吞错误**：任意失败都重查返回 existing；已限定 23505。
8. **两套排序契约**：渲染层 ID sort vs DB priority sort；已统一 `priority, id`。
9. **注册门禁未落地**：已实现并转强制。
10. **Delta ack 无闭环**：已实现 Agent 回送 + 服务端推进。

### 4.3 回退策略

- **总体判断**：方案整体不回退（生产实测有效），只回退有害子行为（已修正）。
- **二进制回退**：每次替换前保留 `node-service.bak.*`；标准回退用 `scripts/panel-upgrade.sh --service=xxx --version=旧版本`。
- **⚠️ migration 70 回退陷阱**：旧二进制只按 `provider_ref IS NULL` 复用 runtime，migration 70 回填后旧代码会 miss 并创建新 runtime，再被 NULL 唯一索引拦住导致注册失败；因此**不要回退 migration 70**，回退二进制必须同时处理 runtime 数据。
- **迁移执行教训**：`psql -f` 会执行 goose Down 块（000071 曾因此丢失 status 列）；标准流程统一 `migrate up` / `MIGRATE_ON_START`，迁移文件如非必须不写 Down 块。

---

## 五、零 SSH 之路

### 5.1 现状盘点

已零 SSH：
- 节点配置变更（面板 → 心跳 reload）
- 证书签发/续期（certmagic 自动）
- WARP 注册/出口（warpreg + Agent 自动）
- nginx/cloudflared（Reconciler 自动）
- **node-agent 升级（面板版本库 → selfUpgrader 自动）**
- **DB 迁移（MIGRATE_ON_START 自动）**
- **前端发布（deploy-frontend.sh 一键部署）**
- **诊断（健康体检 API + 面板页面）**

仍需人工/脚本：
- 面板二进制升级：`scripts/panel-upgrade.sh`（脚本级，非 UI 级）
- 首次节点 bootstrap：`install.sh agent`（一次性）
- SSH 只读诊断（应急）

### 5.2 Agent 自动升级

闭环：面板发版 → `info.json`（version/download_url/sha256）→ Agent 心跳 `ACTION_UPGRADE` / 5 分钟轮询 → 下载 + SHA256 校验 + 原子替换 + systemd 拉起 + 健康门禁（30s 自动回退）。

实战验证：0.7.13 → 0.7.15 → 0.7.16 → 0.7.21 全部自动完成，无人工 SCP。

### 5.3 前端部署

- CI 产出 `admin-web-dist.tar.gz` / `user-web-dist.tar.gz`（v0.7.17+）。
- `scripts/deploy-frontend.sh`：自动探测 nginx 站点根目录（支持 `YUNDU_ADMIN_WEB_ROOT` 覆盖）→ 下载 Release dist → 备份 → 替换 → 改属主。
- `install.sh upgrade panel` 内置 `deploy_frontend` 函数，面板升级时顺带发前端。
- 生产演练已通过：admin-web + user-web 部署到 `/www/wwwroot/{6,7}.tiktokplay.na.am`。

---

## 六、难点与解决方法

### 6.1 生产事故类

| 难点 | 现象 | 解决 |
|---|---|---|
| Agent 重启 xray 丢失 | 自动升级后 10085 消失、xray 节点下线 | `xray-cache.json` 落盘缓存 + 启动恢复 + D3 force 拉取（认证超时也强制） |
| 域名/证书 | 灰云期间节点解析到源站，源站自签名导致 TLS 全链路失败 | 节点 `/etc/hosts` 固定 CF 入口；建议源站配可信证书 |
| 部署管理卡死 | 批次永远 running、target 永远 pending | Publish 先推送后建批次时序修正 + 列表/详情惰性收口 + 清理历史批次 |
| 面板误注册 | vps190 显示离线/心跳超时 | 删除误注册 server + 停用面板机 node-agent |
| 迁移误执行 | `psql -f` 跑 goose Down 块，status 列被删 | 移除 Down 块重跑；标准流程改 `migrate up` |

### 6.2 工程类

| 难点 | 解决 |
|---|---|
| PowerShell SSH 吞引号 | heredoc + base64 传递脚本；psql 用 `-f` 文件 |
| pnpm 阻止 esbuild 构建 | `pnpm-workspace.yaml` 配置 `allowBuilds: esbuild: true` |
| SCP 上传慢/断线 | 重试 + 校验 SHA256；小文件（前端）与大文件（后端）分开传 |
| 字段名不一致 | 前端 `schema_json` ↔ 后端 `config_schema` 对齐 |
| 协议全显示草稿 | `protocol_registry` 缺 status 列，前端 fallback draft → 加列 + 后端返回 status |
| 三写跨表不一致 | 单事务 `ApplyDispatchAndTargetsTx` |
| 逐节点半更新 | `UpdateDispatchStatus` 改 `id = ANY($1)` 批量 SQL |

---

## 七、下一步优化

### P1 生产收尾

1. 源站 nginx 443 配置可信证书（CF `Full(strict)` 或切灰云场景的安全前提）。
2. 面板组件自升级 UI 化（当前 `panel-upgrade.sh` 是脚本级）。
3. 前端发布纳入 `install.sh upgrade panel` 并在生产验证一次（脚本已具备，流程已验证过 deploy-frontend）。
4. 明确 9082 处置：Agent 已禁用 gRPC，端口仍在公网监听，建议防火墙白名单或关闭。

### P2 工程化

5. `DeltaAck` 生产回归验证（当前已实现，等待用户变更场景观察）。
6. 部署批次“被超越”场景策略：目标版本已被更新版本替代时，批次自动标记 failed/rolled_back 或提供清理入口。
7. 更多单测：部署 reconcile、protocol schema 校验、迁移流程。
8. 文档行号自动校验接入 pre-commit（脚本已具备，尚未接入 git 钩子）。
9. 清理 3X-UI provider 或正式接入（当前仅退役标注）。

### P3 可选

10. xray per-user 限速与 sing-box 对齐（`native_xray.go` TODO）。
11. `ACTION_DRAIN` 优雅排空完善、10000 控制端口纳入防火墙、端口常量收敛。
12. 多控制面/多面板高可用与配置队列化。

---

## 八、当前状态基线

| 项 | 状态 |
|---|---|
| 生产 node-service | v9（hash `e8b789e4`），active |
| Agent | vps081/vps206 均 0.7.21，双核 10085 正常 |
| 配置版本 | vps081=46、vps206=369，0 failed |
| 迁移 | goose 71，0 pending，MIGRATE_ON_START 生效 |
| 前端 | admin-web/user-web v0.7.21 已部署 |
| 监控 | Prometheus 领域告警已加载 |
| 发布 | v0.7.14 → v0.7.21，CI 全绿（测试 + 前端 job） |

---

## 九、提交与版本时间线（摘要）

| 版本 | 关键内容 |
|---|---|
| v0.7.14 | 801 简化版 + 三通道修正版基线 |
| v0.7.15 | 零 SSH 闭环：升级版本库/健康体检 API、自愈统一、gRPC 可禁用、CI 测试 |
| v0.7.16 | xray 缓存恢复 + D3 force + 认证超时兜底 |
| v0.7.17 | 排序契约全量、hash 指标、门禁强制、CI 前端 job |
| v0.7.18 | WARP 池出站动作、阻断修复、零 SSH 收尾 |
| v0.7.19 | 协议注册 status/schema、部署批次卡死修复 |
| v0.7.20 | 批量 SQL、三写事务、Delta ack、Agent 顶层版本字段、测试补强 |
| v0.7.21 | MIGRATE_ON_START 修复、前端/面板升级脚本、生产运维启用 |

---

*总结完成：2026-08-01。本文档基于生产验证与代码事实，含生产细节，未入库；如需入库请确认。*
