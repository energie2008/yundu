# 云渡 YunDu 项目 360 度全方位调查分析

> 调查对象：`D:\机场搭建\yundu-src`
> 调查基线：Git `9c47780`（2026-08-01，`fix(routing): buildSingBoxRuleSet skip inline rulesets`）
> 调查日期：2026-08-01
> 方法：源码逐包阅读 + Git 历史核对 + 单元测试实测 + 文档交叉验证。文中所有结论尽量给出代码文件定位，可逐条复核。

---

## 调查说明

| 项目 | 数据 |
|---|---|
| 代码规模 | 501 个 Go 文件 + 134 个 TS/TSX 文件等，共 647 个源文件，约 17.5 万行 |
| 数据库迁移 | `migrations/` 共 67 个迁移文件，编号至 `000070` |
| Go 测试文件 | 53 个 `*_test.go` |
| 测试实测 | `packages/subscription` 除 1 个用例失败外全部通过；`node-agent` 的 hotdiff/pipeline/validator/warp 包全部通过 |
| 仓库状态 | 工作区含用户未提交改动与 `.bak`/二进制残留，调查中未改动 |
| 关联文档 | 技术指导目录存在 730 体检报告、801 维护指南、ABCD 四项工程与简化版方案，本次已交叉核对 |

**一句话结论**：YunDu 已经是一个架构上明显高于传统机场面板的“控制面 + 执行面 + 订阅面”三平面系统，双内核、零 SSH、WARP、链式代理都是真实落地的差异化能力；当前主要风险集中在工程卫生（空文档、测试红、残留文件）、3X-UI 对接半成品、以及部分安全默认值（通道 TLS、硬编码 Secret）。

> **2026-08-03 更新记录**：本轮在生产修复了版本风暴复发、证书自签/真实证书双模式与面板化 ACME 申请、直连节点 tc 限速三大问题，并在文末新增“十四、2026-08-03 生产任务避坑清单”与“附录 C”。以下 4.8 / 5.5 / 6.5 为本次落地内容的正文补充。

---

## 一、系统架构

### 1.1 三平面总体结构

README 与 ADR-0001 描述的三平面架构与代码实际一致：

```text
admin-web(5173) / user-web(5174)
        │
        ▼
api-gateway(8080, REST 反代 + 限流 + 认证 + RBAC)
        │
        ├── identity-service(8081)     用户/管理员/套餐/订单/工单/返佣/支付
        ├── node-service(8082)         服务器/节点/证书/部署/健康/AI 诊断
        ├── subscription-service(8083) 订阅令牌/模板/客户端适配/负载均衡
        └── traffic-service(8084)      流量/额度/在线人数/设备聚合
                │ gRPC AgentChannel(9000) + HTTP/WS 降级通道
                ▼
           node-agent(VPS)
                ├── sing-box(主内核, 常驻)
                └── xray(辅内核, XHTTP 节点时懒加载)
```

代码事实：
- 网关路由表集中在 `apps/api-gateway/internal/app/app.go`，按 public / user / admin / agent 四组注册，`/api/v1/agent/*` 按前缀分发到 traffic-service 或 node-service。
- 服务边界见 `docs/adr/0001-system-architecture.md`；数据模型强调 `users`、`subscription_tokens`、`servers`、`runtimes`、`nodes`、`proxy_chains`、`config_versions` 分层解耦。
- 生产部署为 Docker Compose（`deploy/docker/docker-compose.full.yml`）：PostgreSQL 16、Redis 7、NATS、MinIO、migrate 与四个 Go 服务。

### 1.2 技术栈事实

| 层 | 实际依赖（go.mod / package.json） |
|---|---|
| Go | go 1.26（node-agent）、go 1.25.7（node-service 等） |
| 内核 | sing-box `v1.13.14`、xray-core `v1.260327.0`（Go 库内嵌，非子进程） |
| HTTP | Gin `v1.12.0`（各服务）+ Go 标准库 ReverseProxy（网关） |
| 数据库 | pgx v5、PostgreSQL JSONB、goose 迁移 |
| 缓存 | go-redis v9（限流/在线/设备态） |
| 证书 | certmagic `v0.25.2`（Agent）、lego `v4.35.2`（node-service） |
| gRPC | grpc-go `v1.82.0`，protobuf 定义在 `packages/proto/agent/v1/channel.proto` |
| 前端 | React 18、Vite 5、Tailwind 3、TanStack Query、Zustand、lucide-react |

### 1.3 服务健康观测

- 每个服务暴露 `/healthz`、`/readyz`、`/metrics`（Prometheus），网关统一接入 RequestID、AccessLog、Recovery、CORS、限流、恶意 UA 拦截。
- 可观测性全家桶：`deploy/docker/docker-compose.observability.yml` 提供 Prometheus / Grafana / Loki / OpenTelemetry Collector，Grafana 预置机场仪表盘。
- node-service 有 gRPC Agent 连接数/消息数的 Prometheus 指标（`apps/node-service/internal/grpcserver/agent_server.go`）。

### 1.4 架构层面的已知裂缝

1. `docs/architecture.md` 仍是 XBoard 双轨时代文档（描述 Node.js 网关 + SSH 隧道 + XBoard），与当前 Go 微服务实现完全脱节。
2. `docs/下一阶段执行计划-20260724.md` 与 `docs/架构技术版本更新-20260724.md` 均为 **0 字节空文件**，README 却指向它们。
3. README 声称代码基线 `2b38b68`，实际最新基线为 `9c47780`；README 健康度评级与端口表也存在过期（端口已迁移到 30000+ 段）。
4. 仓库残留 `.bak.20260731` 备份文件、`node-agent-linux`/`node-service-linux`/`agent` 二进制、`admin-dist.tgz`，以及 `.qoder/`、`.tmp-readme/` 目录。

---

## 二、渲染器架构

### 2.1 统一 IR 与注册表

渲染链路采用“NodeSpec（内核中立 IR）→ kernelrender（服务端 inbound 渲染）→ 客户端渲染器（订阅 outbound 视角）”两段式设计：

- `packages/subscription/nodespec/spec.go`：`NodeSpec` 统一协议（vless/vmess/trojan/ss/hysteria2/tuic/anytls/mieru/socks/http）、传输（tcp/ws/grpc/http2/quic/kcp/httpupgrade/xhttp）、安全（none/tls/reality）、多用户 `Clients []CredentialSpec`（含 per-user 限速/设备/IP 限制）。
- `packages/subscription/kernelrender/renderer.go`：定义 `Renderer` 接口（`Render` + `KernelType`），`Registry` 注册中心，`UnsupportedFeatureError/Warning` 结构化表达内核差异。
- `packages/subscription/kernelrender/xray.go`、`singbox.go`：分别渲染 Xray 与 sing-box 的服务端 inbound 配置。
- `packages/subscription/kernelrender/registry.go` 通过 `init()` 注册默认双渲染器。

### 2.2 端口语义显式分离（P8）

`kernelrender/renderer.go` 的 `resolveInboundPort` / `resolveListenAddress` 是重要契约：

- `ServerPort > 0`：CDN/Tunnel/SaaS 节点，xray/sing-box 监听 `127.0.0.1:ServerPort`，由 nginx/cloudflared 回源。
- `ServerPort == 0` 或等于 `Port`：直连节点，监听 `0.0.0.0:Port`。
- 客户端看到的 `Port`（如 443）只用于订阅链接和 nginx proxy_pass，不再决定实际监听。

### 2.3 双核等价校验

`packages/subscription/validator/validator.go` 提供 `DualKernelValidator`：

- 语义校验 → 双核渲染 → 真实二进制 dry-run（`xray -test` / `sing-box check`）→ 双核语义等价性校验。
- 无二进制环境自动跳过 dry-run（`skipDryRun`）。
- 实测：`go test ./...`（packages/subscription）中 `TestEnhancementValidator_RealitySNIWarning` **失败**（期望 apple.com SNI 触发 GFW 主动探测警告，实际未触发），说明 Enhancement 警告规则与测试断言至少有一侧漂移。

### 2.4 客户端订阅渲染

`packages/subscription/renderer/` 提供 Clash/ClashMeta/SingBox/URI/Surge/Loon/Quantumult/Shadowrocket 渲染器；`apps/subscription-service/internal/client/detector.go` 按 User-Agent 识别 40+ 客户端并映射到渲染器：

- Clash 系 → YAML；sing-box 系（SFA/SFI/SFM/Hiddify/Karing）→ sing-box JSON；Surge → conf；其余 → base64 URI。
- `subscription_handler.go` 支持 DB 模板注入（Clash proxies、SingBox outbounds 自动填充空 proxy-group），并对 Shadowrocket/Quantumult/Surge 做内容 sanitize。
- 协议预设 P01–P17（`packages/subscription/presets/*.yaml`）覆盖 REALITY、WS、gRPC、XHTTP、Hysteria2、TUIC、AnyTLS、SS2022、WARP MASQUE Overlay 等组合，预设带内核兼容矩阵（`KernelCompat`）与 uTLS/ECH/Mux 适用性声明。

### 2.5 渲染器风险点

1. `TestEnhancementValidator_RealitySNIWarning` 失败，校验层存在“红测试”：
   `packages/subscription/validator/validator_test.go:192`
2. `RawConfig` 逃生舱允许按内核前缀合并原始配置（`spec.RawConfig["xray"]` / `["sing_box"]`），绕过 IR 校验，存在“面板保存合法但内核配置非法”的旁路。
3. `DownstreamTagSuffix("-dl")` 仍是“临时兜底”契约（`kernelrender/registry.go:9-13`），依赖 tag 后缀判定下行 inbound，P1 上线 `downstream_exposure_mode` 后应退役。
4. `apps/node-service/internal/exposure/` 已标注为 chain 渲染的薄包装（`chain_common.go`），保留只为向后兼容，属于待删 zombie 面。

---

## 三、双内核运行模式

### 3.1 主辅翻转（P2）

`apps/node-agent/internal/runtime/multi_runtime.go` 是双内核核心：

- **sing-box 为主内核**：`NewMultiRuntimePlugin` 构造时立即创建 `NativeSingbox`，常驻运行。
- **xray 为辅内核**：`ensureXrayPlugin()` 懒加载，仅在配置中含 XHTTP 节点时启动。
- `Start` 按配置中 `runtime_type` 分发；`Stop` 同时停两核；`GetTrafficStats` / `GetTrafficStatsNoReset` 合并两核 per-user 流量（同用户累加）。
- `UpdateUsers` 对已启动的内核逐个应用增量用户（xray 失败不阻断 sing-box）。

### 3.2 原生内嵌运行时

`runtime/native_singbox.go` 与 `runtime/native_xray.go` 将内核以 Go 库形式内嵌进 Agent 进程：

- 配置从内存字节流加载（不落盘），`sha256` 记录 config hash。
- sing-box 采用**蓝绿热转**：SO_REUSEPORT 双实例共存，新实例启动成功后将老实例移入排空队列（30s 超时），失败时安全 Close 后重建再启动（避免 netlink goroutine panic）。
- xray 通过内置 gRPC API（`127.0.0.1:10085`）实现 `AlterInbound` 增量用户管理与 `StatsService` 流量统计；启动时对配置按 tag 去重，防止 `existing tag found: api`。
- `runtime/plugin.go` 定义 `RuntimePlugin` 接口（Start/Stop/UpdateUsers/GetTrafficStats/GetTrafficStatsNoReset/Status/Validate）。
- `plugin_adapter.go` 将 RuntimePlugin 适配为 executor 接口，并注入 SpeedLimiter/DeviceChecker/IPChecker 到 sing-box ConnTracker。

### 3.3 热重载与 LKG 回滚

- `internal/hotdiff/hotdiff.go`：配置差异分级（用户级变更走 AlterInbound，结构变更走全量热更）。
- `internal/pipeline/pipeline.go`：统一部署流水线：锁 → 预检 → 临时文件 → dry-run → 备份 → 激活 → apply → 健康检查 → 成功/回滚（LKG `.bak`）。
- Agent 对健康检查失败触发 LKG 回滚，prober 可配置 `YUNDU_DISABLE_PROBER=1` 关闭误判回滚。

### 3.4 双核配置内嵌机制（`_xray_config`）

这是 P2 翻转后最关键的配置链路：

1. **服务端**：`apps/node-service/internal/service/deployment_service.go` 的 `injectXrayConfig()` 在渲染 sing-box 主配置时，查询同一 server 下的 xray runtime；若存在 xray 节点，用 `buildXrayConfigViaKernelRender` 渲染 xray 完整配置，作为 `_xray_config` 字段嵌入 sing-box 配置。
2. **Agent**：`apps/node-agent/cmd/agent/main.go:927-940` 在应用前提取 `_xray_config`，从主配置删除该字段，随后 `StartNative(xrayConfigBytes)` 懒加载 xray 内核。
3. **801 修复**：`injectXrayConfig` 返回 xray 节点列表，供调用方同步 `last_published_version` 与 `dispatch_status`，否则 xhttp 节点面板永远显示“下发失败”。
4. **版本强约束**：`pushConfigToRuntime` 对低于 `MinAgentVersionForP2`（727.0.0）的 Agent 拒绝推送（`deployment_service.go:1270-1290`），防止旧 Agent 无法解析 `_xray_config`。
5. **崩溃恢复**：Agent 缓存 `lastXrayConfig`，watchdog 重启 sing-box 后调用 `maybeRestartXray` 恢复 xray 内核。

同类内嵌字段还包括：`_nginx_vhosts`（NginxReconciler 独立 30s 轮询）、`_limiter`（限速元数据）、`_traffic_quota`（流量限额）、`_port_hopping`（端口跳跃 DNAT）、`_chain_bridges`（链式桥接）、`_audit_rules`（BT/SSRF 阻断规则）。

### 3.5 Delta Sync 增量同步

`packages/proto/agent/v1/channel.proto` 定义 `DeltaSync`（add_users / del_users），`apps/node-agent/internal/delta/applier.go` 实现：

- 版本必须是 `baseVersion + 1`，否则返回 `needFullSync=true` 触发全量同步。
- 通过 `RuntimePlugin.UpdateUsers` 走 AlterInbound / ConnTracker 热更，目标 sub-second 零断流。
- 全量配置应用成功后设置基线版本。

---

## 四、Agent 骨架与推送更新机制

### 4.1 启动与配置拉取

Agent 主流程在 `apps/node-agent/cmd/agent/main.go`（约 2100+ 行）：

- 配置拉取优先加密 Payload Manifest（`internal/client/client.go` + `node-service/internal/crypto/payload.go` 的 AES-GCM + SHA256 签名），失败回退明文 FetchConfig（HTTP 304 支持）。
- 签名校验基于原始内容 hash；加密通道上的签名不匹配仅告警（AES-GCM 已认证），明文通道严格拒绝。
- 应用前依次剥离 `_nginx_vhosts`、`_limiter`、`_xray_config`、`_port_hopping`、`_chain_bridges`，再注入审计规则，最后走 Pipeline。

### 4.2 三通道冗余

`apps/node-agent/internal/transport/` 实现 gRPC / WebSocket / HTTP 三通道：

| 通道 | 优先级 | 说明 |
|---|---|---|
| gRPC | 0 | 双向流，Auth/HMAC，全消息类型 |
| WS | 1 | 实时推送，HMAC 签名头 |
| HTTP | 2 | 兜底轮询 `/api/v1/agent/messages` |

`channel_manager.go` 负责按健康度切换：健康检查 20s、失败阈值 3、指数退避重连（1s–60s + 20% jitter），支持管理员手动 `SwitchChannel`。

### 4.3 心跳与动作

- 心跳周期 10s（`heartbeat.go`），携带 config version、ServerLoad、KernelInfo、NodeStatus（在线人数）、ChannelHealth。
- 面板可返回 `HEARTBEAT_ACTION_RELOAD / RESTART / MAINTENANCE / UPGRADE / SYNC_EXTERNAL_RESOURCES`。
- RELOAD 带 0–3000ms 随机 jitter，避免所有节点同时拉配置；RELOAD 后自动触发 nginx 同步。
- 服务端 gRPC `AgentServer` 首条消息必须 Auth，校验 HMAC 签名与 nonce，25s ping/pong，1 小时 session token。

### 4.4 配置下发与状态机

服务端 `deployment_service.go` 形成“lpv → pending → push → pushed/failed”闭环：

- `dispatchAndPush`（B-收口）统一封装 path1/path2 的状态流转。
- path3（hash 不变）也会更新 `dispatch_status=pushed`（801 修复）。
- `ReportConfigResult` 按 `lpv == versionNo` 精确回写节点 `applied/failed`（`deployment_service.go:3358+`），并有 `HeartbeatSyncDispatchStatus` 幂等兜底。
- `SelfHealFailedDispatch`：failed 超 5 分钟自动重置为 pushed，复用心跳周期 10s 巡检，零额外 goroutine。

### 4.5 自升级与零 SSH 运维

- `internal/upgrader/self_upgrader.go`：下载新二进制 → SHA256 校验 → 原子 rename → `os.Exit(0)` 由 systemd 拉起；防降级版本比较；升级后 30s 内无健康心跳由 systemd 回退旧版本。
- `install.sh` 支持 `agent / panel / upgrade agent / upgrade panel / download`，systemd 单元带 `NoNewPrivileges`、`ProtectSystem`、`ProtectHome`、`ReadWritePaths` 限制。
- `nginx/skeleton.go` 生成/注入 Nginx 骨架（支持宝塔共存检测），`sync.go` 原子写入 + `nginx -t` 失败自动回滚。
- `firewall/firewall.go` 自动检测 ufw / firewalld / iptables 并开放 inbound 端口；端口跳跃通过 iptables/nftables DNAT 实现（sing-box 不支持原生 hop_ports）。

### 4.6 731/801 稳定性增量修复

**731（7-31 累积线上合入）**：commit `be89865`，17 文件 +478/-78，覆盖心跳在线人数上报、连接跟踪、仪表盘、流量统计、健康服务等此前仅本地部署未推送的修复。

**801 稳定性修复点（代码中可逐条核实）**：

| 根因 | 现象 | 修复代码 |
|---|---|---|
| #1 版本号漂移 | DB 重建后 agent 版本高于面板版本，永不 reload | 版本比较逻辑修正 |
| #2 重复 runtime | `UNIQUE(server_id, runtime_type, provider_ref)` 对 NULL 不生效，provider_ref IS NULL 可重复插入 | migration `000070`：回填 NULL + 部分唯一索引 `uq_runtimes_server_type_null_ref` + `runtime_service.go` 注册回退 |
| #3 Agent 版本过低 | 旧 Agent 无法处理 `_xray_config`，静默失败 | 推送前版本门禁 `MinAgentVersionForP2` |
| #4 path3 不更新 | agent 重启 version=0 触发 re-fetch，旧 failed 永远不清除 | path3 补 `markNodesDispatchStatus("pushed")` + `SelfHealFailedDispatch` |
| #5 agent_version 竞态 | 首次心跳无 metrics 时 agent_version 不更新，读到旧版本误拒推送 | `health_service.go` A-时序修复：无条件更新 agent_version |
| #6 版本号死循环 | 渲染顺序不稳定导致 hash 漂移、每 10s 建新版本 | `node_repo.go` `ORDER BY priority ASC, id ASC` + `kernel_render_adapter.go` 入口 sort 双保险 |
| #7 隧道 hostname | 二级子域名不在 CF Universal SSL 覆盖范围 | 证书/SAN 链路修复 |

### 4.7 801 简化版架构缺陷根治方案（ABCD 四项工程）

完整方案见技术指导目录：`YunDu-架构缺陷根治方案-ABCD四项工程-20260801.md`（前置版）与 `YunDu-架构缺陷根治方案-简化版-最小改动零SSH-20260801.md`（落地版）。

**四项工程定义**：

| 工程 | 前置版（大重构） | 简化版（约 150 行） | 当前代码落地情况 |
|---|---|---|---|
| A 协议补全/收口 | proto 增加 provider_ref/agent_version + 双写兼容 | DB 唯一约束 + 心跳无条件更新 agent_version，不改 proto | ✅ 已落地：migration 000070、health_service.go 时序修复 |
| B 状态机收口 | 新表 node_dispatch_states + transition | 抽 `dispatchAndPush` 单一入口 + ack 回写 applied | ✅ 已落地：dispatchAndPush、ReportConfigResult、HeartbeatSyncDispatchStatus |
| C 确定性渲染 | ORDER BY + AssertSorted panic + debounce | `ORDER BY priority, id` tiebreaker + 渲染入口 sort | ✅ 已落地：node_repo.go:514、kernel_render_adapter.go:333-338 |
| D 巡检自愈 | 独立 InvariantChecker + 5 个 Check | 心跳内联不变式 + 注册门禁 + failed TTL 复用 re-fetch | ✅ 基本落地：SelfHealFailedDispatch、心跳巡检；注册门禁为观察模式 |

**核心结论（简化版文档与代码核对一致）**：根治不等于重构。7 类根因背后的 4 个架构缺陷（协议设计不完整、状态管理分散、确定性缺失、校验与自愈缺失）已用“DB 约束 + 单一入口 + 稳定排序 + 心跳内联自愈”封堵，node-agent 零改动。

### 4.8 版本风暴复发根因与 08-03 根治落地

**现象（2026-08-03 生产实测）**：`config_versions` 每约 10 秒 +1（vps206 一路增长到 10858），历史累计 75768 条；node-agent 一小时出现 810 次 xray 启停日志，所有节点在重启窗口短暂不可用。

**根因链**：

```text
mergeRenderedLimiterMeta 用 Go map 聚合 _limiter.users
  → users 数组顺序每次渲染随机
  → content_hash 漂移（实测同一配置在多个 hash 间循环）
  → GetRuntimeConfig 误判“配置已变更”
  → 每心跳创建新 config_version → 推送 → agent 全量 pipeline
```

- 801 的 `ORDER BY priority, id` 只覆盖节点列表排序，未覆盖 limiter users；精读文档曾预警“其他未排序渲染源可能复发”，本次为实锤。
- 次要缺陷：`ReportConfigResult` 写 `config_versions.applied_at`，但没有任何迁移添加该列，ack 回写每次报 `column "applied_at" does not exist`（累计 23234 次），applied 状态永远无法落库。

**解决**：

1. `kernel_render_adapter.go` 的 `mergeRenderedLimiterMeta` 输出前按 email/uuid 稳定排序，并新增确定性单测。
2. 新增迁移 `000077_config_versions_applied_at.sql` 补 `applied_at` 列（生产 goose 77 已应用）。
3. 清理历史垃圾版本：保留每个 runtime 最近 200 条，`config_versions` 75768 → 953，同时清理对应 74768 条 payload。
4. 部署 node-service 后版本稳定：vps206=10859、vps81=10623，心跳 `needs_reload=false`，xray 重启归零。

**防复发**：渲染确定性契约已覆盖 users；`config_render_hash_churn` 指标可用于告警；建议把“10 分钟 config_versions 增量”纳入日常巡检。

---

## 五、证书架构，端口路由设计，协议分析，TLS 分离分析

### 5.1 证书架构

**6+1 种签发模式**（`apps/node-agent/internal/cert/acme.go`）：

| 模式 | 用途 |
|---|---|
| http | HTTP-01 challenge（80 端口） |
| dns | DNS-01（acme.sh + Cloudflare 等） |
| certmagic | 纯 Go 实现（HTTP-01/DNS-01，自动续期） |
| self | 自签名（测试/内网） |
| file | 读取已有证书文件 |
| content | 面板推送 PEM（原子热替换） |
| none | 不签发（直连节点无 nginx） |

关键机制：
- **证书四级回退**（727 runbook P1）：节点 `cert_bundle_id` 绑定证书 → `cert_bundles` SAN 匹配 → `tls_certificates` SNI 匹配 → 自签名 ECDSA P-256 兜底（含 SHA256 指纹告警）。
- **原子热替换**：`atomic.Pointer[PEMBundle]` + `tlsCertCache`，content 模式通过 CertEventBus 通知 xray/nginx reconciler 重载，零中断。
- **SAN 24h 自动同步**：`SyncSANFromNodes` 扫描启用节点 SNI 合并到证书 SAN，带 `sync.Mutex` 防并发（`cert/service.go`）。
- **续期**：15 天阈值，certmagic OnEvent 回调；node-service 侧 lego 注册表支持 acmedns/alidns/cloudflare（`dns_registry.go`），DNS 凭证加密存储。
- **TLS 材料内联**：`crypto/payload.go` 的 `PayloadContent.TLSMaterials` 把 PEM 随加密 Manifest 下发，证书从“文件系统依赖”升级为“版本化配置链路”。

### 5.2 端口路由设计

端口规划收敛在 `apps/node-service/internal/service/port_planner.go`：

| 协议/形态 | 端口段 | 说明 |
|---|---|---|
| REALITY / 直连 TCP+TLS | 30000–30200 | P5 后绕过 nginx stream |
| CDN HTTP 回源 | 8446–8600 | nginx 反代到 xray 127.0.0.1 |
| Hysteria2 UDP | 40020–40200 | 单节点主端口递增，端口跳跃可覆盖区间 |
| TUIC UDP | 40210–40299 | 与 Hy2 区间隔离 |
| ShadowTLS | 30300–30349 | |
| AnyTLS | 30350–30399 | |
| Tunnel 回源 | 20530–20699 | cloudflared |
| 保留端口 | 443 | 永远属于 nginx stream SNI 分流，xray inbound 禁止绑定 |

`AllocateServerPort` 三层防护：范围合法性断言 → 443/系统保留端口硬校验 → 最终安全断言。README 中 9450–9600 旧端口与 `10085–10584` 等描述已过时，迁移 `000066` 已切到 30000+ 段。

### 5.3 协议分析

**协议覆盖**（NodeSpec 支持矩阵）：VLESS、VMess、Trojan、Shadowsocks（含 SS2022）、Hysteria2、TUIC、AnyTLS、Mieru、SOCKS、HTTP；传输层含 WS、gRPC、HTTP/2、QUIC、KCP、HTTPUpgrade、XHTTP；安全层含 none/TLS/REALITY；横切增强含 uTLS、ECH、Mux（yamux/h2mux/smux/xmux）。

**17 个预设**（`packages/subscription/presets/`）：

| 预设 | 协议组合 | 画像 |
|---|---|---|
| P01 | VLESS + REALITY + Vision | 直连主推 |
| P02 | Trojan + TLS | 直连 |
| P03 | VLESS + WS + TLS | CDN 友好 |
| P04 | Trojan + WS + TLS | CDN 友好 |
| P05 | AnyTLS | 新协议 |
| P06/P07 | XHTTP 上下行分离（CDN/REALITY 混搭） | 实验性 |
| P08 | XHTTP + TLS + CDN | CDN |
| P09 | XHTTP Stream-up + REALITY + XMux | 主推 |
| P10 | VLESS + HTTPUpgrade + TLS | CDN |
| P11 | Hysteria2 | UDP/抗封锁 |
| P12 | TUIC v5 | UDP |
| P13 | WARP MASQUE Overlay | Overlay |
| P14 | VLESS + WS + TLS + SS2022 | 纵深 |
| P15 | REALITY + SS2022 | 纵深 |
| P16 | Trojan + gRPC + TLS | CDN |
| P17 | XHTTP Stream-up + REALITY + XMux v4v6 | 双栈 |

**协议层风险**：
- sing-box 对 XHTTP 标记为不支持（`kernelrender/singbox.go` 明确注释，用 httpupgrade 兼容），因此 XHTTP 节点必须走 xray 辅内核；这也正是 `_xray_config` 内嵌机制存在的理由。
- REALITY 私钥硬编码回退已被删除（`reality_keys_test.go` Bug-B1），现在必须显式提供私钥（`preflight_validator.go`）。
- Xray 的 ECH 不支持以结构化 Warning 返回；ECH 仅 sing-box 支持。

### 5.4 TLS 分离分析

TLS 终止分类收敛在 `apps/node-service/internal/service/termination_class.go`：

| TerminationClass | 判定 | TLS 终止点 | nginx stream | 证书需求 |
|---|---|---|---|---|
| `cf_edge` | argo_tunnel | CF 边缘，源站明文 | 否 | 否 |
| `nginx` | cdn / cdn_saas | nginx 8445 vhost，proxy_pass http 回源 | 是（仅 CDN） | 否（nginx 持证书） |
| `self_tcp` | 直连 TCP+TLS | xray 自身 | 否 | 是 |
| `self_udp` | hysteria2/tuic | xray 自身（UDP 不经 nginx） | 否 | 是 |
| `reality` | security=reality | REALITY 握手，无传统证书 | 否 | 否 |
| `nginx_plus_xray` | 已退役常量 | 仅向后兼容 | — | — |

**TLS 分离（客户端/服务端字段分离）仅允许 argo_tunnel**（`tls_split_validator.go`）：
- 服务端 DB `SecurityType=none`（cloudflared 明文 HTTP 回源）。
- 客户端 `config_json.security_type=tls`、`security=tls`、`tls=1`（客户端到 CF 边缘必须 TLS）。
- 非分离节点禁止出现 `DB=none + config_json=tls` 的矛盾态。

**443 冲突自愈**：443 恒归 nginx stream `ssl_preread` SNI 分流；CDN 节点按 SNI 转发到 8446+ 回源端口；UDP 与 REALITY 直连节点完全绕过 nginx，天然避免 443 端口冲突。

### 5.5 证书自动下发逻辑与自签/真实证书双模式（08-03 落地）

**自动下发链路（四级回退 + content 下发）**：

```text
injectCertFromBundle：cert_bundle_id 精确 → cert_bundles SAN 匹配 → tls_certificates SNI 匹配 → 自签兜底
  → PEM 写入 node.ConfigJSON
  → buildTLSMaterials 聚合成 payload TLSMaterials（加密 manifest）
  → agent SetContentPEM 原子写 /etc/yundu/certs/{domain}/，xray/sing-box/nginx 共享
```

**自签模式（伪装直连节点）**：

- 自签证书持久化到 `cert_bundles`（`provider=self-signed`，版本递增）：首次生成后落库，进程重启不重建、指纹稳定，客户端 pin 长期有效。
- SNI 使用伪装域名（`cn-hnzz-cm-01-01.bilivideo.com`、`cn-beijing.cdn.aliyuncs.com` 等），系统不会为伪装域名自动申请真实证书。
- 客户端校验默认 `allow_insecure=true`（sing-box `insecure`、Clash `skip-cert-verify` 全客户端自动生效）；`pinSHA256` 仅作为 URI/Xray 高级选项。
- 保存一致性校验 `CertConsistencyWarnings`：最终来源为自签、且未开 insecure、无 pin 时返回黄色警告，只提示不阻断、不自动改配置（手动优先原则）。

**真实证书模式（可控域名）**：

- 面板 TLS 证书页创建 ACME 证书，自动继承面板全局默认账户（`system_settings.acme`，DB 优先、`.env` 兜底；DNS 凭证加密存储），Token 只在面板配置一次。
- lego DNS-01（cloudflare/alidns/dnspod/gandi/namesilo）或 HTTP-01；签发成功后自动同步 `cert_bundles`，节点渲染 SAN 匹配即用真实证书。
- ACME 证书的 SAN 由管理员显式维护：节点保存钩子与 24h SAN 批量任务均跳过 `cert_type=acme`，避免伪装域名被合并进 ACME 证书导致 DNS-01 失败。
- 生产验证：`seed4.xinti.na.am` 已签发 Let's Encrypt 真实证书（RSA，90 天），vps81 节点证书目录同步替换，客户端可关闭 insecure 做严格校验。
- 通配符支持：证书 SAN 支持 `*.xinti.na.am` 单层通配符（DNS-01 签发），节点匹配采用“精确 SAN 优先、通配符兜底”，一次申请覆盖后续所有子域名，已有精确证书的节点不会被通配符覆盖。

**模式选择原则**：自签与真实证书是显式二选一；自签适合伪装（无需域名/token），真实证书适合严格校验（需可控域名且 token 覆盖对应 zone）。系统只提供判定与提示，不自动改写用户配置。

---

## 六、流量监听与限制（人数、设备）

### 6.1 流量采集链路

```text
sing-box ConnTracker（atomic.Int64 per-user）
xray StatsService（gRPC 10085, 非破坏性读取）
        │ 每 60s 读取增量 → 合并 pending → TrafficBuffer（失败持久化）
        ▼
POST /api/v1/agent/traffic/report → traffic-service
        │ 凭证反查 user_id → RecordUsage
        ▼
Redis 在线标记 + PostgreSQL 日流量 + 每日 23:55 统计归档
```

关键实现：
- `runtime/conntracker.go`：包装连接 Read/Write，`atomic.Int64` 累加，`Swap(0)` 实现增量；`ActiveUserCount()` 基于连接生命周期精确统计在线人数。
- `cmd/agent/traffic.go`：非破坏性读取 + baseline 增量 + 计数器重置检测（`current < baseline` 退化为全量）；baseline 与 buffer 均持久化，Agent 重启不丢不重。
- `traffic-service`：`ReportTraffic` 按 credential 反查 user_id（缓存同一请求内结果）；`GetOverview` 优先从 `channel_health_current` 聚合全站在线人数，失败回退 Redis / session。
- `StatisticsService.DailyStatistics`：每日 23:55 归档 upload/download/active_users/online_count 到 `traffic_statistics_daily`。

### 6.2 限速

- **Xray**：`kernelrender/xray.go` 为每个唯一限速值分配独立 `policy.levels`，`statsUserOnline` 开启在线追踪。
- **sing-box**：`limiter/speed_limiter.go` 每用户令牌桶（`golang.org/x/time/rate`），burst 下限 64KB；通过 `ConnTracker` 的 `Wait(ctx, uuid, n)` 在 Read/Write 路径阻塞限速。

### 6.3 设备数限制

- `limiter/device_limiter.go`：本地 IP 集合 + 面板下发的跨节点全局设备数，取 `max`；全局态超 60s 过期退化为本地判定；同 IP 多连接复用引用计数不重复计设备。
- `executor/device_enforcer.go`：每 15s 轮询 xray gRPC `GetAllOnlineUsers` + `GetStatsOnlineIpList`，超限用户经 `AlterInbound` 移除；空响应连续 3 次且 QueryStats 无流量才清空本地设备态，防 gRPC 抖动误杀。
- 面板侧 `traffic-service/device_state_service.go`：Redis Hash（`{nodeId}:{ip} → timestamp`），TTL 5 分钟，SCAN + 前缀删除支持节点断连清理。

### 6.4 IP 黑白名单

`limiter/ip_limiter.go` 实现黑名单 > 白名单 > IP 数限制三级判定，同一 IP 重连始终放行，并定期清理空条目防内存泄漏。

**评价**：链路完整、容错意识强（持久化、非破坏读取、防误清）；但 xray 侧限速仍依赖 StatsService 轮询（`native_xray.go:547 TODO(U1)`），sing-box 是原生连接级执行，两核能力不完全对称。

### 6.5 直连节点 tc 限速落地与避坑（08-03）

**现象**：日志刷 `tc qdisc/class add ... exit status 2`，每用户限速不生效。

**根因（与“缺 CAP_NET_ADMIN”判断不同）**：

- agent 以 root 运行且 `CapabilityBoundingSet` 含 `cap_net_admin`，权限本身无问题；qdisc/class 的 exit 2 是“已存在”的幂等噪声。
- 旧版 agent（0.7.13）未启用 `SpeedEnforcer`，tc 整形从未真正执行。
- `ensureBaseLocked` 每次循环都执行 ingress `tc filter add`，同款规则可重复添加，实测累积 8681 条（已清理）。

**解决**：

1. `tcshape.go` 对 ingress filter 改为“先删除同款再添加”，保持 1 条、幂等收敛。
2. node-agent 升级 v0.7.22（按 CI 完整 tags 编译：`with_utls,with_wireguard,with_gvisor,with_quic,with_grpc,with_clash_api`）。
3. 验证：vps206 `speed enforcer started`，管理直连端口 30002-30013，tc filter 收敛 1 条；vps81 无直连端口自动跳过。

**避坑**：编译 node-agent 必须使用 CI 完整 tags；缺少 `with_wireguard` 会导致 sing-box 启动失败并回滚（本次实测事故，正确构建后恢复）。

---

## 七、WARP 内置（重点）

### 7.1 架构分层

WARP 分为“控制面账户管理”与“执行面出口管理”两层：

**控制面**（`apps/node-service/internal/warpreg/`）：
- `Registrar`：调用 Cloudflare 注册 API，curve25519 密钥对 + 90s 注册频率限流；支持 WARP+ License 绑定、优选 Endpoint 覆盖、IP 轮换（RotateIP 保留旧 License）、账户健康检查。
- `Pool`：`RegisterForNode` / `ImportExisting`，自动生成 `warp-{n}` outbound tag，写入 `warp_profiles` 表（含 DeviceID/AccessToken/PrivateKey/双栈地址）。

**执行面**（`apps/node-agent/internal/warp/`）：

| 模式 | 实现 | 内存 | 适用 |
|---|---|---|---|
| `real` | warp-cli 系统服务，SOCKS5 `127.0.0.1:40000` | 30–50MB | 默认 |
| `wireproxy` | userspace WireGuard 子进程，SOCKS5 `127.0.0.1:40001` | ~4MB | ≤150MB 内存约束 |
| `mock` | 假实现 | 0 | 本地开发/CI |

`WARP_MODE` 环境变量选择；wireproxy 凭证来自 `WARP_PRIVATE_KEY` 等环境变量，支持 IPv4/IPv6 双栈（AllowedIPs 自动扩展 `::/0`）。

### 7.2 出口接入

- **sing-box 优先原生 wireguard outbound**：有 `private_key` 时渲染 wireguard（含双栈 local_address、MTU），否则回退 wireproxy SOCKS5。
- **xray 用 SOCKS5 outbound**（`warp-out` tag）。
- 节点级 WARP 出口重构（commit `5c5e646`）：inbound 精确路由替代全局 `route.final`，不同节点可走不同 WARP 账户/直连。
- WARP 状态每 5 分钟上报面板：`warp_status / warp_ip / warp_latency_ms`，未运行自动重连。

### 7.3 近期修复

- `ed209c0`：sing-box wireguard endpoint 地址自动补全 CIDR（修复 vps206 local_address 无 `/32` 校验失败）。
- `9aa83db`：`EnableWarpForNodes` 自愈补建缺失的 load_balance 策略（修复勾选后仍直连）。
- `c3646c1`：WARP 负载均衡 + sing-box 配置 `route` 字段修正 + tag 去重（避免 duplicate tag）。
- 后续方向：MASQUE/Zero Trust 设计（技术指导目录有 `WARP-零SSH-负载均衡方案-singbox原生-load_balance-20260729.md` 与 MASQUE 交接文档），`P13-warp-masque-overlay.yaml` 预设已就位。

**风险点**：wireproxy 不支持自动安装（防运行时下载安全风险，需预装二进制）；warp-cli 依赖系统包安装；WARP 账户凭据明文存储于 DB/环境变量，需保证表级权限与密钥轮换。

---

## 八、链式代理 / 中转链路（重点）

### 8.1 领域模型

`packages/subscription/chain/chain.go`：

- `ChainSpec` = `LandingNode`（落地节点）+ `Relays []ChainHop`（中转跳）+ `TrafficPolicy`（`bill_at_landing` / `bill_at_entry`）。
- `ToNodeSpecs()` 将链路展平成 NodeSpec 数组：每个 hop 以 `tag` 命名（`relay-N`），`ParentNodeID` + `Tags` 建立父子引用，landing 收尾。
- 校验覆盖：地址、端口 1–65535、协议/传输/安全合法、计费策略枚举。

### 8.2 统一渲染

`chain/render.go` 提供唯一入口：

- `RenderChainForKernel(kernel, spec)` → xray/sing-box 的 outbounds + routing。
- `RenderOutboundFromNodeSpec(kernel, ns, tag, detourTag)` → 单跳套娃出站构建。
- 双内核分别实现：`chain/render_xray.go`（outbounds 数组 + 父 tag detour）与 `chain/render_singbox.go`。
- `apps/node-service/internal/exposure/chain_*.go` 现为薄包装（P1-1 迁移后保留兼容），`chain_uri.go` 支持从 URI 导入跳点。

### 8.3 服务端链路

- `apps/node-service/internal/service/chain_service.go` + `repo/chain_repo.go`：chain CRUD、绑定节点、优先级排序（`ORDER BY priority ASC`）。
- `model/chain.go`：链到 NodeSpec 的映射，支持多跳。
- 下行/上行分流（XHTTP split mode）与链式代理可叠加；`_chain_bridges` 元数据在 Agent 端合并到主配置。

### 8.4 计费策略

`bill_at_landing` / `bill_at_entry` 双策略已建模，决定流量计入落地节点还是入口节点；订阅渲染按链整体暴露，用户拿到的是套娃后的统一入口。

**评价**：模型清晰、双核渲染统一、URI 导入与校验完整；扣分点是 exposure 薄包装尚未删除、多跳链路的前端配置体验（admin-web ProxyChains 页面）仍偏“技术员向”。

---

## 九、整体健康度评分

参考 730 体检报告（技术指导目录 `YunDu-项目深度体检报告-yundu-src版-20260730.md`）的分维度口径，本次按 8-01 代码现状独立复核：

| 维度 | 评分 | 依据与扣分点 |
|---|---:|---|
| 系统架构 | 9.0 | 三平面解耦完整、ADR 齐备、服务边界清晰；扣：architecture.md 过期、两份计划文档为 0 字节 |
| 渲染器架构 | 8.5 | 注册表/IR/预设/客户端渲染完整；扣：validator 红测试、RawConfig 逃生舱、DownstreamTagSuffix 临时契约 |
| 双内核运行 | 9.0 | 主辅翻转彻底、蓝绿热转、`_xray_config` 内嵌、delta sync 完整；扣：xray 限速 TODO、蓝绿 drain 只告警不强制 |
| Agent 骨架与推送 | 8.5 | 三通道、八阶段流水线、LKG、自升级、nginx 原子同步全部落地；扣：main.go 超 2100 行、通道默认 insecure、bak 残留 |
| 证书架构 | 8.5 | 六模式 + 四级回退 + SAN 同步 + 原子热替换；扣：DNS 凭证与密钥管理依赖环境变量 |
| 流量与限制 | 8.0 | 原子计数 + 三限制器 + 容错上报完整；扣：xray 设备限制靠轮询、限速能力两核不对称 |
| WARP | 8.5 | 账户池/注册/License/双模式出口/精确路由齐备；扣：wireproxy 需预装、账户密钥明文存储风险 |
| 链式代理 | 8.5 | 统一入口 + 双核渲染 + 计费策略；扣：exposure 薄包装待删、UI 体验 |
| 订阅/网关 | 8.0 | 40+ 客户端、模板注入、LB 六策略；扣：站点名与 LB sticky secret 硬编码默认值、compat 同步留 TODO |
| 工程卫生 | 6.5 | 红测试 1 例、空文档、过期 README、二进制/bak/临时目录残留、工作区未提交改动 |
| **加权总分** | **8.2 / 10（B+）** | 与 README 标注一致，但工程卫生若不处理会持续拖累 |

**综合结论**：核心运行面（双内核、下发、证书、WARP、链式）已进入可运维状态，当前主要失分来自“文档与仓库卫生”和“非主链路半成品”，而非核心架构。

---

## 十、核心对比：YunDu vs Xboard vs Xboard-Node vs 3X-UI

> 对比依据：本仓库代码、`docs/architecture.md` 对 XBoard 的记录、`apps/node-service/internal/provider/threexui_provider.go` 对 3X-UI 的对接事实，以及技术指导目录中的官方仓库分析文档（2026-07-30 抓取口径）。

| 维度 | YunDu | Xboard | Xboard-Node | 3X-UI |
|---|---|---|---|---|
| 语言/形态 | Go 微服务 + Go Agent | PHP/Laravel + Caddy/Octane 单体 | PHP Agent | Go 单体 + xray |
| 架构 | 控制面/执行面/订阅面三平面，monorepo | 单体面板 + 独立节点 Agent | 节点端 Agent | 面板 + xray 同机 |
| 内核策略 | sing-box 主 + xray 辅，Go 库内嵌双核并行 | 单内核（xray）为主 | 跟随面板 | xray 单核 |
| 配置下发 | 加密 Manifest + gRPC/WS/HTTP + Delta Sync + LKG | HTTP 拉取/推送 | HTTP | API 推送，无版本化回滚闭环 |
| 零 SSH 运维 | 自升级、nginx 骨架、防火墙、证书、WARP 全自动 | 需要人工运维较多 | 部分 | 面板内管理，节点系统操作仍需 SSH |
| 证书 | 六模式 + 四级回退 + SAN 同步 | 依赖站点/Nginx 证书 | 简化 ACME | 面板证书管理 |
| 商业化能力 | 套餐/订单/优惠券/返佣(ERC20)/工单/支付 | 成熟商业功能 | 无 | 无 |
| WARP | 内置账户池 + License + 双模式出口 | 无内置 | 无 | 社区脚本 |
| 链式代理 | 双核统一渲染 + 计费策略 | 有基础链式 | 跟随 | 有 outbound 链 |
| 可观测性 | Prometheus/Grafana/Loki/OTel | 基础 | 无 | 基础 |
| 3X-UI 对接 | 已有 provider 骨架（仅登录 + 在线数拉取） | 无 | 无 | 自身 |
| 成熟度 | 开发中，B+ | 生产成熟 | 生产成熟 | 生产成熟（个人/VPS 向） |

**定位结论**：Xboard 是“运营成熟但架构传统”的商业面板；3X-UI 是“节点控制强、商业化弱”的 VPS 面板；YunDu 选择的是“商业化 + 双内核 + 零 SSH + 可观测”的工程化路线，当前处于从 XBoard 并行生产向自研 Go 底座切换的中后期。

---

## 十一、可借鉴点：从 3X-UI 与 Xboard-Node 学什么

### 11.1 从 3X-UI

1. **WARP 一键注册体验**：`warpreg/pool.go` 已明确对齐 3x-ui 的“填 License 后一键注册”流程（注册 → SetLicense → 优选 Endpoint），可继续补齐批量注册/批量轮换的 UI。
2. **多账户池与优选 Endpoint**：3X-UI 生态对 WARP 优选 IP 的玩法成熟，YunDu 已有 `Endpoint` 覆盖与 `RotateIP`，可增加自动健康切换（坏账户自动下线并轮换）。
3. **面板内 xray 全参数管理**：3X-UI 对 inbound 的细粒度表单管理值得参考，YunDu 的 NodeConfigEditor 已用预设 + JSON 编辑器混合，可进一步把高频参数（flow、mux、padding、obfs）收敛为可视化控件。

### 11.2 从 Xboard-Node

1. **ConnTracker / limiter / TrafficBuffer 移植路径已验证**：代码注释明确这些能力移植自 Xboard-Node（conntracker、speedtracker、RestoreTraffic），说明其流量与设备限制实现是成熟参照。
2. **多面板适配**：Xboard-Node 的多面板 Agent 思路与 ADR-0001 的 provider 抽象一致；YunDu 应把 provider 接口继续做实，而不是被 3X-UI skeleton 卡住。
3. **运维手册沉淀**：Xboard 系有大量生产运维 runbook；YunDu 的 `docs/runbooks/`（727 complete、vps206 维护、node-domain、db-bootstrap）方向正确，应补齐 801 后的标准化巡检清单。

---

## 十二、超越方案（按优先级）

### P0：止血工程卫生（1–2 天）

1. 修复 `validator` 红测试（对齐 SNI 警告规则或更新断言），恢复 `packages/subscription` 全绿。
2. 删除仓库中的 `.bak.20260731`、二进制产物（`node-agent`、`node-service-linux`、`agent`、`admin-dist.tgz`）、`.qoder/`、`.tmp-readme/`，并补 `.gitignore`。
3. 重写/填充 0 字节文档，更新 README 基线、端口表、健康度评级与待办（10000 端口防火墙、端口常量收敛、ACTION_DRAIN）。
4. 移除 `subscription_handler.go` 硬编码站点名 “YunDu” 与 `lb/engine.go` 默认 sticky secret（接入配置项），并补 config 校验。

### P1：下发状态机与自愈补完（1 周）

1. 将注册阶段 Agent 版本门禁从观察模式转为强制拒绝（对齐简化版 D 工程）。
2. `dispatch_status` 增加 TTL 指标与 Grafana 告警（`failed > 5min` 自动重试 3 次仍失败转人工）。
3. 补齐 Delta Sync 的用户级 ack 回写与 `applied` 全链路展示；为 `config_version` 增长速率加观测指标，防止排序回归再次引发版本风暴。
4. 蓝绿 drain 超时从“仅告警”升级为“可配置强制断开”。

### P2：双内核能力对齐（2–4 周）

1. 实现 xray per-user 限速（`native_xray.go TODO(U1)`），让两核限速语义一致。
2. 退役 `DownstreamTagSuffix` 临时判定，上线 `downstream_exposure_mode` 字段驱动的下行 inbound 识别。
3. 删除 `exposure/chain_*.go` 薄包装，全部调用收敛到 `packages/subscription/chain`；同时把多跳链路做成可视化配置（中继节点拖拽/测速/一键测试）。
4. 为 Machine 模式补端口分配冲突检测与 10000 控制端口防火墙规则（已知待办）。

### P3：WARP 产品化（1–2 周）

1. wireproxy 二进制由面板版本管理接口下发（带 SHA256），消除“手工预装”门槛，同时保留禁止运行时任意下载的安全边界。
2. 多账户池自动健康巡检：坏账户自动禁用 → RotateIP → 重新绑定，面板展示每个节点 WARP 出口 IP 与延迟趋势。
3. 落地 MASQUE/Zero Trust 路线（P13 预设已有雏形），形成“WARP 普通出口 / WARP+ / MASQUE”三档能力。

### P4：高可用与多控制面（按业务规模）

1. gRPC/WS 通道强制 TLS（当前默认 insecure，需要证书时仍可配）。
2. node-service 与 traffic-service 支持多实例 + 数据库队列化下发（避免推送集中在单进程内存 session）。
3. 订阅服务接入站点设置存储，补齐模板/域名/客户端兼容矩阵的远程同步（`compat/service.go TODO(task-32)`）。
4. 3X-UI provider 从 skeleton 完善为可用适配器（login → inbounds add/update → stats），或明确退役该能力。

---

## 十三、维护建议与风险

### 13.1 维护建议

1. **以 801 简化版为状态基线**：每次改动后跑一遍“验收 SQL + 日志关键字”巡检（维护指南 §14），核对 `runtimes` 无重复、`dispatch_status` 无 >5min failed、版本号无 10 分钟 >5 条异常增长。
2. **测试门槛**：将 `packages/subscription`、`node-agent/internal/{hotdiff,pipeline,validator,warp}` 测试纳入 CI；当前 CI 只有编译发布，没有测试 job。
3. **文档与代码同源**：恢复 7-24 两份文档并建立“架构决策 → 迁移 → runbook”链路；README 的端口/基线/评分随每个 release 更新。
4. **密钥与敏感信息**：确保 `.env` 不进入仓库；WARP 账户凭据、ACME DNS 凭证的加密与访问控制补审计；AGENT_API_TOKEN_SALT/JWT/HMAC 强制非默认值。
5. **灰度纪律**：内核翻转、WARP、端口迁移等重大变更沿用 727 runbook 的灰度流程（vps206 30 分钟判定 → 全量）。

### 13.2 风险清单

| 风险 | 等级 | 说明与建议 |
|---|---|---|
| 文档漂移 | 中 | architecture.md/README/空文档与代码脱节，接手成本高；优先重写 |
| 测试红 | 中 | validator 红测试会掩盖后续真实回归；先修到全绿再谈新功能 |
| 密钥默认值 | 中 | compose 中 JWT_SECRET/HMAC/ARGON2_SALT 有 `change-me` fallback，上线必须覆盖 |
| 通道明文 | 中 | gRPC/WS 未配证书时明文；Agent 与面板跨公网时应强制 TLS |
| 仓库卫生 | 低中 | 二进制与 bak 文件可能泄漏环境信息、增大镜像体积 |
| 3X-UI 依赖 | 低 | 仅 skeleton，若业务依赖混用 3X-UI 需明确定位并完善 |
| 单进程瓶颈 | 低中 | node-service 内存 session 推送，多面板/大规模节点需队列化 |
| WARP 账户风险 | 中 | Cloudflare 注册频率与风控（90s 限频已有），需监控注册失败率 |

---

## 十四、2026-08-03 生产任务避坑清单

### 14.1 Agent 编译与升级

- node-agent 必须按 CI 完整 tags 编译：`with_utls,with_wireguard,with_gvisor,with_quic,with_grpc,with_clash_api`。缺 `with_wireguard` 会导致带 WARP/WireGuard 的 sing-box 配置启动失败并回滚（本次实测事故）。
- 面板“Agent 升级版本库”页可用：填写 version/download_url/sha256 发布一次，全部节点自动升级；`sha256` 必须与实际二进制一致，否则 `checksum mismatch`。二进制可放 vps190 `/opt/yundu/agent-upgrade/`，download_url 用面板自身 `/api/v1/agent/download/node-agent-linux-amd64`，避免依赖 GitHub。
- 发布前确认当前节点运行的本地修复版本（如 v0.7.22 的 tc-shape/nginx Host 修复）已合入正式 release，否则升级会覆盖修复。

### 14.2 版本风暴

- 渲染确定性契约必须覆盖所有进入 hash 的数据源：节点列表（priority,id）、limiter users（email/uuid）、outbound/chain 等；`_limiter.users` 是 801 之后首个复发源。
- 任何 `config_versions.applied_at` 类代码改动必须同步补迁移（本次缺失导致 ack 永久失败）。
- `config_versions` 历史表会无限膨胀：保留最近 N 条 + 定期清理；删除前先处理 `config_payloads` 等引用（本次 75768 → 953）。

### 14.3 证书

- DNS-01 token 权限必须覆盖目标 zone（Cloudflare API token 按 zone 授权）：给错 zone 的 token 会报 `zone could not be found`；一次配置覆盖全部要签名的 zone 即可，无需每域名一个。
- ACME 证书 SAN 禁止自动合并节点 SNI（节点保存钩子与 24h SAN 批量任务已跳过 `cert_type=acme`），否则伪装域名会被合并导致 DNS-01 失败。
- `cert_bundles` 的 SAN 查询必须用 `jsonb_array_elements_text`，`san::text[]` 会报 `cannot cast type jsonb to text[]`（已修复）。
- obtain/renew 是同步长请求，超过 10s 会先返回网关 504，但后台可能已成功（本次多次出现）；建议后续改为异步任务。
- 自签节点必须显式开 `allow_insecure` 或配置 pinSHA256，否则客户端严格校验失败；保存时已有黄色告警。自签证书已持久化到 cert_bundles，重启不换指纹。
- 真实证书与自签的取舍：自签适合伪装 SNI（无域名/token 依赖）；真实证书需可控域名且 token 覆盖 zone，客户端可严格校验。

### 14.4 限速与 nginx

- tc-shape 的 `exit status 2` 多数是 qdisc/class “已存在”的幂等噪声，不代表权限缺失；真正要防的是 ingress filter 重复累积（已改为先删后加）。
- CDN 回源 Host 固定为 `server_name` 后与“优选 IP”兼容（优选只改 TCP 目标，SNI 仍是域名）；约束是节点 transport host 必须与 SNI/server_name 一致。

---

## 附录 A：关键文件索引

| 关注点 | 文件 |
|---|---|
| 三平面入口 | `apps/api-gateway/internal/app/app.go` |
| 服务边界 | `docs/adr/0001-system-architecture.md` |
| gRPC 协议 | `packages/proto/agent/v1/channel.proto` |
| 统一 IR | `packages/subscription/nodespec/spec.go` |
| 双核渲染 | `packages/subscription/kernelrender/{renderer,xray,singbox}.go` |
| 双核运行时 | `apps/node-agent/internal/runtime/multi_runtime.go`、`native_singbox.go`、`native_xray.go` |
| `_xray_config` 链路 | `apps/node-service/internal/service/deployment_service.go`、`apps/node-agent/cmd/agent/main.go` |
| 下发状态机 | `apps/node-service/internal/service/deployment_service.go` |
| 证书 | `apps/node-agent/internal/cert/acme.go`、`apps/node-service/internal/cert/service.go` |
| 端口规划 | `apps/node-service/internal/service/port_planner.go` |
| TLS 分类 | `apps/node-service/internal/service/termination_class.go`、`tls_split_validator.go` |
| 流量 | `apps/node-agent/cmd/agent/traffic.go`、`apps/traffic-service/internal/service/traffic_service.go` |
| 设备/限速 | `apps/node-agent/internal/limiter/`、`internal/executor/device_enforcer.go` |
| WARP | `apps/node-agent/internal/warp/`、`apps/node-service/internal/warpreg/` |
| 链式代理 | `packages/subscription/chain/`、`apps/node-service/internal/exposure/chain_common.go` |
| 订阅 | `apps/subscription-service/internal/handler/subscription_handler.go`、`internal/lb/engine.go` |
| 部署 | `deploy/docker/docker-compose.full.yml`、`install.sh`、`.github/workflows/release.yml` |
| 运行手册 | `docs/runbooks/yundu-727-complete-runbook.md` |

## 附录 B：测试与验证记录

```text
go test ./...   (packages/subscription)
  ok: chain / inboundgroup / kernelrender / nginxrender / nodespec / renderer / ruleset
  FAIL: validator.TestEnhancementValidator_RealitySNIWarning
        validator_test.go:213 期望 apple.com SNI 触发警告，实际未触发

go test ./internal/hotdiff/... ./internal/pipeline/... ./internal/validator/... ./internal/warp/...  (apps/node-agent)
  ok: 4 个包全部通过
```

未执行：全量 Go 测试（依赖较多外部模块且耗时）、前端 build（Node 22/pnpm 依赖未在本次环境安装）。上表为代码事实复核 + 定向测试结果。

---

## 附录 C：2026-08-03 生产变更与验证记录

```text
版本风暴：
  vps206 版本 10858 停止增长，vps81 10623；10 分钟窗口 0 新增
  config_versions 75768 → 953；applied_at 迁移 000077 applied
  xray 重启归零，bad host 日志归零

证书：
  seed4.xinti.na.am 签发 Let's Encrypt 真实证书（RSA，90 天）
  vps81 /etc/yundu/certs/seed4.xinti.na.am issuer=Let's Encrypt
  cert_bundles 新增 provider=self-signed 持久化包（seed4 / bilivideo / tdnsv6）
  面板默认 ACME 账户入库（system_settings.acme：email/dns-01/cloudflare）

限速：
  vps206 speed enforcer started（ports 30002-30013）
  tc ingress filter 从 8681 条收敛到 1 条
  vps81 无直连端口，speed enforcer 正确跳过

部署：
  node-service v0.7.22（多轮备份 node-service.bak.*）
  node-agent v0.7.22（vps206/vps81，备份 node-agent.bak.v0722full.*）
  admin-web 重新构建部署（默认 ACME 账户设置卡片）
  agent-upgrade info.json 已禁用（原 0.7.23 sha256 不匹配）
```

---

*报告完。调查日期 2026-08-01（正文基线），2026-08-03 追加生产修复与证书策略落地（见 4.8 / 5.5 / 6.5 / 十四 / 附录 C）。*
